---
name: project-map
description: Карта проекта Telegram-бота C4_Max (War Thunder Mobile) — структура файлов, все команды, кнопки, автоматика, скрытые зависимости и хрупкие места. ОБЯЗАТЕЛЬНО читать перед ЛЮБОЙ правкой кода в этом проекте — даже мелкой: перед изменением обработчиков, панелей, антиспама, новостей, RAG, базы данных или конфига. Также использовать, когда пользователь спрашивает «что умеет бот», «где находится X», «что сломается, если поменять Y». После правки, меняющей структуру, команды, кнопки или зависимости — обновить эту карту.
---

# Карта проекта: бот C4_Max

## Инструкция для Claude (для самого себя)

1. **Перед любой правкой кода** — прочитай эту карту целиком. Особенно разделы
   «Зависимости» и «Хрупкие места»: там перечислено, что ломается по незнанию.
2. **После любой правки**, которая меняет структуру файлов, команды, кнопки,
   автоматическое поведение или зависимости — обнови соответствующий раздел карты
   в этом же коммите работ. Карта, отставшая от кода, хуже, чем её отсутствие.
3. В конце каждой сессии с правками кода — пройди «Самотест» внизу файла.
4. Карта построена по коду на 2026-07-06, полная сверка с кодом — 2026-07-09
   (расхождений не найдено); обновление 2026-07-13 (аудит: экранирование HTML,
   неблокирующие хендлеры, лимит картинок авансом); обновление 2026-07-16
   (режим «Сам в разговор»: services/proactive.py, ask_group_proactive,
   архив своих ответов, блок в панели промптов; в тот же день по решению
   Максима: отвечает АКТИВНАЯ модель с обычным фолбэком — принудительная
   бесплатная цепочка force_chain УДАЛЕНА, мысли в репликах показываются);
   обновление 2026-07-17 (журнал модерации: графа admin_name — кто снял мут,
   показывается в /mod припиской «(снял: …)»; новости при рассылке в ГРУППЫ
   пишутся в архив групп — стенограмма «Сам в разговор» теперь видит новость,
   которую обсуждают участники); обновление 2026-07-18 (панель /rag: статьи
   в ТРИ столбца вместо двух, потолок `_KB_LIST_LIMIT` поднят 40 → 84 —
   решение Максима «вместить все статьи»; запас до потолка Telegram
   ~100 кнопок на сообщение с учётом 12 служебных); обновление 2026-07-19
   (лимит Google на эмбеддинги: индексация статей ждёт при 429 — паузы
   10/30/60 сек, 0.5 сек между статьями, RagQuotaError при исчерпанной
   квоте; «Пересобрать RAG» — фоновой задачей с защёлкой kb_rebuild_running
   и честным итогом «проиндексировано X из Y»; в тот же день по решению
   Максима: пауза между статьями поднята до 1 сек и добавлен ежечасный
   автодобор базы rag_catchup_loop в jobs.py — дешёвая проверка полноты
   index_lag() без запросов к Google, уведомления админам в личку только
   на границах инцидента «⚠️ неполная / ✅ снова полная»); разбор лога
   2026-07-19 и четыре правки по его итогам: тело ответа сервера в лог при
   отказе модели (_err_body), альбом фото = одно отправление для антиспама
   (_count_messages), регулятор «📈 Запас над фоном» в панели /rag с новым
   дефолтом 0.14 вместо 0.06 (выверен замером на 30 запросах — см. config.py),
   кнопка уже активной модели не перерисовывает клавиатуру; в тот же день —
   смысловые значки типа техники на кнопках одобренных статей /rag
   (🚜 наземная / ✈️ авиация / 🛥️ корабли / ⚓ подлодки / 📘 механики,
   выбор значков — решение Максима) с сортировкой списка по этим группам:
   `ARTICLE_KINDS` и `detect_kind` в knowledge_store.py, тип читается из
   первой строки описания статьи; значок 👤 вместо 🤖 в логах на входящих
   сообщениях пользователей (легенда в logging_setup.py); в тот же день —
   СМЫСЛОВЫЕ ЗНАЧКИ ПРОВАЙДЕРОВ в логах вместо общего 🤖 (♊️ Gemini/Gemma,
   🐪 Qwen, 🐋 DeepSeek, 📚 поиск по базе знаний; список `PROVIDER_ICONS`
   в config.py — единый для логов и панели «📡 Настройки API») и УДАЛЕНИЕ
   моделей qwen3.6-max-preview и qwen3.6-plus (решение Максима: не
   использовались; убраны из AVAILABLE_MODELS, QWEN_PRICES и раскладки кнопок);
   обновление 2026-07-20 (РАЗДЕЛ «👥 ПОЛЬЗОВАТЕЛИ», этап 1 из трёх — только
   просмотр: новый handlers/admin/panel_users.py, команда /users, кнопка в /adm,
   префикс кнопок usr:; две новые таблицы user_settings и known_chats, графы
   username/first_name в user_dossier; collect_group_message теперь пишет имя
   в личное дело и запоминает группу. На этапе 1 поведение бота НЕ менялось;
   этап 2 (персональные настройки: антифлуд, ссылки, ИИ, картинки) и этап 3
   (действия: мут/размут/кик/бан, очистка контекста, почётное звание) СДЕЛАНЫ
   в тот же день (см. ниже) — весь раздел «👥 Пользователи» закрыт 2026-07-20.
   Решения Максима: настраивать всё перечисленное, действия все четыре, выбор
   человека — только списком кнопок, звание — отдельным «почётным» полем);
   в тот же день ЭТАП 2 — персональные настройки В РАБОТЕ: новый модуль-кэш
   services/user_settings.py (загрузка из main.py), личные пороги антифлуда,
   иммунитет от мута, разрешение ссылок, игнор ИИ (3 обработчика + проактивный
   режим), персональный лимит картинок (единый помощник для /imagine и /rank),
   ручной статус «проверенный» в trust_info; кнопки-регуляторы и тумблеры
   в карточке. Замер: обращений к БД на сообщение группы столько же, сколько
   было до этапа 2 — кэш своё дело делает; и ЭТАП 3 — ручные действия
   (мут на срок, размут, кик, бан, разбан — с выбором группы и подтверждением
   у необратимых), очистка диалога, обнуление нарушений, почётное звание;
   список званий вынесен в config.QUIZ_RANKS; журнал /mod научился показывать
   новые типы действий отдельными значками (раньше всё незнакомое рисовалось
   как «размут»). Раздел «👥 Пользователи» на этом закончен.
   Обновление 2026-07-20 (РОЛИ И ПАНЕЛЬ МОДЕРАТОРА, этап 1 из трёх): новый
   services/roles.py — три уровня доступа, права галочками (тогда пять, с
   конца дня — четыре), ЕДИНАЯ таблица
   «кнопка → право» с запретом по умолчанию; таблица staff в БД; все ~20
   проверок «ты админ» в командах заменены помощником `_require`, единый гейт
   кнопок в роутере теперь спрашивает право; /adm и карточка собираются по
   правам; иерархия (модератор не трогает персонал) и потолок срока мута
   `MODERATOR_MAX_MUTE_SEC`; /adm в меню Telegram появляется у модератора сразу
   при назначении. Этап 2 (журнал действий персонала и уведомления владельцу
   о тяжёлых действиях) и этап 3 (уведомления модераторам: муты в группах,
   запуск/остановка, авто-панель при старте) СДЕЛАНЫ в тот же день (см. ниже) —
   весь раздел «Роли и модер-панель» закрыт 2026-07-20.
   В тот же день ЭТАП 2 (контроль): таблица staff_log и экран «📋 Журнал
   персонала» (только владельцу, в разделе «👥 Пользователи»), запись всех
   действий персонала через `_audit` (модерация, персональные настройки,
   очистка диалога, обнуление нарушений, звания, роли и права, общие настройки
   антиспама, размут кнопкой и командой /unmute), уведомления владельцу о
   киках/банах/разбанах МОДЕРАТОРА через `_notify_owners`, суточная чистка
   журнала в cleanup_loop. И ЭТАП 3 (уведомления модераторам): автоматический
   мут → в личку модераторам с правом `mod` с кнопкой размута
   (`_notify_moderators_mute` в antispam.py); запуск и остановка бота + авто-
   открытие панели — всему персоналу с хотя бы одним правом
   (`main._staff_recipients`). Раздел «Роли и модер-панель» на этом закончен.
   В тот же день ДВЕ ДОРАБОТКИ ПАНЕЛЕЙ (решения Максима): (1) кнопка
   «🧹 Очистить журнал» в панели /mod — стирает журнал модерации ЦЕЛИКОМ
   вместе с уликами (`hist.clear_moderation_log`), с подтверждением, ТОЛЬКО
   владельцу (модератор не должен уметь стереть записи о себе — как у журнала
   персонала); ради неё `_build_mod_panel_text_and_keyboard` и `send_mod_panel`
   получили параметр «кто смотрит». (2) Права модератора переехали из карточки
   на ОТДЕЛЬНЫЙ ЭКРАН «⚙️Настройки Прав Модератора» (`usr:rights:<id>`; кнопка
   переименована 2026-07-20 из «⚙️ Права модератора»): в карточке
   осталась одна кнопка входа и строка-сводка «прав N из 5» (к концу дня — из
   4, см. ниже), а галочки прав,
   пояснения к каждому праву (`hint` из `roles.PERMS` — раньше нигде не
   показывались) и «🚫 Снять модератора» (теперь С ПОДТВЕРЖДЕНИЕМ,
   `usr:role:<id>:offgo`) живут на экране; галочка перерисовывает сам экран,
   а не выбрасывает в карточку.
   В тот же день КНОПКА «🧹Очистить РАЗГОВОРЫ» в панели промптов (стоит
   В ОДНОМ РЯДУ с тумблером «🗣 САМ В РАЗГОВОР», два столбца — решение
   Максима): бот забывает стенограмму бесед во ВСЕХ группах и у всех
   людей сразу, включая персонал. Сделано «чертой», а НЕ удалением (решение
   Максима из двух предложенных вариантов): в settings пишется метка времени
   `proactive_reset_mark`, старше которой стенограмма не читается. Новых
   таблиц и граф — ноль; счётчик «архив группы за 10 дней» в /stats и
   запасной источник имён для /users не задеты; решение обратимо.
   В тот же день ЛОГИ: вместо трёх отдельных файлов-сессий с удалением самого
   старого — ДВА файла в папке `logs` (текущая сессия + общий архив
   `archive.log`, последние 7 сессий); решение Максима — «ничего не терять,
   но папку не захламлять». Подробности в разделах 1 и 4.
   В тот же день ПЕРЕДЕЛКА ДОСТУПА «ВИДИТ = МОЖЕТ НАЖАТЬ» (решения Максима):
   панели больше НЕ решают сами, кому что показывать — клавиатура собирается
   ПОЛНОЙ и прогоняется через `_filter_keyboard(rows, viewer_id)` (common.py),
   который оставляет только кнопки, разрешённые таблицей `_CALLBACK_RULES`;
   ряд, где после отбора остались одни заглушки `…:noop` (среднее значение
   регулятора), выбрасывается целиком. Так собираются /adm, /mod, список и
   карточка /users — прежние проверки `roles.can(...)` внутри сборки кнопок
   УБРАНЫ намеренно, чтобы правило доступа жило в ОДНОМ месте. Заодно: право
   «🔧 Антиспам» УДАЛЕНО из `PERMS` (общие пороги и тумблеры — только
   владелец; графа p_antispam осталась в БД мёртвой), КИК переехал из права
   `mod` в `ban`, а модератору в карточке закрыты пороги/окно/мут, все четыре
   тумблера, «🧹 Очистить диалог» и «↩️ Сбросить всё персональное» — ему
   остаются картинки, мут/размут, обнуление нарушений и почётное звание.
   Прав теперь ЧЕТЫРЕ, на экране прав они рисуются деревом ├ │ └ с подписью
   `hint`. Отдельной панели /mods намеренно НЕ заводили (вопрос Максима):
   две копии одних и тех же панелей разъезжаются при первой же правке.
   В тот же день РАЗВИТИЕ РЕЖИМА «САМ В РАЗГОВОР» — четыре связанные правки
   (план утверждён Максимом; повод — тест 20.07 09-35, где бот пригрозил мутом,
   которого выдать не мог): (1) ПАУЗА СУДЬИ — жёсткая константа
   `PROACTIVE_JUDGE_MIN_SEC` (20 сек) сначала стала регулятором панели, а в тот
   же день УДАЛЕНА ЦЕЛИКОМ по решению Максима — и следом за ней УДАЛЁН ВТОРОЙ
   ТАЙМЕР `PROACTIVE_COOLDOWN_SEC` («пауза после своей реплики»): два регулятора
   тормозили бота по часам, делая практически одно и то же. ⚠️ ВРЕМЕННЫХ ПАУЗ
   В РЕЖИМЕ БОЛЬШЕ НЕТ. Сдерживают бота: порог `proactive_min_msgs` (у Максима
   1), защёлка `_in_flight` и САМА МОДЕЛЬ (правило «ты недавно уже писал»
   в промпте участия — единственный реальный тормоз, и он мягкий). Ключи
   settings `proactive_judge_min_sec` и `proactive_cooldown_sec` мёртвые.
   Таймеры заново НЕ заводить (решение принято дважды): тараторит — лечить
   порогом или промптом. `note_bot_group_reply` и метки `_last_*_ts` остались,
   но больше ничего не тормозят.
   (2) СПРАВКА ОБ АВТОРЕ —
   `gemini._who_is_talking(user_id)` кладёт в запрос блок «[С кем ты говоришь]»
   (стаж, сообщения, муты, ссылки, статус доверия, почётное звание) через тот же
   `antispam.trust_info`, что и /rank; ради неё через всю цепочку
   (`consider_message` → `_run_proactive` → запрос) протянут ID автора триггера —
   раньше передавался только текст. (3) СУДЬЯ — решение «вступать или нет»
   принимает отдельная дешёвая модель (`gemini.ask_group_judge`, ключ
   `proactive_judge_model`, кнопки в панели промптов) с ПРИНУДИТЕЛЬНО
   отключёнными размышлениями; ради этого у `_gemini_chat_request` появились
   `thinking_override` и `chain_override`, а общая подготовка контекста вынесена
   в `_build_proactive_parts` (её делят судья и писатель). Замер на живом чате:
   решение за 1–2.4 сек вместо 28–40, «размышления=0». Судья вернул None
   (выключен/не ответил) → работает ПРЕЖНЯЯ одношаговая схема. Список судей
   `PROACTIVE_JUDGE_MODELS` собирается из AVAILABLE_MODELS АВТОМАТИЧЕСКИ
   (в отличие от `_MODEL_BUTTON_ROWS`), модели с `native_thinking` (Gemma)
   исключены — их мысли параметром не гасятся (решение Максима). (4) РУКИ —
   бот может сам выдать мут: модель ставит в конце реплики пометку
   `[МУТ:секунды]`, `proactive._extract_mute` вырезает её из текста (участник
   не видит), `_apply_mute` выдаёт мут через `antispam.mute_user(action="mute_ai")`.
   Предохранители: тумблер `proactive_hands` (дефолт ВЫКЛ, отдельный ряд
   в панели), потолок `PROACTIVE_MUTE_MAX_SEC`=300 сек, цель — ТОЛЬКО автор
   сообщения-триггера (ID из апдейта, не из текста модели), персонал защищён
   (`actor_id=None` → `_manual_guard`), запись `mute_ai` в журнал модерации
   и уведомление владельцам с кнопкой размута
   (`antispam.notify_owners_ai_mute`). Блок прав `PROACTIVE_HANDS_INSTRUCTION`
   дописывается в запрос ТОЛЬКО при включённом тумблере — модель всегда знает
   правду о своих возможностях, промпт под тумблер переписывать не надо.
   Тумблер проверяется ДВАЖДЫ: перед запросом и перед выдачей мута.
   В тот же день блок прав стал ПЯТЫМ РЕДАКТИРУЕМЫМ ПРОМПТОМ (просьба
   Максима): `hist.get_hands_instruction()` (ключ settings `hands_instruction`,
   дефолт `PROACTIVE_HANDS_INSTRUCTION`), команды `/hands_prompt_set`
   и `/hands_prompt_reset`, блок «🤚 PROMPT РУК» в панели промптов (в заголовке
   показано состояние тумблера — иначе непонятно, работает текст или лежит
   мёртвым грузом) и файл `5_HANDS_PROMPT.txt` в кнопке «📄 Показать полные
   PROMPTы». Устроен как RAG-инструкция: тоже уходит модели НЕ всегда, а по
   условию. ⚠️ ФОРМАТ ПОМЕТКИ `[МУТ:секунды]` В ЭТОМ ТЕКСТЕ МЕНЯТЬ НЕЛЬЗЯ —
   её ищет `PROACTIVE_MUTE_PATTERN`, при другом написании мут молча перестанет
   срабатывать (реплика уйдёт, наказания не будет). Команда /hands_prompt_set
   предупреждает, если в новом тексте нет слова МУТ/MUTE. Подстановка
   `{max_sec}` необязательна — format обёрнут в try.
   Если код и карта расходятся — верить коду, исправить карту.
   Обновление 2026-07-20 (УДАЛЕНИЕ СУДЬИ И PROMPT РУК — решение Максима):
   быстрые модели без мышления плохо следуют инструкциям, поэтому судья
   (`ask_group_judge`, `PROACTIVE_JUDGE_MODEL`, `PROACTIVE_JUDGE_MODELS`,
   `PROACTIVE_JUDGE_YES`) удалён; решение «вступать/молчать» и реплика —
   снова одна думающая модель. PROMPT РУК (отдельный редактируемый промпт
   и команды /hands_prompt_set, /hands_prompt_reset) удалён; блок прав
   `PROACTIVE_HANDS_INSTRUCTION` включён в заводской `PROACTIVE_INSTRUCTION`
   и виден модели всегда (тумблер «РУКИ» управляет только кодом выдачи мута).
   Ключ settings `hands_instruction` стал мёртвым. Из панели «Управление
   PROMPTами» исчезли: строка выбора судьи, блок «🤚 PROMPT РУК» и 5-й файл
   выгрузки; тумблер «🤚 РУКИ» остался.
   В тот же день ГЛУБИНА МЫШЛЕНИЯ (решение Максима): для DeepSeek
   `reasoning_effort="max"` в `_deepseek_chat_request` — максимальная
   глубина размышлений; Gemini — `thinking_budget=-1` (динамический
   максимум, без изменений); Qwen — без ограничений (полный максимум
   модели, только `enable_thinking: true`).
   Обновление 2026-07-21: для DeepSeek `reasoning_effort` изменён с `"max"`
   на `"xhigh"` — экстремальная глубина размышлений (решение Максима).
   В тот же день ПАРСЕР НОВОСТЕЙ (решение Максима): блоки «Премиумный …» /
   «Способы получения» и всё их содержимое вырезаются ЦЕЛИКОМ до следующего
   заголовка (skip_until_heading вместо skip_next_list); добавлены маркеры
   "премиумный" (мужской род) и "способы получения" в _DROP_PREFIXES.
   В тот же день СТАТУС «ПРОВЕРЕННЫЙ» (решение Максима): бонус +2 к порогу
   антифлуда удалён (TRUST_EXTRA_MESSAGES убрана, проверенный мутится
   наравне со всеми); ручное управление статусом удалено (кнопка в карточке,
   trusted_manual, trusted_override) — проверенный присваивается только
   автоматически (стаж ≥7 дн, ≥100 сообщений, без свежих мутов).
   Обновление 2026-07-21 (проактивный режим: анализ медиа перед решением):
   фото описываются, голосовые расшифровываются через Gemini flash-lite
   в `_run_proactive` ДО вызова `ask_group_proactive`; голосовые теперь
   попадают в коллектор групп (`VOICE|AUDIO` в group_filter); стенограмма
   подменяет `[фото]`/`[голосовое]` реальным текстом анализа; графа
   `has_voice` в `group_messages` + миграция; потолок мута
   `PROACTIVE_MUTE_MAX_SEC` 300 → 86400 (24 ч); эмодзи логов proactive
   🗣 → 🤖; хелперы `_proactive_describe_image`/`_proactive_transcribe_audio`
   в gemini.py с логами запросов и времени ответа.
   В тот же день КОМАНДА /clear: подтверждение самоудаляется через 7 сек
   (schedule_delete вместо register_and_clean_bot_message); в личке у персонала
   автоматически открывается /adm (send_adm_panel).
   ПОЛНАЯ СВЕРКА С КОДОМ 2026-07-21 (прочитан весь проект, 32 файла):
   логика бота карте соответствует, но 9 мест карты отставали от решений
   2026-07-20 и исправлены в этой сверке: USER_TABLES 14 → 18; убран
   `trusted_override` из списка помощников user_settings; помечен удалённым
   бэкап admin.py.bak-before-split; в панели промптов ряд «⏸ пауза» заменён
   на факт (два ряда регуляторов + отдельный ряд «🤚 РУКИ»); /rag 84/12 →
   81/15 кнопок; тумблер «✅ Проверенный» вычеркнут из карточки (осталось
   три); в 2.4 зафиксировано удаление бонуса TRUST_EXTRA_MESSAGES и
   `trusted_manual`; в разделе 3 у `_count_messages` остался один читатель;
   белый список kb_-кнопок дополнен kb_margin_*. Заодно найденные УСТАРЕВШИЕ
   ТЕКСТЫ В КОДЕ исправлены в тот же день по решению Максима (поведение бота
   не менялось, только надписи и комментарии): /rank больше не обещает
   проверенному «бонус к антифлуду» (handlers/quiz.py); легенда значков в
   logging_setup.py и debug-строки в messages.py переведены с 🗣 на 🤖,
   лог кнопки «Очистить РАЗГОВОРЫ» в panel_prompts.py — на 🔧 (лог панели);
   шапка scraper.py честно говорит, что «Способы получения…» вырезаются;
   поправлены комментарии про судью/«три ряда» в panel_prompts.py,
   «прав N из 5» и «Проверенный» в panel_users.py, «дефолт 0.10» в
   panel_rag.py (реальный 0.14), «бонус к порогу» у user_dossier
   (history.py), докстринг _count_messages в antispam.py; докстринг
   thinking_override/chain_override в gemini.py теперь честно помечает
   механизм спящим (судья удалён, параметры никем не передаются — оставлены
   на будущее); шапка handlers/admin/__init__.py — бэкап монолита удалён.
   ⚠️ Правки текстов вступят в силу после перезапуска бота.
   В тот же день БАЛАНС DEEPSEEK В ПАНЕЛИ API (решения Максима): строка
   «💰 Расход DeepSeek» стала «расход / остаток» — новый ключ settings
   `deepseek_balance_usd` (внесён вручную, $5.32 на 2026-07-21),
   `add_deepseek_cost` теперь вычитает стоимость каждого запроса из остатка;
   счётчик `deepseek_cost_usd` выставлен на $1.67 (по кабинету DeepSeek)
   и СДЕЛАН ВЕЧНЫМ — месячный сброс `_monthly_stats_reset` его больше
   не обнуляет, в «Итогах месяца» строка DeepSeek помечена «за всё время»;
   Qwen и картинки остались месячными.
   Обновление 2026-07-23 (режим «Сам в разговор»: персональные исключения ТОЛЬКО
   для владельца, по его просьбе после живого теста): (1) СТЕНОГРАММА —
   ⚠️ ЭТО РЕШЕНИЕ ОТМЕНЕНО 2026-07-26 (см. ниже), подпись владельца снова по
   имени. Было: в `_build_proactive_parts` (services/gemini.py) подпись строки
   для автора из `ADMIN_IDS` бралась из ника (`@username`), а не из имени
   (`first_name`), как у всех остальных участников. (2) СПРАВКА ОБ АВТОРЕ —
   в `_who_is_talking` бит «статус: проверенный/обычный участник» для владельца
   ЗАМЕНЁН на «Владелец - @ник» (ник читается заново из `hist.get_dossier`,
   а не хранится строкой — переименование ника в Telegram подхватится само);
   у всех остальных участников бит статуса не менялся. Оба исключения проверяют
   `user_id in ADMIN_IDS`, а не конкретный ID — при появлении второго владельца
   правило автоматически распространится и на него (сейчас владелец один).
   Прочие биты справки (стаж, сообщения, нарушения, почётное звание) для
   владельца не тронуты — заменена только одна строка статуса.
   Обновление 2026-07-24 (СМЕНА КЛЮЧА QWEN И СЧЁТЧИК ТОКЕНОВ): Максим завёл
   новое рабочее пространство Alibaba — заменены `QWEN_API_KEY` в `.env` и
   `QWEN_API_URL` в config.py (хост `ws-lwbwgvfaatohre88` → `ws-twqbbc4ukfp8kv5h`),
   счётчик `qwen_cost_usd` обнулён ($1.253822 → 0). В тот же день по просьбе
   Максима добавлен ВЕЧНЫЙ счётчик израсходованных токенов по каждой модели
   Qwen: `spend_qwen_tokens`/`get_qwen_tokens` в database/history.py (ключи
   settings `qwen_tokens_<модель>`), списание из services/gemini.py рядом с
   `add_qwen_cost`, показ в панели «📡 Настройки API» рядом с числом вызовов.
   Решения Максима: счётчик ВЕЧНЫЙ (не месячный — иначе теряется счёт
   бесплатной квоты Alibaba), показывать ТОЛЬКО у Qwen, и в тот же день —
   считать не «сколько потрачено», а ОСТАТОК: выставлено по 1 000 000 токенов
   на модель, дальше тает сам (как остаток баланса DeepSeek). Подробности
   и грабли — в разделе 3.
   В тот же день ПЕРЕБОРКА СПИСКА МОДЕЛЕЙ И ВИДЕО (решения Максима, план
   утверждён): (1) УДАЛЕНЫ `gemini-2.5-flash-lite`, `gemini-2.5-flash`
   и `gemma-4-26b-a4b-it`; ДОБАВЛЕНА `gemini-3.6-flash` (вышла 21.07.2026).
   Моделей стало 8, раскладка кнопок — 4 ряда по 2 (порядок Gemini задан
   Максимом «по столбцам»). `FALLBACK_MODEL` = `gemini-3.6-flash`, следом
   в цепочке `gemini-3.1-flash-lite` — ею же заменена удалённая
   `gemini-2.5-flash-lite` в ТРЁХ зашитых местах services/gemini.py.
   Активная модель переведена на `gemini-3.5-flash` (прежняя 2.5-flash-lite
   удалена; авто-сброс на дефолт сработал штатно). Gemma была единственным
   носителем `native_thinking` — поле осталось без владельца, разбор в коде
   не трогали. (2) БОТ НАУЧИЛСЯ ПРИНИМАТЬ ВИДЕО: `ask_gemini_video`
   в services/gemini.py (своя цепочка `VIDEO_FALLBACK_CHAIN`, свой таймаут
   `VIDEO_TIMEOUT`=180 с, подпись к видео уходит модели как вопрос),
   `handle_video` в handlers/messages.py, регистрация `filters.VIDEO`
   с `block=False`, графа `has_video` в `group_messages` + миграция,
   пометка «[видео]» в стенограмме, `_proactive_describe_video` для режима
   «Сам в разговор». Новое поле моделей `"video": True` (у всех Gemini)
   и хелпер `_supports_video`. ⚠️ Кружочки (`VIDEO_NOTE`) и гифки
   (`ANIMATION`) НЕ обрабатываются — решение Максима, обработчик на них
   не зарегистрирован; не «чинить». Живой тест 3.6 Flash на бесплатном ключе
   Максима: цвета кадров видео названы верно по порядку, русская и английская
   речь расшифрованы дословно, картинка описана точно; НЕ умеет считать
   звуковые сигналы и определять высоту тона (для бота неважно — там речь).
   (3) В ТОТ ЖЕ ДЕНЬ по требованию Максима УДАЛЕНЫ ВСЕ ПЯТЬ зашитых фраз-
   инструкций к медиа (подробности и последствия — в разделе 3). Претензия
   была не к формулировкам, а к тому, что они сочинены и зашиты без его
   ведома: в плане я показал только то, что видит человек («🎬 Смотрю
   видео…»), а что уходит модели — не показал. Урок на будущее: при работе
   с моделями показывать Максиму И текст, который уходит модели, — проверить
   код он не может, это его единственный способ узнать.
   В тот же день СУТОЧНЫЙ ОТЧЁТ О РАСХОДАХ (просьба Максима, план утверждён):
   в 00:00 по Киеву ВЛАДЕЛЬЦУ в личку уходит отчёт по счётчикам панели
   «📡 Настройки API» за прошедшие сутки — вызовы по каждой модели, расход $
   по провайдерам, сожжённые токены Qwen, остатки на счетах. Панельные счётчики
   накопительные, суточной цифры в них нет, поэтому раз в сутки делается СНИМОК
   их значений (новая таблица `stats_snapshots`), а отчёт показывает РАЗНИЦУ
   между соседними снимками; вызовы при этом считаются точно, по времени из
   `api_calls`. Новый модуль `services/daily_report.py` (сборка текста),
   цикл `jobs.py::daily_report_loop`, кнопка «📊 Отчёт за вчера» в /adm
   (решение Максима — нужна). Решения Максима: считать РОВНО панельные
   счётчики (токены Gemini и DeepSeek не нужны — из-за них не пришлось лезть
   в services/gemini.py, и он НЕ тронут), слать ТОЛЬКО владельцу в личку.
   Подробности и грабли — в разделах 2.5 и 3.
   В тот же день КОМАНДЫ УБРАНЫ ИЗ СТЕНОГРАММЫ «Сам в разговор» (просьба
   Максима, решение «все команды, а не только /clear»): в
   `_build_proactive_parts` строки архива, начинающиеся со слэша,
   пропускаются. Повод — `/clear`: сообщение из чата удаляется, но в архив
   групп попадает, и бот видел стёртое. Подробности — в разделе 3.
   В тот же день ВТОРАЯ СМЕНА КЛЮЧА QWEN (id 952403) И РАЗБОР ОТКАЗОВ 403:
   утром 24.07 Alibaba раз за разом отбивала запросы бота кодом 403 с ПУСТЫМ
   телом (06:04–06:43; каждый раз отрабатывала подстраховка Gemini 3.6 Flash —
   люди сбоя не заметили). Ключ заменён на новый (пространство ТО ЖЕ, поэтому
   правился только `QWEN_API_KEY` в `.env`, адрес и счётчики не трогали) — не
   помогло. ПРИЧИНА ОКАЗАЛАСЬ НЕ В КЛЮЧЕ: кнопка перезапуска наследует
   окружение старой копии, а `load_dotenv()` не перезаписывал уже имеющиеся
   переменные — бот с ночи тащил по непрерывной цепочке перезапусков
   (02:15 → 02:40 → 03:12 → 03:15 → 05:09 → 06:03 → 06:31 → 06:42) ключ ОТ
   СТАРОГО пространства и слал его на НОВЫЙ адрес. Лечение: `load_dotenv(
   override=True)` в config.py (решение Максима, правка проверена прогоном
   дочернего процесса с подставленным старым ключом). Подробности и признаки
   отказов — в разделе 3.
   Обновление 2026-07-25 (НЕДЕЛЬНЫЙ ОТЧЁТ О РАСХОДАХ, просьба Максима, план
   утверждён): каждый ПОНЕДЕЛЬНИК в 00:00 по Киеву владельцу в личку следом за
   суточным уходит ВТОРОЕ сообщение — «📅 РАСХОД ЗА НЕДЕЛЮ» за пн–вс, теми же
   блоками и подписями, плюс строка «📈 В среднем в день» внизу (решение
   Максима). Считается НЕ разницей снимков за 7 дней, а СУММОЙ уже посчитанных
   суток: каждый суточный отчёт кладёт свои цифры в копилку (ключ settings
   `weekly_report_acc`), в понедельник копилка становится отчётом и обнуляется —
   причина в месячном сбросе, см. раздел 3. Весь код — в том же
   `services/daily_report.py` (новых файлов нет), рисовалка текста ОДНА на оба
   отчёта (`render` + `period_totals`). В тот же день по решению Максима ОБЕ
   кнопки отчётов ПЕРЕЕХАЛИ ИЗ /adm В ПАНЕЛЬ «📡 Настройки API» одним рядом
   («📊 Отчёт за вчера» + «📅 Отчёт за неделю») — они собраны ровно из счётчиков
   этой панели; с экранов отчётов возврат ведёт сначала в панель API
   («⬅️ Настройки API»), потом в /adm. Проверено прогоном на временной базе
   с подменённым временем: неделя из 7 суток, месячный сброс в середине недели
   (цифры не занижаются), пропущенный понедельник, неполная неделя.
   Обновление 2026-07-25 (ЧЕТВЁРТЫЙ ПРОВАЙДЕР — XIAOMI MiMo, план утверждён
   Максимом): подключены `mimo-v2.5` и `mimo-v2.5-pro` (provider `xiaomi`,
   значок 🍚 — выбор Максима). Адрес `XIAOMI_API_URL`
   (https://api.xiaomimimo.com/v1/chat/completions), ключ `XIAOMI_API_KEY`
   в `.env`, цены `XIAOMI_PRICES`. API OpenAI-совместимый и УПРАВЛЯЕТСЯ ТЕМ ЖЕ
   ПАРАМЕТРОМ, что DeepSeek (`thinking: {type, reasoning_effort}`), поэтому
   запрос идёт через общий `_openai_stream_request`; глубина мышления `xhigh`,
   как у DeepSeek. Деньги считаются точно (`_xiaomi_cost`, кэш из
   `prompt_tokens_details.cached_tokens` — как у Qwen), копятся в settings
   `xiaomi_cost_usd` и вычитаются из остатка счёта `xiaomi_balance_usd`
   ($2.72 на 2026-07-25, назван Максимом) — устройство «расход / остаток»
   скопировано с DeepSeek, счётчик ВЕЧНЫЙ (месячный сброс его не трогает, в
   «Итогах месяца» помечен «за всё время»). Живая разведка на API Xiaomi
   (что можно и чего нельзя — детали в разделе 3): `mimo-v2.5` видит КАРТИНКИ
   (`vision: True`), понимает ВИДЕО и РЕЧЬ; `mimo-v2.5-pro` не принимает ни
   картинок, ни видео, ни аудио (404 «No endpoints found that support image
   input»). Поле `video` НЕ поставлено НИ ОДНОЙ модели намеренно. В цепочки
   подстраховки MiMo пока НЕ включён (решение Максима — сначала посмотреть
   в деле). Проверено живым прогоном через код бота: значок 🍚 в логах, ответы
   в образе Полковника по-русски, мысли приходят, стоимость списана
   ($0.00015343 за два запроса), остаток стал $2.71984657, вызовы легли
   в блок Xiaomi отчётов, а не в «прочие».
   Обновление 2026-07-26 (СПРАВКА ОБ АВТОРЕ ВИДНА В ПАНЕЛИ ПРОМПТОВ, просьба
   Максима): в панель «⚙️ НАСТРОЙКИ PROMPTов» добавлены ДВА блока. (1) «🪪 СПРАВКА
   ОБ АВТОРЕ» — ДОСЛОВНЫЙ текст блока «[С кем ты говоришь]», который бот сам
   собирает из карточки участника и шлёт модели в режиме «Сам в разговор»
   (единственный кусок запроса, который не берётся из настроек, — владелец
   иначе никак не мог его увидеть). Пример собирается НА СМОТРЯЩЕГО (решение
   Максима: живые цифры нагляднее шаблона), источник — новая ПУБЛИЧНАЯ
   `services/gemini.py::author_brief` (обёртка над `_who_is_talking`, чтобы
   показанное и отправленное модели не разъехались). Блок стоит ПЕРЕД
   «PROMPT УЧАСТИЯ» — как в реальном порядке сборки запроса. Редактировать
   справку из Телеграма пока НЕЛЬЗЯ (решение Максима — сначала посмотреть).
   (2) Сводка «📦 ЧТО УХОДИТ МОДЕЛИ В РЕЖИМЕ „САМ В РАЗГОВОР“» в самом низу:
   пять строк в порядке сборки (system prompt → база знаний → справка →
   promt участия → стенограмма) с живыми размерами и итогом постоянной части.
   Панель выросла до ~3.3 тыс. символов из 4096 — проверено сборкой.
   Повод — разбор задачи «карточки и справка об авторе» из бэклога: выяснилось,
   что справка уходит модели ТОЛЬКО в проактивном режиме, а в личке и при прямом
   обращении модель о человеке не знает НИЧЕГО (ask_gemini её не добавляет).
   В тот же день ЧИСТКА КАРТОЧКИ УЧАСТНИКА (разбор задачи из бэклога, решения
   Максима): (1) строка статуса доверия («✅ Проверенный боец» / «⚠️ Взыскание» /
   «⏳ Испытательный срок») УДАЛЕНА из карточки — с 2026-07-20 статус ни на что
   не влияет, а стаж и число сообщений строкой выше говорят то же самое
   (`trust_info` остался — из него берутся цифры); (2) строка викторины
   ПРЯЧЕТСЯ при нуле попыток («0 верных из 0» — пустой звук); (3) честные
   формулировки вместо обманчивых: «Контекст диалога: N из 20 сообщ.»,
   «Запросов к ИИ: N за <месяц>» (счётчик месячный, раньше читался как «за всё
   время»), «Картинки: осталось N из M» (раньше «N из M» читалось наоборот),
   «⚠️ ограничен Telegram (писать может)» (остаточный статус после мута),
   «Ссылки: по общему правилу (фильтр 🟢/🔴)» вместо «Ссылки разрешены: 🔴 нет»
   (красный кружок означал не запрет, а отсутствие ЛИЧНОГО разрешения),
   «Иммунитет от мута: 🟢 есть (по роли, тумблер не нужен)» у персонала;
   (4) ЗНАЧОК 🧠 (`_BRAIN`) у строк, которые уходят модели в справке об авторе
   (стаж+сообщения, нарушения, роль владельца, заданное почётное звание),
   и легенда внизу карточки.
   В тот же день СПРАВКА ОБ АВТОРЕ ПЕРЕСОБРАНА (решения Максима): из неё
   УДАЛЕНЫ статус доверия, СТАЖ, ЧИСЛО СООБЩЕНИЙ и ВСЕ счётчики нарушений
   (мутов/ссылок) — довод Максима: «модель просто будет знать и всё», цифры
   на разговор не влияют, а прошлые нарушения не должны на него влиять вовсе.
   ДОБАВЛЕНЫ имя и ник автора (справка была безымянной — модель сопоставляла её
   со стенограммой сама и могла отнести не к тому) и пометка «— модератор»
   (раньше модель знала только владельца и грозила мутом персоналу, которого
   не тронет). Итоговый состав: `Имя (@ник) — роль; звание: X.` Вызова
   `antispam.trust_info` в справке БОЛЬШЕ НЕТ. Ни имени, ни ника — справка
   не собирается вовсе (пустая строка). Значки 🧠 в карточке переставлены:
   теперь у имени с ником, у роли и у заданного почётного звания; со «Службы
   в гарнизоне» сняты. И РАСШИРЕНА ОБЛАСТЬ ДЕЙСТВИЯ: справка уходит модели
   не только в проактивном режиме, но и при ПРЯМОМ обращении в ГРУППЕ
   (`ask_gemini`, условие `chat_id != user_id`, после блока RAG — тот же
   порядок, что в `_build_proactive_parts`). В ЛИЧКЕ справки нет намеренно.
   В тот же день ОТМЕНЕНО ИСКЛЮЧЕНИЕ ДЛЯ ВЛАДЕЛЬЦА В СТЕНОГРАММЕ (решение
   Максима, отменяет его же решение 2026-07-23): его реплики снова подписаны
   ИМЕНЕМ (`first_name`), как у всех участников, а не ником. Довод: то, что он
   владелец, модель узнаёт из справки об авторе — дублировать это подписью
   незачем. В `_build_proactive_parts` ветка `elif r["user_id"] in ADMIN_IDS`
   удалена, подпись у всех собирается одинаково. Не возвращать без его просьбы.
   Тогда же слово «бота» убрано из обозначения роли ВЕЗДЕ (просьба Максима):
   в карточке «👑 Владелец бота» → «👑 Владелец», в справке модели
   «— Владелец бота» → «— Владелец». Я предупредил, что модель может понять
   голое «Владелец» как владельца группы или канала, — Максим решение
   подтвердил. Не «уточнять» обратно.
   В тот же день БЛОК «🧠 СПРАВКА ДЛЯ НЕЙРОСЕТИ» ДОБАВЛЕН В КАРТОЧКУ КАЖДОГО
   участника (просьба Максима): дословный текст `gemini.author_brief(user_id)`,
   собранный НА ВЛАДЕЛЬЦА КАРТОЧКИ, в самом низу под персональными настройками.
   Цитата ОБЫЧНАЯ (`<blockquote>` без `expandable`) — решение Максима, справка
   короткая. Текст экранируется вручную (имя приходит из Telegram, символ «<»
   в нике сломал бы отправку). Нет имени и ника — вместо цитаты «(не собирается
   — бот не знает ни имени, ни ника)». Легенда значка 🧠 переписана: она теперь
   объясняет, что помеченные строки собираются В ЭТУ справку. Длина карточки
   с блоком ~1.2 тыс. символов из 4096 — проверено сборкой.
   Обновление 2026-07-27 (УСКОРЕНИЕ КОДА БОТА — просьба Максима «ускорить работу
   самого кода, а не скорость моделей»; все цифры ниже ЗАМЕРЕНЫ на копии рабочей
   базы, не оценены на глаз). Пять правок, поведение бота НЕ менялось нигде:
   (1) ⚡ ЕДИНОЕ СОЕДИНЕНИЕ С БАЗОЙ (`database/history.py`): раньше каждая из 74
   функций открывала своё соединение на один запрос и закрывала — открывание
   файла базы стоит в сотни раз дороже самого запроса. Теперь соединение одно на
   процесс (`_conn`, ленивое открытие в `_get_connection`, закрытие `close_db()`
   из `main.py::post_shutdown`), все 74 вызова `conn.close()` из функций УБРАНЫ,
   а сам `close()` у `_LoggingConnection` СДЕЛАН ПУСТЫМ — заслон от случайного
   закрытия общего соединения в новой функции. Замеры: чтение настройки
   2.0 → 0.006 мс, запись в архив 6.8 → 0.05 мс. (2) `synchronous=NORMAL` вместо
   FULL (решение Максима): запись 0.885 → 0.048 мс; штатная пара к WAL, падение
   бота записи не теряет, потерять последние доли секунды можно только при сбое
   Windows или обесточивании. ИТОГ по (1)+(2): одно сообщение группы стоило боту
   32.7 мс, стало 0.21 мс — в 156 раз меньше (замер одним и тем же скриптом до
   и после). (3) `_lock` из простого `threading.Lock` стал обёрткой `_DbLock`,
   которая ОТКАТЫВАЕТ недописанную транзакцию, если функция выпала с ошибкой
   на середине записи — при общем соединении такая транзакция висела бы в нём
   и уехала бы в чужой commit (проверено тестом: половинчатая запись в базу не
   попала, база продолжила работать). Реентерабельность НЕ появилась — правило
   «get_setting/set_setting звать только ВНЕ `with _lock`» в силе.
   (4) 🌐 НОВЫЙ ФАЙЛ `services/http.py`: соединения к нейросетям и сайтам
   переиспользуются (по сессии `requests` на рабочий поток). Раньше каждый
   `requests.post` заново делал TLS-«рукопожатие» — замерено 130–200 мс до
   Google НА КАЖДЫЙ запрос, а запросов на один ответ обычно два (эмбеддинг для
   базы знаний + сама модель). Живой замер до и после: 400–650 мс на запрос
   стало 86–260 мс. Заменено 12 вызовов в `services/gemini.py` (9),
   `services/rag.py` (1), `services/scraper.py` (2). ⚠️ ПОВТОРЫ В СЕССИИ
   ВЫКЛЮЧЕНЫ НАМЕРЕННО (`max_retries=0`) — у бота своя логика подстраховки.
   (5) Мелочи: карточка участника опрашивает группы ВСЕ РАЗОМ (`asyncio.gather`
   в `_chat_membership`, было по очереди — при шести группах почти две секунды);
   поиск по базе знаний перестал пересчитывать длины векторов на каждый вопрос
   (`_norm` запоминается в `_load_into_memory`, счёт через `_cosine_fast`):
   30.3 → 12.3 мс, результаты СОВПАДАЮТ ДО БИТА (расхождение 0.00e+00 на всех
   80 статьях), поэтому калибровка поиска (порог, «запас над фоном») остаётся
   верной и перемерять её не надо. ⚠️ ЧТО ПРОВЕРЕНО И ОКАЗАЛОСЬ НЕ ВИНОВАТО
   (не «оптимизировать»): логирование SQL в `_LoggingConnection` — 2 микросекунды
   на запрос; форматирование ответа `convert_md` — 0.4 мс; размер базы (825
   сообщений архива, нужные индексы на месте); загрузка векторного индекса при
   старте — 50 мс.
   В тот же день 🐕 СТОРОЖ: УВЕДОМЛЕНИЕ О ТОМ, ЧТО БОТ ПЕРЕСТАЛ ОТВЕЧАТЬ
   (просьба Максима, план утверждён — он выбрал ОБА уровня и «тревогу присылать
   даже при ручной остановке»). Исходное ограничение, из которого всё следует:
   БОТ НЕ МОЖЕТ СООБЩИТЬ О СОБСТВЕННОЙ СМЕРТИ — при выключенном компьютере или
   пропавшем интернете отправлять некому и нечем. Поэтому сторожей ДВА и они
   независимы. (1) ВНЕШНИЙ — `jobs.py::watchdog_loop` раз в `WATCHDOG_PING_SEC`
   (5 мин) «отмечается» по адресу `WATCHDOG_URL` (healthchecks.io, бесплатный
   тариф; у Максима Period 5 мин / Grace 15 мин, уведомления в Telegram через
   @HealthchecksBot). Отметки прекратились — сервис САМ пишет владельцу. Это
   ЕДИНСТВЕННОЕ, что переживает смерть компьютера. ⚠️ ОТМЕТКА ЗНАЧИТ НЕ «ПРОЦЕСС
   ЗАПУЩЕН», А «БОТ ДОСТАЁТ ДО TELEGRAM»: перед каждой отметкой идёт `get_me`,
   и без ответа отметка НЕ шлётся — иначе сторож молчал бы в случае «бот жив, но
   Telegram недоступен», а для людей в чате это выглядит как смерть бота.
   (2) МЕСТНЫЙ — новый файл `watchdog_local.py` + задание планировщика Windows
   «C4_Max Bot Watchdog» (создано 2026-07-27, каждые 2 мин, `pythonw.exe` — без
   мигающего окна). Ловит «бот упал или завис, а комп и интернет живы» за 2–4
   минуты вместо 15. Живость определяется ДВУМЯ признаками: процесс из `bot.pid`
   существует И метка `bot.alive` свежее `WATCHDOG_STALE_SEC` (12 мин) — метку
   пишет сам бот в том же `watchdog_loop` после удачной проверки Telegram, и
   именно она отличает ЗАВИСШЕГО бота от работающего. Тревога — только если
   плохо ДВА РАЗА ПОДРЯД (память в `watchdog.state`), иначе кнопка перезапуска
   будила бы её каждый раз. За происшествие ровно ДВА сообщения: «🔴 БОТ НЕ
   ОТВЕЧАЕТ» и «✅ Бот снова работает» (проверено прогоном конечного автомата).
   ⚠️ ДВЕ МИНЫ, НА КОТОРЫХ НАСТУПИЛ ПРИ НАПИСАНИИ и которые нельзя вернуть:
   (а) `watchdog_loop` СНАЧАЛА завершался, если `WATCHDOG_URL` пуст — тогда
   метка живости не писалась и местный сторож слал бы вечную ложную тревогу;
   теперь цикл работает ВСЕГДА, без URL пропускается только отметка наружу;
   (б) `watchdog_local.py` читает `.env` ПО ПОЛНОМУ ПУТИ и ДО `import config` —
   `config.py` зовёт `load_dotenv()` без пути, а тот ищет от РАБОЧЕЙ ПАПКИ, и у
   задания планировщика она чужая: токен оказывался пустым, сторож молча ничего
   не отправлял (проверено запуском из `C:\` — худший вид поломки, невидимый).
   В ТОТ ЖЕ ДЕНЬ, ЧЕРЕЗ ПОЛЧАСА ПОСЛЕ ЗАПУСКА, СЛУЧИЛАСЬ НАСТОЯЩАЯ АВАРИЯ и
   сторож её отработал — хронология по логам: 08:01 пропал интернет (бот:
   «отметка НЕ прошла, getaddrinfo failed»), 08:09 местный сторож «плохая
   проверка 1 из 2», 08:11 тревога — но ОТПРАВИТЬ НЕ СМОГ (интернета нет),
   08:13 повторил и ДОСТАВИЛ, 08:13 Максим перезапустил бота. Два вывода,
   оба важные. (а) Сработало решение ставить `alarm_sent` ТОЛЬКО при удачной
   доставке: первая попытка провалилась, и при «пометить отправленной в любом
   случае» владелец не узнал бы НИЧЕГО. Не «упрощать». (б) Задержку дал НЕ
   интервал проверок, а порог `WATCHDOG_STALE_SEC` = 12 минут.
   Поэтому в тот же день ПО ПРОСЬБЕ МАКСИМА («сделай, чтобы приходило сразу»)
   сторож УСКОРЕН, план утверждён: отметка живости 5 мин → 1 мин, порог
   протухания 12 мин → 3 мин, местный сторож — задание раз в минуту и ТРИ
   проверки внутри одного запуска с шагом 20 сек (планировщик Windows не умеет
   чаще раза в минуту, а постоянно висящий процесс заводить не стали: его
   самого пришлось бы сторожить), внешний — Period 1 мин / Grace 2 мин.
   Добавлен «КРИК УМИРАЮЩЕГО» `main.py::_cry_for_help` — при штатной остановке
   бот шлёт на адрес сторожа хвост `/fail`, и тревога приходит за СЕКУНДЫ, не
   дожидаясь сроков; при перезапуске кнопкой крик НЕ шлётся (проверка
   `shutdown_reason == "restart"` стоит выше). Итог: бот упал или закрыли окно —
   ~40 сек; бот завис — ~3.5 мин; штатная остановка — секунды; пропал интернет
   или свет — ~3 мин. ⚠️ ЦЕНА, ПРИНЯТАЯ МАКСИМОМ ОСОЗНАННО: сеть, моргнувшая
   дольше 3 минут, теперь даёт ЛОЖНУЮ тревогу и следом «снова работает».
   Начнёт раздражать — поднимать `WATCHDOG_STALE_SEC` и Grace, а не выключать
   сторожа. И ⚠️ ФИЗИЧЕСКИЙ ПРЕДЕЛ, который ускорением не берётся: пока нет
   интернета, НИ ОДИН сторож на этом компьютере отправить ничего не может —
   уведомление уходит только когда связь вернулась (ровно это и было 08:11→08:13).
   В ТОТ ЖЕ ДЕНЬ, СРАЗУ ПОСЛЕ УСКОРЕНИЯ, Максим пожаловался: «постоянно
   выскакивает окно терминала». Причина — МОЯ: `watchdog_local.py` проверял
   процесс бота вызовом `tasklist` через subprocess, а задание работает под
   `pythonw.exe`, у которого своей консоли нет, — Windows заводила НОВОЕ ОКНО
   под каждый запуск консольной программы. При проверке раз в две минуты это
   мигало 30 раз в час и прошло незамеченным; после ускорения (3 проверки
   в минуту) стало 180 раз в час, и это уже невозможно не заметить. Лечение:
   `tasklist` убран СОВСЕМ, процесс проверяется напрямую через ctypes
   (`OpenProcess` + `QueryFullProcessImageNameW`, имя должно начинаться на
   «python» — защита от переиспользованного номера сохранена). Окну взяться
   неоткуда СТРУКТУРНО, а не «подавлено флагом»; заодно исчезли 180 запусков
   процессов в час. ⚠️ ВЕРНЁШЬ subprocess в этот файл — обязателен
   `creationflags=subprocess.CREATE_NO_WINDOW`, иначе мигание вернётся.
   Проверено: новых окон не появляется (счётчик conhost до и после запуска
   задания одинаков), проверка верна на трёх случаях — свой python-процесс
   True, несуществующий номер False, чужой процесс с занятым номером
   (conhost.exe) False. УРОК ШИРЕ ЭТОГО СЛУЧАЯ: всё, что запускается
   планировщиком под `pythonw.exe`, не должно порождать внешние процессы.
   ⛔ В ТОТ ЖЕ ДЕНЬ, ВЕЧЕРОМ, МЕСТНЫЙ СТОРОЖ ВЫКЛЮЧЕН (решение Максима):
   задание планировщика «C4_Max Bot Watchdog» переведено в состояние Disabled
   (`schtasks /change /tn "C4_Max Bot Watchdog" /disable`, идущий запуск
   завершён через `/end`). Довод Максима: уведомления приходят от
   healthchecks.io в Телеграм И на Gmail, и их достаточно — два сообщения
   местного сторожа («🔴 БОТ НЕ ОТВЕЧАЕТ» и «✅ Бот снова работает») дублируют
   их. Я показал развилку и он выбрал выключение целиком: эти два сообщения —
   ЕДИНСТВЕННОЕ, что местный сторож делает, поэтому «убрать уведомления» и
   «выключить сторожа» здесь одно и то же. ⚠️ ЦЕНА, ПРИНЯТАЯ ОСОЗНАННО: о
   падении бота теперь узнаём за ~3 мин (Period 1 + Grace 2 у healthchecks.io)
   вместо ~40 сек. Внешний сторож покрывает ВСЕ случаи местного, только
   медленнее. КОД НЕ ТРОНУТ НИГДЕ: `watchdog_local.py` цел, `watchdog_loop`
   в jobs.py работает по-прежнему (он кормит внешний сторож и пишет метку
   `bot.alive` — метка теперь никем не читается, но это дёшево и возвращает
   местного сторожа одной командой `schtasks /change … /enable`). Файлы
   `watchdog.state` и `watchdog.log` остались как есть.

---

## 1. Структура: файлы и за что отвечают

| Файл | За что отвечает |
|---|---|
| `main.py` | Запуск: логирование → PID-защита от двух копий → init_db → RAG-синк → Application с хуками post_init/post_stop/post_shutdown → polling; кнопка рестарта перезапускает бота через `_restart_self()` (в Windows — в НОВОМ окне консоли, см. раздел 3) |
| `config.py` | Все дефолты: токены из .env, реестр моделей `AVAILABLE_MODELS` (provider: gemini/qwen/deepseek; поле vision — кто принимает картинки: Gemini/Gemma да, Qwen/DeepSeek нет — их API только текст, сверено 2026-07-10), `FALLBACK_MODEL`, `AUDIO_FALLBACK_CHAIN`, модели картинок Nano Banana `AVAILABLE_IMAGE_MODELS` + цены `IMAGE_PRICES`, `ADMIN_IDS`, лимит картинок (3/день), `GEMINI_TIMEOUT` 60с + `GEMINI_STREAM_DEADLINE` 90с (общий предел на весь потоковый ответ Qwen/DeepSeek), дефолты антиспама, пути и дефолты RAG (`RAG_MIN_SIMILARITY` 0.58, `RAG_TOP_K` 3 — живые значения в settings; `RAG_CHUNK_MODE` "file", константы гибридного поиска `RAG_LEX_BOOST`/`RAG_STRONG_SIM` и `RAG_PEAK_MARGIN` (0.14 с 2026-07-19 — ДЕФОЛТ живого регулятора «Запас над фоном», значение в settings `rag_peak_margin`; в комментарии рядом записан замер, которым выбрано число), `RAG_INSTRUCTION` — заводская «шапка»-инструкция RAG, живой текст в settings `rag_instruction`, правится из /prompt), `DEEPSEEK_PRICES` (сверены 2026-07-04) и `QWEN_PRICES` (сверены 2026-07-05; ⚠️ qwen3.7-max — АКЦИОННАЯ цена −50%) — цены $/1 млн токенов, зашиты руками; адрес и ключ Xiaomi MiMo `XIAOMI_API_URL` / `XIAOMI_API_KEY` (2026-07-25) с ценами `XIAOMI_PRICES`; значки провайдеров `PROVIDER_ICONS` (♊️ gemini / 🐪 qwen / 🐋 deepseek / 🍚 xiaomi / 🎨 image) + `PROVIDER_ICON_FALLBACK` (🤖, модель не из конфига) + `RAG_ICON` (📚) — ЕДИНЫЙ источник значков для логов и панели API; `QUIZ_RANKS` — ЕДИНЫЙ список званий викторины (имя, значок, границы min/max верных ответов, характеристика; 2026-07-20 вынесен сюда из `get_user_stats`): читают его расчёт звания в history.py и кнопки выбора ПОЧЁТНОГО звания в карточке /users; границы обязаны идти по возрастанию без «дыр» (max предыдущего = min следующего −1), иначе попавший в дыру получит первое звание списка; блок «Сам в разговор» `PROACTIVE_*` (дефолты тумблера/порога, `PROACTIVE_CONTEXT_MSGS` 25, `PROACTIVE_MUTE_MAX_SEC` 86400 (24 ч, с 2026-07-21 было 300), маркер `PROACTIVE_SKIP_MARKER` «ПРОПУСК», заводская инструкция `PROACTIVE_INSTRUCTION` с блоком рук внутри — живой текст в settings `proactive_instruction`; судья удалён, бывший ключ `hands_instruction` мёртв) |
| `logging_setup.py` | Единая настройка логов: DEBUG в консоль + файл сессии `logs/«дд.мм чч-мм».log` (новый на каждый запуск). С 2026-07-20 в папке `logs` всегда ДВА файла: текущая сессия и общий архив `archive.log` — `archive_old_logs()` дописывает логи прошлых запусков в конец архива (каждый под заголовком `_session_mark` «════ СЕССИЯ дд.мм чч-мм ════»), удаляет их и обрезает архив до `ARCHIVE_SESSIONS_TO_KEEP` (=7) последних сессий (`_trim_archive` — построчно, через временный файл и `os.replace`; счёт сессий идёт ПО ЗАГОЛОВКАМ). Зовётся НЕ из `setup_logging`, а из `main.py` после `_kill_existing_instance()` — пока старая копия жива, её лог дописывается и последние строки в архив бы не попали; все ошибки глушатся (перенос логов не должен мешать запуску). Путь к файлу текущей сессии хранится в `CURRENT_LOG_FILE` (читает кнопка «📜 Логи бота»); легенда эмодзи-значков (с 2026-07-19 у нейросетей значок ПО ПРОВАЙДЕРУ: ♊️/🐪/🐋, поиск по базе — 📚, общий 🤖 остался только запасным); импортируется в main.py ДО всего остального |
| `jobs.py` | Фоновые циклы: `news_polling_loop` (новости каждые 10 мин; `send_news_to_chat` после успешной отправки в ГРУППУ (chat_id < 0) пишет сводку в архив групп через `save_group_message` — БЕЗ `note_bot_group_reply`, намеренно: новость не должна откладывать проактивное участие в её обсуждении) и `cleanup_loop(application)` (суточная чистка: архив групп 10 дн, модерация 7 дн, журнал БЗ 30 дн; + месячный сброс статистики API `_monthly_stats_reset` — см. 2.5); `rag_catchup_loop(application)` (2026-07-19, ежечасный добор базы знаний — см. 2.5; при выключенном RAG завершается сразу); `daily_report_loop(application)` (2026-07-24, суточный отчёт о расходах в 00:00 по Киеву владельцу в личку — см. 2.5; с 2026-07-25 он же по понедельникам шлёт ВТОРЫМ сообщением недельный отчёт: проверка `week_closed()` стоит ПОСЛЕ суточного, чтобы последние сутки успели попасть в копилку; спит до полуночи кусками не больше часа, чтобы пережить спящий компьютер и перевод часов); `watchdog_loop(application)` (2026-07-27, «отметки живости» внешнему сторожу — см. 2.5; работает ВСЕГДА, даже без `WATCHDOG_URL`: без него пропускается только отметка наружу, а местная метка `bot.alive` пишется по-прежнему) |
| `services/daily_report.py` | СУТОЧНЫЙ И НЕДЕЛЬНЫЙ ОТЧЁТЫ О РАСХОДАХ (суточный 2026-07-24, недельный 2026-07-25 — в том же файле, отдельного модуля намеренно нет): собирает текст отчёта по счётчикам панели «📡 Настройки API» за период. Панельные счётчики накопительные, поэтому раз в сутки снимается их «фотография» (`collect_counters` → `stats_snapshots`), а расход = РАЗНИЦА снимков; вызовы берутся точно, по времени из `api_calls` (`hist.count_api_calls_between`). Главное: `midnight_report()` (закрывает период, шлёт текст, ставит новый снимок, сохраняет текст в settings `daily_report_last_text` для кнопки), `period_closed()` (единственное условие отправки — «по Киеву наступили новые сутки с момента снимка»: ловит и пропущенную полночь, и не даёт слать отчёт на каждый перезапуск), `start_snapshot_if_needed()` (первый запуск — только точка отсчёта, без отчёта), `today_so_far()` (живой хвост кнопки «сколько набежало с полуночи»), `note_monthly_reset()` (перенос — см. раздел 3), `last_report_text()`. Подписи периода: ровные сутки → «24 июля 2026 (00:00–24:00 по Киеву)», иначе честный диапазон + предупреждение «бот был выключен в полночь». НЕДЕЛЬНАЯ ЧАСТЬ (2026-07-25, нижний раздел файла): цифры считает `period_totals` (одна на оба отчёта), рисует `render` (тоже одна — иначе отчёты разъедутся по виду); `week_add_day` (зовётся из `midnight_report` — закрытые сутки идут в копилку `weekly_report_acc`), `week_closed()` (понедельник по Киеву ЛИБО ≥7 суток с начала копилки — страховка на проспанный понедельник), `weekly_report()` (собирает, сохраняет текст в `weekly_report_last_text`, копилку обнуляет), `week_so_far()` (живой хвост кнопки), `last_weekly_text()`. Подпись недели — «20–26 июля 2026 (по Киеву) · дней: 7» + предупреждение, если суток не семь; внизу строка «📈 В среднем в день» (делится на РЕАЛЬНОЕ число суток) |
| `utils.py` | `should_respond_in_group` (mention/reply, в т.ч. в подписи к фото), `clean_mention`, `keep_chat_action` (статус «печатает…»), `register_and_clean_bot_message` (новая панель удаляет старую), `delete_user_message_safe`, `schedule_delete`, `mention` |
| `utils_format.py` | Ответы модели → (text, entities) через telegramify-markdown; `<thought>` → сворачиваемая цитата; `send_formatted` (нарезка длинных по границам entity), `convert_md`, `reply_md`, `fits_caption`, `strip_thoughts` |
| `watchdog_local.py` | 🐕 2026-07-27. ⛔ **СЕЙЧАС ВЫКЛЮЧЕН** (решение Максима 2026-07-27 вечером: уведомления healthchecks.io в Телеграм и на Gmail достаточно, местный дублировал их). Задание планировщика в состоянии Disabled — файл цел и не запускается ничем; включить обратно: `schtasks /change /tn "C4_Max Bot Watchdog" /enable`. Всё описанное ниже — про то, как он работает, КОГДА включён. МЕСТНЫЙ сторож: отдельный скрипт, который запускает ЗАДАНИЕ ПЛАНИРОВЩИКА Windows «C4_Max Bot Watchdog» раз в минуту (`pythonw.exe` — без мигающего окна), а внутри одного запуска делается ТРИ проверки с шагом `CHECK_STEP_SEC`=20 сек: минута — предел планировщика, и без этого она была бы пределом скорости реакции. Смотрит, жив ли бот (процесс из `bot.pid` + свежесть метки `bot.alive`), и при падении пишет владельцу в Telegram НАПРЯМУЮ через Bot API. ⚠️ НАМЕРЕННО НЕ ЗАВИСИТ ОТ КОДА БОТА (ни handlers, ни services, ни базы — сторож, ломающийся вместе с подопечным, бесполезен); единственное исключение — `config.py`, откуда берутся ТОКЕН и ADMIN_IDS, чтобы не заводить их вторую копию. ⚠️ `os.kill(pid, 0)` для проверки процесса использовать НЕЛЬЗЯ: в Windows это ЗАВЕРШАЕТ процесс — сторож убил бы бота; процесс проверяется через ctypes (`OpenProcess` + `QueryFullProcessImageNameW`). ⚠️ И НИКАКИХ ВНЕШНИХ ПРОГРАММ через subprocess: задание работает под `pythonw.exe` без консоли, и каждый запуск консольной программы МИГАЕТ ОКНОМ (наступили 2026-07-27 на `tasklist` — 180 миганий в час). Память между запусками — `watchdog.state`, свой лог — `watchdog.log` (в папку `logs` не лезет: там всегда ровно два файла). Ручная проверка: `python watchdog_local.py --test` — пришлёт пробное сообщение и ничего больше не сделает |
| `reset_db.py` | Сброс таблиц БД (только при остановленном боте); флаг `KEEP_SETTINGS`; список `USER_TABLES` — 19 таблиц (+settings при KEEP_SETTINGS=False) |
| `handlers/__init__.py` | `setup_handlers`: регистрация ВСЕХ команд и хендлеров; group=1 — `collect_group_message` (TEXT|PHOTO|VOICE|AUDIO с 2026-07-21); незарегистрированная команда попадает в `handle_unknown_command`. С 2026-07-13 четыре ДОЛГИХ хендлера (handle_message, handle_photo, handle_voice, cmd_imagine) зарегистрированы с `block=False` — не держат очередь апдейтов, пока ждут нейросеть; всё остальное обрабатывается строго по одному |
| `handlers/commands.py` | Публичные: /start, /help, /clear, /subscribe, /unsubscribe; `handle_unknown_command` (молча удаляет); `log_incoming_command` (2026-07-13) — центральный регистратор: пишет в лог КАЖДУЮ входящую команду (значок ⌨️), включая неизвестные и админские от не-админов; зарегистрирован в group=-1 (раньше основных), никогда не бросает исключения |
| `handlers/admin/` | ПАКЕТ (2026-07-13 монолит admin.py ~2140 строк разрезан дословно; бэкап admin.py.bak-before-split из корня УДАЛЁН — сверка 2026-07-21 его не нашла, откатываться не на что; шапка `handlers/admin/__init__.py` на него ещё ссылается). `__init__.py` — «оглавление»: re-export всех имён, снаружи пакет выглядит как прежний handlers.admin. Части: `common.py` (гейт админа, `_send_panel_message`, чтение логов, `_fmt_mod_time`), `router.py` (`handle_callback_query` — ЕДИНСТВЕННЫЙ роутер всех callback-кнопок), `panel_main.py` (панель /adm, /stats, модели/API + раскладки `_MODEL_BUTTON_ROWS`/`_IMAGE_BUTTON_ROW`), `panel_prompts.py` (панель промптов + команды /prompt_*, /news_prompt_*, /rag_prompt_*, /proactive_prompt_*; тумблер и регуляторы «Сам в разговор» — `_handle_proactive_callback`, `_PROACTIVE_LIMITS`), `panel_mod.py` (модерация, /unmute), `panel_rag.py` (база знаний, `handle_kb_document`, `_end_kb_test`, «Проверить поиск»), `panel_users.py` (2026-07-20 — раздел «👥 Пользователи»: список `_build_users_panel` + карточка участника `_build_user_card`, роутер `_handle_users_callback` с префиксом `usr:`, команда /users) |
| `handlers/media.py` | /imagine: генерация картинки (Nano Banana — `generate_image` в services/gemini.py), лимит 3/день (админ — безлимит), статус «отправляет фото…». Лимит списывается АВАНСОМ до генерации (`register_image_call`), при неудаче попытка возвращается (`unregister_image_call`) — защита от залпа /imagine при неблокирующем хендлере |
| `handlers/messages.py` | Текст/фото/голос/видео (`handle_video`, 2026-07-24 — только `message.video`, кружочки и гифки намеренно не берём) → ИИ-ответ; тумблер «ответы ИИ» глушит ТОЛЬКО личку админа; `collect_group_message` — архив всех сообщений групп (TEXT/PHOTO/VOICE/AUDIO; с 2026-07-21 различает `has_photo`/`has_voice`) + личное дело (dossier_add_message) + антиспам + фильтр ссылок (только если антиспам не замутил) + хук проактивного режима `proactive.consider_message` (только если не было ни мута, ни удаления ссылки). Прямые ответы бота в группах пишутся в архив помощником `_archive_bot_group_reply` (свои сообщения апдейтами не приходят — без этого стенограмма не видела бы реплик бота) |
| `handlers/quiz.py` | /rank (личное дело, звания, прогресс-бар); викторины Quiz Poll: автоудаление через 60 сек, после ответа следующий вопрос через 2.5 сек (отложенной задачей `application.create_task`, НЕ sleep в обработчике — иначе стояла бы вся очередь), повышение звания → поздравление в ЛС |
| `database/history.py` | ЕДИНСТВЕННЫЙ модуль с SQLite (`history.db`, WAL + `synchronous=NORMAL`, каждый запрос логируется на DEBUG). ⚡ С 2026-07-27 СОЕДИНЕНИЕ ОДНО НА ВЕСЬ ПРОЦЕСС: `_conn` + ленивое открытие в `_get_connection` (там же оба PRAGMA), настоящее закрытие — `close_db()` из `main.py::post_shutdown`, а `close()` у `_LoggingConnection` ПУСТОЙ (заслон: случайный `conn.close()` в новой функции убил бы базу для всего бота). Замок `_lock` — не простой threading.Lock, а обёртка `_DbLock`: при ошибке внутри `with _lock` она ОТКАТЫВАЕТ недописанную транзакцию (при общем соединении та висела бы и уехала в чужой commit). Реентерабельности НЕТ — `get_setting`/`set_setting` звать только ВНЕ `with _lock`. 20 таблиц (2026-07-24 добавлена `stats_snapshots` — снимки счётчиков для суточного отчёта: `save_stats_snapshot` / `get_last_stats_snapshot` / `get_prev_stats_snapshot` / `count_api_calls_between` / `delete_old_stats_snapshots`; последний снимок чистка НЕ удаляет — от него отсчитывается текущий период): messages, user_context, sent_news, news_subscriptions, quiz_stats, bot_sent_messages, settings, api_calls, user_image_calls, group_messages (с 2026-07-21 графа `has_voice` — голосовые в стенограмме проактивного режима), user_token_usage, moderation_log (с 2026-07-17 графа admin_name — кто снял мут; у мутов/ссылок автоматики пусто), mute_evidence, knowledge_log, user_dossier (ВЕЧНОЕ личное дело участника групп: first_seen-стаж, счётчики сообщений/мутов/ссылок — не чистится никем; с 2026-07-20 графы username/first_name — имя для списка пользователей, архив групп чистится через 10 дней и именами больше не источник), user_settings и known_chats (2026-07-20, раздел «👥 Пользователи»). `user_settings` — персональные настройки участника, ПУСТОЕ поле = «работает общая настройка бота» (пороги антифлуда, лимит картинок, звание); белый список изменяемых колонок `_USER_SETTING_FIELDS`, функции get/set/clear_user_settings + get_all_user_settings (загрузка кэша). `known_chats` — группы бота (chat_id, title, last_seen; `remember_chat`/`get_known_chats`), нужны карточке, чтобы знать, ГДЕ мутить/кикать; одноразово засеяны из архива групп в init_db, название подтягивается при первом сообщении или при открытии карточки. `list_known_users` — сводный список всех, кого знает бот (личное дело + архив групп + викторина + учёт токенов; люди из лички тоже попадают). `staff` (2026-07-20) — МОДЕРАТОРЫ и их права: графы p_mod / p_ban / p_cards / p_cards_edit / p_antispam + granted_by/granted_at; владельцев здесь НЕТ (они в config.ADMIN_IDS); белый список колонок `_STAFF_PERM_FIELDS`, функции get_all_staff / get_staff / add_staff / remove_staff / set_staff_perm — но звать их напрямую нельзя, только через services/roles.py (иначе кэш прав отстанет). `staff_log` (2026-07-20) — ЖУРНАЛ ДЕЙСТВИЙ ПЕРСОНАЛА (ts, actor_id, actor_name, action, target_id, details): сюда пишутся ТОЛЬКО осознанные действия человека — и владельца, и модераторов, включая смену настроек и выдачу прав (в moderation_log, для сравнения, пишет и автоматика). Функции log_staff_action / get_recent_staff_actions / count_staff_actions / delete_old_staff_log / clear_staff_log; хранится `STAFF_LOG_DAYS`(30) дней, чистит cleanup_loop. Журнал МОДЕРАЦИИ чистят двое: `delete_old_moderation_log` (по сроку, cleanup_loop) и `clear_moderation_log` (2026-07-20, кнопка «🧹 Очистить журнал» в /mod) — оба стирают `mute_evidence` ВМЕСТЕ с записями журнала: улики привязаны к `log_id`, без журнала до них уже не добраться. Для проактивного режима: `get_recent_group_messages(chat_id, limit)` (стенограмма чата, индекс idx_group_messages_chat; уважает «черту» — см. ниже) и `get_proactive_instruction()` (своя из settings или заводская — как get_rag_instruction). `set_proactive_reset_mark()` (2026-07-20, кнопка «🧹Очистить РАЗГОВОРЫ») — «ЧЕРТА» под стенограммой: пишет в settings ключ `proactive_reset_mark` (время самой SQLite, `SELECT CURRENT_TIMESTAMP` — формат обязан совпадать с `created_at`, иначе сравнение строк перестанет быть хронологическим), после чего `get_recent_group_messages` добавляет условие `created_at > метка`. НИЧЕГО НЕ УДАЛЯЕТ — архив цел, счётчик /stats и имена для /users не страдают; обратимо стиранием ключа |
| `services/http.py` | ⚡ 2026-07-27. Переиспользуемые сетевые соединения: `session()` отдаёт `requests.Session` ТЕКУЩЕГО ПОТОКА (thread-local — запросы к моделям идут из рабочих потоков, а Session потокобезопасности не обещает; потоки в пуле переиспользуются, поэтому TLS-«рукопожатие» в каждом случается один раз за жизнь бота). Заменяет прямые `requests.post/get` в gemini.py, rag.py, scraper.py — раньше каждый запрос заново делал рукопожатие (замерено 130–200 мс до Google на КАЖДЫЙ вызов). ⚠️ `max_retries=0` НАМЕРЕННО: у бота своя логика повторов и подстраховки в gemini.py, повторы здесь умножились бы на те и превратили таймаут 60 с в минуты молчания |
| `services/gemini.py` | Прямые REST-вызовы через переиспользуемую сессию `services.http.session()` (импортируется как `_http`; НЕ SDK — намеренно!). `ask_gemini` (текст/фото), `ask_gemini_audio` (цепочка `AUDIO_FALLBACK_CHAIN`), `format_news_as_colonel`, `generate_image`. Qwen и DeepSeek ходят через общий потоковый OpenAI-совместимый запрос `_openai_stream_request` (мысли из reasoning_content → `<thought>`; общий предел `GEMINI_STREAM_DEADLINE`=90с на весь поток — иначе «висяк» при подтекающих данных, см. раздел 2.5); параметр включения рассуждений у каждого свой (enable_thinking / thinking.type). Для DeepSeek и Qwen считается точная стоимость запроса (`_deepseek_cost` / `_qwen_cost`: кэш-поля usage × `DEEPSEEK_PRICES`/`QWEN_PRICES`; у Qwen кэш из `prompt_tokens_details.cached_tokens`, на практике часто 0 — неявный кэш Alibaba «без гарантий») — пишется в лог строкой «≈$…» и копится в settings (`deepseek_cost_usd` / `qwen_cost_usd`, атомарно через `hist.add_deepseek_cost`/`add_qwen_cost`; `add_deepseek_cost` с 2026-07-21 заодно вычитает сумму из остатка баланса `deepseek_balance_usd` — ключа нет, вычитать нечего, UPDATE молча пропускается). Учёт токенов сверен с кабинетами всех 3 провайдеров 2026-07-05 — совпадение токен в токен. 2 попытки активной модели (запасные в цепочке — по одной; НО на таймаут — сразу к следующей, без второй попытки, см. раздел 2.5) → фолбэк-цепочка «перекрёстной подстраховки»: активна Gemini → `FALLBACK_MODEL`, затем `gemini-3.1-flash-lite`, в конце `qwen3.7-plus`; активна Qwen/DeepSeek → сразу бесплатные Gemini-lite; active_model НЕ меняется, админам уведомление ≤1 раз/час, юзеру — `SOFT_FAIL_MESSAGE`. VISION-REROUTE (2026-07-10): фото при активной модели БЕЗ поля vision (DeepSeek/Qwen) в неё не отправляется вовсе — анализ сразу идёт цепочке Gemini (2026-07-24 порядок задан Максимом и совпадает с аудио/видео: gemini-3.5-flash → `FALLBACK_MODEL` → gemini-3.1-flash-lite), БЕЗ уведомления админам (штатный режим, как у аудио; `has_image` в `_gemini_chat_request`, помощник `_supports_vision`). `ask_group_proactive` (2026-07-16) — проактивный запрос «Сам в разговор»: стенограмма чата + характер + RAG по триггеру + инструкция участия, ответ либо реплика (С блоком `<thought>` — в чате станет свёрнутыми «Мыслями»), либо маркер «ПРОПУСК» (`_is_proactive_skip` — проверяется по видимой части ответа, без мыслей); идёт через АКТИВНУЮ модель с обычной цепочкой подстраховки (решение Максима 2026-07-16, принудительная бесплатная цепочка force_chain удалена в тот же день), add_messages НЕ вызывается. С 2026-07-21: `_proactive_describe_image` (лёгкий vision-запрос к gemini-3.1-flash-lite, без истории/RAG) и `_proactive_transcribe_audio` (native generateContent к gemini-3.1-flash-lite) — анализ медиа для проактивного триггера; время ответа логируется на INFO. Стенограмма в `_build_proactive_parts` подменяет `[фото]`/`[голосовое]` реальным текстом анализа для последнего сообщения |
| `services/roles.py` | ЕДИНСТВЕННЫЙ источник правды О ДОСТУПЕ (2026-07-20). Три уровня: ВЛАДЕЛЕЦ (`config.ADMIN_IDS`, из панели не меняется — иначе управление ботом можно потерять), МОДЕРАТОР (таблица `staff` + галочки прав), обычный участник. Права `PERMS` — ЧЕТЫРЕ (2026-07-20): `mod` (мут/размут, журнал, улики), `ban` (бан/разбан И КИК), `cards` (смотреть карточки), `cards_edit` (лимит картинок, обнуление нарушений, почётное звание). Право `antispam` УДАЛЕНО — общие пороги и тумблеры только владельцу; графа p_antispam осталась в БД, но не читается. Связка `_IMPLIES`: `cards_edit` включает `cards`, снятие `cards` снимает `cards_edit`. Кэш в памяти, `load()` из main.py; менять права только через `make_moderator`/`unmake_moderator`/`grant_perm` (пишут в БД И в кэш). Вопросы: `is_owner`, `is_moderator`, `is_staff`, `role_of`, `can(uid, право)`, `has_any_perm`, `perms_of`, `list_moderators`, `can_act_on(actor, target)` (иерархия: владелец — ко всем, модератор — только к обычным участникам, не к себе). **ТАБЛИЦА ДОСТУПА К КНОПКАМ** `_CALLBACK_RULES` + `perm_for_callback` + `may_press` — ЕДИНОЕ место, где записано, какое право нужно для какой кнопки; ветки `usr:pick/step/do` разбираются отдельно (право зависит от действия внутри данных, `_ACTION_PERMS`). ЗАПРЕТ ПО УМОЛЧАНИЮ: кнопка, которой нет в таблице, доступна ТОЛЬКО владельцу |
| `services/user_settings.py` | ЕДИНСТВЕННЫЙ источник правды о персональных настройках участников (2026-07-20, карточка /users). Держит таблицу `user_settings` ЦЕЛИКОМ В ПАМЯТИ: настройки нужны на каждое сообщение группы, а на горячем пути бот в БД не ходит (замерено: обращений к БД на сообщение поровну — с персональными настройками и без). `load()` — один раз при старте из main.py сразу после init_db; `set_field(user_id, поле, значение)` — ЕДИНСТВЕННЫЙ разрешённый способ записи (пишет в БД И обновляет кэш), `clear(user_id)`, `refresh(user_id)`, `get(user_id)` → dict ({} = всё общее; при незагруженном кэше сам зовёт load один раз). Смысловые помощники, которыми пользуется остальной код: `thresholds_for`, `is_immune`, `links_allowed`, `ai_ignored`, `image_limit_for` (None = безлимит у админов), `honorary_rank` (`trusted_override` удалён 2026-07-20 вместе с ручным статусом «проверенный»). Модуль «тихий»: ошибка = «персональных настроек нет» |
| `services/antispam.py` | Антифлуд + фильтр ссылок: см. раздел 2.4. Входы — `check_and_mute()` и `check_and_delete_links()` из collect_group_message; счётчики в памяти, журнал/улики в БД. Размер всплеска считает ОДИН помощник `_count_messages` (2026-07-19): альбом фото = одно отправление |
| `services/proactive.py` | Режим «Сам в разговор» (2026-07-16): бот сам решает, вступить ли в беседу группы — см. 2.5. `consider_message` (СИНХРОННАЯ, зовётся из collect_group_message: дешёвые фильтры → защёлка → отдельная задача; с 2026-07-21 передаёт `has_photo`/`has_voice` и `file_id` в `_run_proactive`), `_run_proactive` (с 2026-07-21: ДО вызова `ask_group_proactive` анализирует медиа-триггер — фото через `_proactive_describe_image`, голосовое через `_proactive_transcribe_audio`; результат подставляется как `trigger_text`; при пустом результате проверка пропускается; запрос модели через executor БЕЗ «печатает…»; при реплике — пауза 1.5–6с с «печатает…» по длине ВИДИМОЙ части → send_formatted с Reply, мысли уходят в чат свёрнутым блоком → своя реплика в архив групп БЕЗ мыслей, strip_thoughts), `note_bot_group_reply` (обновление паузы после ПРЯМЫХ ответов), `is_enabled`, `forget_conversations` (2026-07-20 — сбрасывает счётчик `_msgs_since_check` после «Забыть разговоры»; паузы и защёлку `_in_flight` НЕ трогает намеренно: сброс пауз только приблизил бы проверку, а снятие защёлки открыло бы дорогу второй параллельной). Счётчики/паузы в памяти (обнуляются рестартом, как у антиспама); модуль «тихий» — наружу не бросает никогда. Значок логов 🤖 (с 2026-07-21, было 🗣) |
| `services/scraper.py` | wtmobile.com (всегда версия /ru/): `fetch_latest_news` (список), `fetch_article` (полный текст + главная картинка + карточки ТТХ) |
| `services/knowledge_store.py` | ВСЕ файловые операции с knowledge/: save_pending_news, list_articles, approve/add/replace/delete_article, read_title. С 2026-07-18 — тип статьи для панели /rag: `ARTICLE_KINDS` (единственное место, где заданы значок и порядок групп: 🚜 ground 0, ✈️ air 1, 🛥️ ship 2, ⚓ sub 3, 📘 guide 4, 📄 other 5), `detect_kind(descriptor)` (по ПЕРВОЙ СТРОКЕ ОПИСАНИЯ статьи — где есть «War Thunder Mobile»; порядок правил `_KIND_PATTERNS` важен: «истребитель танков» ловится наземным правилом ДО авиационного), `_read_meta(path)` → (title, kind) одним чтением файла (read_title — обёртка над ним, старые вызовы работают). `list_articles` кладёт в каждый элемент поле `kind` и сортирует ОДОБРЕННЫЕ по (порядок группы, заголовок); pending остаётся новыми сверху |
| `services/rag.py` | База знаний (⚡ 2026-07-27: длина каждого вектора считается ОДИН раз при загрузке в память — поле `norm` в `_load_into_memory`, счёт сходства через `_cosine_fast(вектор, длина, вектор, длина)` вместо `cosine_similarity`; поиск 30.3 → 12.3 мс, числа совпадают до бита, калибровка остаётся верной. `cosine_similarity` оставлена как есть — ею больше никто не пользуется). Режим нарезки `RAG_CHUNK_MODE` (дефолт "file" — 1 статья = 1 чанк, решение владельца 2026-07-05; "sections" — по `## `), эмбеддинги gemini-embedding-2, индекс `knowledge/knowledge_base_vectors.json` (формат 2 + метка chunk_mode: смена режима → авто-пересборка; инкрементально по mtime); `sync_knowledge_base` (дёшево), `rebuild_knowledge_base` (всё заново). ГИБРИДНЫЙ поиск (2026-07-05, откалиброван 32/32): балл = косинус + лексический бонус (`RAG_LEX_BOOST` 0.10 × доля основ слов запроса, найденных в статье; грубый стемminг `_stem`, слова ≥4 букв, минус `_QUERY_STOPWORDS` и минус сверхчастые основы `_COMMON_STEMS` — в >половине чанков). Отбор `_chunk_passes(score, baseline, floor, margin)`: балл ≥ порога (вспомогательный, панель /rag) И (балл ≥ `RAG_STRONG_SIM` 0.70 ИЛИ отрыв от медианы всех чанков ≥ запаса — «пик против полки»; запас живой, `_live_peak_margin()`, регулятор панели /rag, дефолт 0.14). `test_search` — диагностика для «🔍 Проверить поиск». Устойчивость к лимиту Google (2026-07-19): при индексации 429 → паузы/повторы `_INDEX_RETRY_DELAYS` (10/30/60 сек) + `_INDEX_PAUSE_SEC` (1 сек) между статьями, при исчерпанной квоте `RagQuotaError` и досрочная остановка sync; sync/rebuild возвращают итог (проиндексировано, всего); `index_lag()` — дешёвая проверка полноты индекса для ежечасного добора (без запросов к Google); эмбеддинги запросов пользователей не ждут никогда |
| `data/quiz_questions.py` | Вопросы викторины + `get_random_question()` |
| `knowledge/pending/` | Новости, ждущие одобрения админа в БЗ |
| `knowledge/approved/` | Одобренные статьи — источник RAG (по одному .md на статью) |
| `.env` | TELEGRAM_TOKEN, GEMINI_API_KEY, GEMINI_IMAGEN_API_KEY, QWEN_API_KEY, DEEPSEEK_API_KEY, XIAOMI_API_KEY, WATCHDOG_URL (2026-07-27, адрес отметки healthchecks.io; пустой = наружу не отмечаемся), RAG_ENABLED (мёртвая переменная TRANSLATE_THOUGHTS удалена 2026-07-09 — код её никогда не читал) |
| `requirements.txt` | Внешние библиотеки бота: `python-telegram-bot` (движок), `requests` (запросы к нейросетям и сайту), `beautifulsoup4` (парсинг новостей), `python-dotenv` (чтение `.env`), `telegramify-markdown` (форматирование ответов), `tzdata` (часовой пояс Киева для суточного лимита картинок) |

## 2. Функции бота (проверено по коду)

### 2.1 Публичные команды
- `/start`, `/help` — справка (HTML, панельное сообщение).
- `/clear` — очистка контекста диалога пользователя; подтверждение самоудаляется через 7 сек, после чего в личке у персонала автоматически открывается /adm.
- `/subscribe`, `/unsubscribe` — подписка чата на рассылку новостей.
- `/imagine <описание>` — картинка через Nano Banana (gemini-*-image); лимит 3/день на юзера, админ — безлимит.
- `/rank` — «личное дело»: звание, статистика викторин, остаток лимита картинок, блок «🪪 Служба в гарнизоне» (стаж, сообщений в группах, статус «✅ Проверенный» / «⏳ испытательный срок» / «⚠️ взыскание» — antispam.trust_info), прогресс-бар.
- **Команды `/quiz` НЕТ** — `cmd_quiz` существует в quiz.py, но НЕ зарегистрирован в handlers/__init__.py. Викторина запускается только кнопкой.
- Неизвестная команда → молча удаляется, без ответа.
- В меню Telegram: start, help, imagine, rank, subscribe, clear (без unsubscribe). У админов в личке сверху добавлена /adm.

### 2.2 Админ-команды (только ADMIN_IDS; не-админу — молчаливое удаление команды)
- `/adm` — главная панель-хаб (единственная админ-команда в меню). Только в личке; в группе молча выходит.
- `/stats` — тонкая панель «💬 СТАТИСТИКА» (только личка): 3 строки — обмены «вопрос-ответ» за текущий месяц (из user_token_usage, обнуляется месячным сбросом — см. 2.5), подписчики на новости, архив группы за последние 10 дней; из кнопок только «⬅️ Админ-панель». Бывшая общая панель разделена 2026-07-05 на три: статистика, промпты (`send_prompts_panel`), модели/API (`send_api_panel`) — все три шлются через общий помощник `_send_panel_message` (нарезка >4000 симв., запасной путь без HTML). `get_bot_stats` больше НЕ считает: окна контекста, «сегодня» по диалогам, уникальных пользователей/чатов, «сегодня» по архиву.
- `/mod` — панель модерации (только личка).
- `/users` — панель «👥 Пользователи»: список всех, кого знает бот, и карточка участника (только личка, 2026-07-20). В меню Telegram НЕ добавлена — как /mod, /rag, /stats.
- `/rag` — панель базы знаний (только личка).
- `/unmute` — ТОЛЬКО В ГРУППЕ: ответить командой на сообщение нарушителя. В личке — подсказка.
- `/prompt_set`, `/prompt_add`, `/prompt_reset` — системный промпт (текстом или .txt через Reply).
- `/news_prompt_set`, `/news_prompt_reset` — промпт форматирования новостей.
- `/rag_prompt_set`, `/rag_prompt_reset` — «шапка»-инструкция RAG (текстом или .txt через Reply); reset возвращает ЗАВОДСКУЮ (не «пусто»), с подтверждением. Хранится в settings `rag_instruction`, дефолт `RAG_INSTRUCTION` (config.py). Показывается блоком в панели /prompt.
- `/proactive_prompt_set`, `/proactive_prompt_reset` — инструкция участия в разговоре (режим «Сам в разговор»); зеркала /rag_prompt_* (текстом или .txt через Reply; reset возвращает ЗАВОДСКУЮ, с подтверждением). Хранится в settings `proactive_instruction`, дефолт `PROACTIVE_INSTRUCTION` (config.py). Показывается блоком в панели /prompt.

### 2.3 Кнопки (все проходят через handle_callback_query в handlers/admin/router.py)
**Для всех пользователей:** `quiz_start` (🎮 Викторина) и `clear_history_btn` (🗑️ Очистить историю) — в панели /rank. Все остальные callback'и — только админ (чужим — грубый alert).

- **⚠️ ВРЕМЕННЫЙ РЯД В /adm (2026-07-26, просьба Максима на время тестов новой карточки и справки об авторе — УБРАТЬ, когда тесты закончатся):** «🧹 Очистить РАЗГОВОРЫ» (`adm:wipe`) и «🗑 Очистить мой диалог» (`clear_history_btn`) одним рядом перед кнопкой перезапуска. Первая — дубликат кнопки из панели промптов, но со СВОИМ callback: обработчик `_handle_proactive_wipe` получает флаг `from_adm=True` и возвращает клавиатуру /adm (иначе в админ-панели оказались бы кнопки панели промптов), подтверждение тоже своё — `adm:wipe_yes` / `adm:wipe_no`. Вторая — существующая ветка кнопки из /rank: чистит контекст НАЖАВШЕГО, без подтверждения, панель не трогает. Ради возврата клавиатуры раскладка /adm вынесена в `_adm_rows()` + публичную `build_adm_keyboard(user_id)` (panel_main.py) — ими же собирается сама панель.
- **/adm СОБИРАЕТСЯ ПО ПРАВАМ (2026-07-20):** владелец видит все кнопки ниже; модератор — только «🛠Управление DDoS-Guard» (право `mod`) и «👥 Пользователи» (право `cards`), заголовок у него «🛡 ПАНЕЛЬ МОДЕРАТОРА»; модератор без единого права получает панель без кнопок и подсказку обратиться к владельцу. `send_adm_panel(bot, chat_id, user_id=None)` — user_id по умолчанию равен chat_id (панель только в личке).
- **/adm:** «💬 Статистика» (`adm_open_stats`), «⚙️ Управление PROMPTами» (`adm_open_prompts`, добавлена 2026-07-05), «📡 Настройки API» (`adm_open_api`, добавлена 2026-07-05), «🛠Управление DDoS-Guard» (`adm_open_mod`; надпись переименована 2026-07-20 из «🛡 Модерация» — callback и право `mod` те же), «👥 Пользователи» (`adm_open_users`, добавлена 2026-07-20), «📚 База знаний (RAG)», «📜 Логи бота» (`adm_logs` — присылает ТЕКСТОВОЕ сообщение с максимумом последних строк лога, текст собирает `_build_log_text` под лимит 4096; под сообщением кнопка «💾 Скачать файл» (`adm_logs_file`) — присылает полный файл лога документом с короткой подписью; из-за гигиены панелей файл заменяет собой сообщение с текстом; чтение файла — общий помощник `_read_current_log`, путь берётся из `logging_setup.CURRENT_LOG_FILE`); «🔄 ПЕРЕЗАПУСТИТЬ БОТА» (`system_restart`).
- **Панель статистики (кнопка «💬 Статистика» / команда /stats):** только «⬅️ Админ-панель».
- **Панель промптов (кнопка «⚙️ Управление PROMPTами», `send_prompts_panel`):** заголовок «⚙️ НАСТРОЙКИ PROMPTов»; текст — SYSTEM PROMPT, NEWS PROMPT, 🧠 RAG-PROMPT и 🗣 PROMPT УЧАСТИЯ В РАЗГОВОРЕ (заголовки переименованы 2026-07-20; раньше «RAG-ИНСТРУКЦИЯ» и «УЧАСТИЕ В РАЗГОВОРЕ» — на логику не влияет, это только текст панели) со свёрнутыми превью и командами (блоки RAG и УЧАСТИЕ: счётчик + пометка «своя»/«заводская» + команды /rag_prompt_* и /proactive_prompt_*; тексты из `get_rag_instruction` / `get_proactive_instruction`). С 2026-07-26 между RAG и УЧАСТИЕМ стоит блок «🪪 СПРАВКА ОБ АВТОРЕ» (дословный текст `gemini.author_brief(user_id)`, собранный на смотрящего; НЕ редактируется командами — её собирает код), а в самом низу — сводка «📦 ЧТО УХОДИТ МОДЕЛИ В РЕЖИМЕ „САМ В РАЗГОВОР“» из пяти строк в порядке сборки запроса. Кнопки: тумблеры «⚙️ PROMPT ВКЛ/ВЫКЛ» (персональный промпт админа, ключ `admin_no_prompt_<id>`; RAG при выключенном промпте ПРОДОЛЖАЕТ работать — с 2026-07-05) и «💬 ОТВЕТЫ ИИ ВКЛ/ВЫКЛ» (`ai_replies_enabled` — глушит бота ТОЛЬКО в личке админа) — В ОДИН ряд; ниже блок «Сам в разговор» (2026-07-16): тумблер «🗣 САМ В РАЗГОВОР: ВКЛ/ВЫКЛ» (`proactive:toggle`, ключ `proactive_enabled`, дефолт ВЫКЛ; с 2026-07-24 ВЫКЛючение рассылает во все известные группы объявление `PROACTIVE_OFF_ANNOUNCE` «🛡DDoS-Guard✅ … выдал мне БАН!», ВКЛючение — молча удаляет его, см. раздел 3), отдельным рядом тумблер «🤚 РУКИ (мут до N мин)» (`proactive:hands`, ключ `proactive_hands`, дефолт ВЫКЛ) + ДВА ряда ➖/➕ — «💬 порог» (`proactive:mm_dec/mm_inc`, 1–20 сообщ., ключ `proactive_min_msgs`) и «📜 контекст» (`proactive:ctx_dec/ctx_inc`, шаг 5, 5–50 сообщ., ключ `proactive_context_msgs` — размер стенограммы, добавлен 2026-07-17), серединки `proactive:noop`, клампы `_PROACTIVE_LIMITS` (ряд «⏸ пауза» УДАЛЁН 2026-07-20 вместе с обоими таймерами — см. хронологию); живые цифры и статус — ТОЛЬКО на кнопках (не в тексте!), обновление дешёвым `edit_message_reply_markup`; В ОДНОМ РЯДУ с тумблером (два столбца) стоит «🧹Очистить РАЗГОВОРЫ» (`proactive:wipe` → подтверждение подменой клавиатуры «❗️ Да, очистить РАЗГОВОРЫ» `proactive:wipe_yes` / «Отмена» `proactive:wipe_no` → `_handle_proactive_wipe`): бот забывает стенограмму бесед ВО ВСЕХ группах и у всех людей сразу, включая персонал, БЕЗ удаления архива (метка `proactive_reset_mark`, см. history.py); пишется в журнал персонала кодом `proactive_wipe`, ТОЛЬКО владелец; ниже «📄 Показать полные PROMPTы» (`prompts_files`, 2026-07-20 — см. ниже) и «⬅️ Админ-панель».
  **Кнопка «📄 Показать полные PROMPTы»** (`send_prompt_files` в panel_prompts.py, ветка `prompts_files` в router.py): превью в панели обрезано (`PROMPT_PREVIEW_MAX` = 600 символов), поэтому полные тексты выдаются .txt файлами — по одному на промпт (`1_SYSTEM_PROMPT.txt`, `2_NEWS_PROMPT.txt`, `3_RAG_PROMPT.txt`, `4_PROACTIVE_PROMPT.txt`), состав собирает `_collect_prompt_files`. ПУСТЫЕ промпты пропускаются (решение Максима), при полном отсутствии — самоудаляемое «отправлять нечего». Три правила, которые нельзя «чинить»: файлы НЕ регистрируются в гигиене панелей (иначе каждый следующий удалил бы предыдущий и саму панель) и потому самоудаляются через `_PROMPT_FILES_TTL` = 600 сек; внутри файла ТОЛЬКО текст промпта без шапок — файл задуман обратно загружаемым (Reply на него командой /prompt_set и т.п.), любая служебная строка вклеилась бы в промпт; вся справка (счётчик, «своя/заводская», команда замены) — в ПОДПИСИ к файлу. В файл SYSTEM PROMPT идёт склейка с дополнениями /prompt_add — то, что реально уходит модели. Кнопка отвечает попапом «⏳ Отправляю файлы…» ДО отправки (у Telegram ~15 сек на ответ callback). Кнопка «⬅️ Назад к панели» после /prompt_* ведёт СЮДА (callback-имя историческое `back_to_stats`). Тумблеры перерисовывают клавиатуру этой панели.
- **Панель моделей/API (кнопка «📡 Настройки API», `send_api_panel`):** заголовок «🤖 УПРАВЛЕНИЕ МОДЕЛЯМИ»; текст — вызовы API по провайдерам (♊️ Gemini / 🐪 Qwen / 🐋 DeepSeek / 🍚 Xiaomi — значки берутся из `PROVIDER_ICONS` (config.py), общих с логами; группировка по полю `provider` в AVAILABLE_MODELS; Imagen → Gemini; модели не из конфига — только в общем счётчике; счётчики жирным), расход DeepSeek в виде «расход / остаток» (`deepseek_cost_usd` / `deepseek_balance_usd`, 2026-07-21: остаток аккаунта тает с каждым запросом, счётчик расхода ВЕЧНЫЙ — месячный сброс его не трогает; ссылка на platform.deepseek.com/usage), расход Qwen (`qwen_cost_usd`; ссылка на консоль Model Studio; с 2026-07-24 у каждой модели Qwen рядом с числом вызовов показан ВЕЧНЫЙ счётчик израсходованных токенов — `qwen_tokens_<модель>`, см. раздел 3) и расход картинок в виде «расход / остаток» (`image_cost_usd` / `image_balance_usd`, 2026-07-21: как у DeepSeek — остаток тает с каждым запросом через `add_image_cost`; пополнил счёт Google — поправить `image_balance_usd` в settings; ссылка на aistudio.google.com/spend) — все с точностью 6 знаков; у Qwen при действующей бесплатной квоте Alibaba это «расчётная» стоимость по прайсу, реальных списаний пока нет; общие вызовы всего/сегодня. Ссылки-цифры есть и в панели промптов (счётчики символов → чат с ботом); превью ссылок в панелях отключено в `_send_panel_message`. расход Xiaomi в виде «расход / остаток» (`xiaomi_cost_usd` / `xiaomi_balance_usd`, 2026-07-25: устроен как DeepSeek — остаток тает с каждым запросом через `add_xiaomi_cost`, счётчик расхода ВЕЧНЫЙ; ссылка на platform.xiaomimimo.com). Кнопки: 10 текстовых моделей (раскладка — `_MODEL_BUTTON_ROWS` в handlers/admin/panel_main.py, 5 рядов × 2; ряд Xiaomi добавлен 2026-07-25 — было 4 ряда и 8 моделей) + 2 модели картинок Nano Banana (`_IMAGE_BUTTON_ROW`: gemini-3.1-flash-image / gemini-3.1-flash-lite-image); значки 🧠/🖼 с кнопок убраны 2026-07-05, ✅ — активная; ниже РЯД ОТЧЁТОВ О РАСХОДАХ `_REPORT_BUTTON_ROW` (2026-07-25, решение Максима — переехали сюда из /adm, потому что собраны ровно из счётчиков этой панели): «📊 Отчёт за вчера» (`adm_daily_report`) и «📅 Отчёт за неделю» (`adm_weekly_report`) в ОДНОМ ряду; обеих кнопок в `_CALLBACK_RULES` НЕТ намеренно — запрет по умолчанию оставляет их владельцу, как и всю панель API. С экранов отчётов возврат двумя рядами `_report_back_rows()`: «⬅️ Настройки API» (`adm_open_api`, туда, откуда пришли) и «⬅️ Админ-панель». Смена модели перерисовывает клавиатуру через `_build_api_keyboard`; нажатие УЖЕ активной модели (обе кнопки — текст и картинки) отвечает «уже выбрана» и клавиатуру не трогает — иначе Telegram даёт «Message is not modified» (2026-07-19). Счётчики вызовов и расходов — МЕСЯЧНЫЕ (с 2026-07-09): автоматически обнуляются первого числа, перед сбросом админам приходят «📊 Итоги месяца» (см. 2.5).
- **/mod:** тумблер антиспама; тумблер фильтра ссылок «🟢 Включить / 🔴 Выключить» — как у антиспама (`mod:antispam:linkfilter_toggle`, ключ `linkfilter_enabled`, дефолт ВЫКЛ); ➖/➕ порог сообщений (2–50), окно (2–60 сек), мут (30–86400 сек, шаг 60); кнопки «📜 …» — просмотр удалённых сообщений (улик) последних мутов И удалённых ссылок; «🧹 Очистить журнал» (2026-07-20, ТОЛЬКО у владельца — `mod:clearlog` → подтверждение «❗️ Да, очистить журнал» / «Отмена» подменой клавиатуры → `mod:clearlog_yes`; стирает `moderation_log` ЦЕЛИКОМ вместе с `mute_evidence` через `hist.clear_moderation_log`, пишется в журнал персонала кодом `modlog_clear`); «⬅️ Админ-панель». В журнале панели значки: 🛡 мут, 🔓 размут, 🔗 ссылка удалена; счётчик за 7 дней показывает и ссылки. Панель ЗНАЕТ, КТО ЕЁ СМОТРИТ: `_build_mod_panel_text_and_keyboard(viewer_id)` и `send_mod_panel(bot, chat_id, viewer_id=None)` (по умолчанию = chat_id, панель только в личке). МОДЕРАТОР (право `mod`) видит ВЕСЬ ТЕКСТ панели, кнопки улик «📜 …» и «⬅️ Админ-панель» — и ничего больше: тумблеры, регуляторы ➖/➕ и очистка журнала помечены уровнем владельца и отсеиваются фильтром `_filter_keyboard`.
- **Уведомление о муте в группе:** кнопка «🔓 Размутить» (`mod:unmute:<chat>:<user>`) — жмёт только админ; после размута текст меняется на «размучен администратором …» и удаляется через 30 сек; имя нажавшего админа пишется в журнал (admin_name) и видно в /mod.
- **/users (2026-07-20):** список пользователей кнопками по 2 в ряд (`usr:card:<id>`), метка 🛡 у админов бота и ⚠️ у тех, у кого были муты, рядом с именем — число сообщений в группах; потолок `_USERS_LIST_LIMIT` = 40 (сейчас людей 13, листания пока нет — при превышении в тексте панели пишется «скрыто ещё N»). САМ БОТ из списка ИСКЛЮЧЁН (2026-07-20): он попадает туда через архив групп — там лежат его собственные реплики; `_build_users_panel(viewer_id, bot_id)` отфильтровывает его, `send_users_panel` передаёт `bot.id`, выборка берётся с запасом +2. Его карточка недоступна НИКОМУ, включая владельца (`target_id == context.bot.id` → «⛔ Карточка бота недоступна», проверка стоит первой в адресных ветках `_handle_users_callback`, перед проверкой персонала) — заслон на случай кнопки из старого, ещё не затёртого сообщения со списком: в карточке живые «👢 Кик» и «⛔ Бан». Карточка участника (`usr:card:<id>`) показывает: имя/ник/ID, пометку админа бота, службу в гарнизоне (стаж и сообщения из `antispam.trust_info` — СТРОКА СТАТУСА ДОВЕРИЯ удалена 2026-07-26), нарушения, звание (строка викторины прячется при нуле попыток), контекст диалога «N из 20» и запросы к ИИ за текущий месяц, остаток лимита картинок, где состоит и не под мутом ли (живой опрос Telegram `get_chat_member` по `known_chats` — только при открытии карточки), и блок персональных настроек с пометками «(своё)»/«(общее)». Кнопки карточки (этап 2, 2026-07-20): четыре регулятора ➖/➕ — «💬 порог» (`count`, 2–50), «⏱ окно» (`window`, 2–60 сек), «🔇 мут» (`mute`, шаг 60, 30–86400 сек) и «🎨 картинки» (`img`, 0–50; ряд НЕ показывается админам бота — у них безлимит, регулятор противоречил бы тексту); на средней кнопке живое значение с пометкой «своё»/«общ.», границы `_USER_LIMITS`. Шаг НИЖЕ минимума не упирается, а возвращает поле на общую настройку — единственный способ отменить одно персональное значение; следующий шаг снова стартует от общего. Если значение не изменилось (упёрлись в границу) — перерисовки НЕТ, только попап (защита от ошибки Telegram «Message is not modified»). Тумблеры — ТРИ: «🛡 Иммунитет», «🔗 Ссылки», «🤖 ИИ» (игнор); тумблер «✅ Проверенный» УДАЛЁН 2026-07-20 вместе с ручным статусом (статус присваивается только автоматикой). «↩️ Сбросить всё персональное» (`usr:reset` → подтверждение → `usr:resetok`, отмена `usr:cancel` возвращает карточку в том же сообщении) — кнопка появляется, только если персональное что-то задано. Каждая правка пишется в лог с id админа и именем участника. Изменение перерисовывает карточку ЦЕЛИКОМ (`_redraw_card`), включая живой опрос членства в группах. Ниже — РУЧНЫЕ ДЕЙСТВИЯ (этап 3, 2026-07-20): «🔇 Мут», «🔓 Размут», «👢 Кик», «⛔ Бан», «🔙 Разбан», «🧹 Очистить диалог», «🧾 Обнулить нарушения», «🎖️ Почётное звание». Действия с группой идут в три шага: `usr:pick:<id>:<действие>` (выбор группы; если группа одна — шаг пропускается) → `usr:step:<id>:<действие>:<чат>` (для мута экран сроков 5/30 мин, 1/6/24 ч; для кика и бана — подтверждение с предупреждением, чем они отличаются; размут и разбан выполняются сразу) → `usr:do:<id>:<действие>:<чат>:<аргумент>`. Очистка диалога (`usr:clr`/`clrgo`, зовёт общий `clear_history` — то же, что /clear) и обнуление нарушений (`usr:viol`/`violgo`, `dossier_reset_violations`: обнуляет муты/ссылки/метку взыскания, стаж и число сообщений НЕ трогает) — с подтверждением. Почётное звание (`usr:rank` → `usr:rankset:<id>:<номер в QUIZ_RANKS>`, номер −1 = убрать) хранится ИМЕНЕМ в `user_settings.honorary_rank`, значок и характеристика берутся из `QUIZ_RANKS`. Всего в карточке 27 кнопок. Каждое действие пишется в лог и в журнал модерации с именем админа. РОЛЬ И ПРАВА (2026-07-20, видно и доступно ТОЛЬКО владельцу): в тексте карточки ОДНА строка роли (👑 владелец / 🛡 модератор · прав N из 4 / 🙍 обычный участник), кнопкой — «🛡 Сделать модератором» (`usr:role:<id>:on`) либо, если человек уже модератор, «⚙️Настройки Прав Модератора» (`usr:rights:<id>`) — вход на ОТДЕЛЬНЫЙ ЭКРАН (`_show_rights`, заменяет карточку в том же сообщении): четыре галочки прав (`usr:perm:<id>:<код>`), нарисованные ДЕРЕВОМ ├ │ └ с пояснением к каждому (поле `hint` из `roles.PERMS`), «🚫 Снять модератора» (`usr:role:<id>:off` → подтверждение «✅ Да, снять» → `usr:role:<id>:offgo`; «⬅️ Отмена» возвращает на экран прав, а не в карточку) и «⬅️ К карточке» (`usr:cancel:<id>`). Галочка перерисовывает САМ ЭКРАН прав (`_show_rights`), а не карточку — права накликиваются подряд; после снятия модератора возврат в карточку (`_redraw_card`), экран прав снятому уже не о чем. Владельца из панели не тронуть — он в config.py. Вся карточка собирается ПО ПРАВАМ смотрящего (`_build_user_card(bot, target_id, viewer_id)`) — клавиатура строится полной, лишнее убирает `_filter_keyboard` по таблице прав. МОДЕРАТОРУ с «⚙️ Правкой карточек» достаются: регулятор картинок, «🔇 Мут»/«🔓 Размут» (право `mod`), «🧾 Обнулить нарушения», «🎖️ Почётное звание», навигация; с правом `ban` добавляются «👢 Кик», «⛔ Бан», «🔙 Разбан». Владельцу оставлены персональные пороги/окно/мут, все три тумблера, «🧹 Очистить диалог» и «↩️ Сбросить всё персональное». ТЕКСТ карточки у модератора полный — режется только клавиатура. Карточки персонала модератору НЕДОСТУПНЫ вовсе (одна проверка на все ветки в начале `_handle_users_callback`). В списке пользователей метки 👑 владелец / 🛡 модератор / ⚠️ были муты, и ТОЛЬКО у владельца кнопка «📋 Журнал персонала» (`usr:slog`): экран последних 25 действий персонала со значками из `_ACTION_TITLES`, счётчиком за 7 дней и кнопкой «🧹 Очистить журнал» (`usr:slogclear`). Ниже «⬅️ К списку» (`usr:list`) и «⬅️ Админ-панель».
- **/rag:** до 81 кнопки статей В ТРИ СТОЛБЦА (🕐 pending / ✅ approved; потолок `_KB_LIST_LIMIT` в panel_rag.py — Telegram даёт ~100 кнопок на сообщение, 15 из них служебные; было 84 при 12 служебных, регулятор «Запас над фоном» 2026-07-19 добавил ряд из трёх; статей больше 81 → снова надпись «скрыто ещё N», тогда делать листание). С 2026-07-18 у ОДОБРЕННЫХ после ✅ стоит смысловой значок типа техники (`✅🚜` наземная, `✅✈️` авиация, `✅🛥️` корабли, `✅⚓` подлодки, `✅📘` игровые механики, `✅📄` тип не опознан — сигнал, что в первой строке статьи забыли указать тип), и список отсортирован по этим группам (наземная → авиация → корабли → подлодки → механики), внутри группы по алфавиту; pending по-прежнему сверху и новыми вперёд. Значки/порядок — `ARTICLE_KINDS` в knowledge_store.py, тип определяет `detect_kind`. Заголовок на кнопке урезан до 16 символов (значок съедает ширину), у статей-механик срезается приставка «Игровая механика:» — иначе разные статьи выглядели на кнопке одинаково; «🔍 Проверить поиск» (`kb_test` — ПЕРЕКЛЮЧАТЕЛЬ: включает `user_data["kb_test_mode"]`, следующие текстовые сообщения админа в личке уходят в `handle_kb_test_query` (диагностика поиска с процентами сходства), а не в ИИ; пока включён — метка кнопки «⏹️ Завершить проверку», повторное нажатие завершает; режим переживает подстройку ➖/➕ и тумблер «RAG в моей личке», любой другой выход завершает его через `_end_kb_test` — с УДАЛЕНИЕМ всех накопленных тестовых сообщений из чата, см. раздел 3); три ряда ➖/➕ — «🎯 Порог» (шаг 0.02, диапазон 0.05–0.95, ключ `rag_min_similarity`), «📦 Статей в ответ» (шаг 1, диапазон 1–10, ключ `rag_top_k`) и «📈 Запас над фоном» (2026-07-19; шаг 0.01, диапазон 0.00–0.30, ключ `rag_peak_margin`, дефолт `RAG_PEAK_MARGIN` 0.14 — насколько статья должна оторваться от медианы, чтобы пройти как «пик над полкой»; 0 = правило выключено), перерисовывают панель через `_build_rag_panel(context, admin_id)`; тумблер «📖 RAG в моей личке» (`kb_myrag`, ключ `admin_no_rag_<id>` — глушит подмешивание базы в ответы ИИ ТОЛЬКО в личке этого админа, проверка в gemini.py по `chat_id == user_id`; на «Проверить поиск» не влияет; режим теста переживает этот тумблер); в тексте панели два статуса: «🌐 RAG для всего бота» (глобальный RAG_ENABLED из .env) и «📖 База в моей личке» (персональный тумблер); «➕ Добавить RAG» (следующие .md/.txt файлы в личке станут статьями); «🔄 Пересобрать RAG» (с 2026-07-19 — ФОНОВАЯ задача: сразу попап «⏳ запущена», по завершении сообщение с честным итогом «проиндексировано X из Y» (самоудаляется 60 сек; итог и в журнале БЗ) и перерисовка панели; на время пересборки защёлка `kb_rebuild_running` в bot_data закрывает повторный запуск, одобрение, удаление и приём файлов вежливым «⏳…»); «🧹 Очистить журнал» (с подтверждением). Карточка статьи (отправляется ФАЙЛОМ): «✅ Одобрить в базу» (для pending), «📝 Заменить», «🗑 Удалить» (с подтверждением), «⬅️ К списку».
- Подтверждения сброса промптов: prompt_reset_confirm/cancel, news_prompt_reset_confirm/cancel, rag_prompt_reset_confirm/cancel (возврат заводской RAG-инструкции), proactive_prompt_reset_confirm/cancel (возврат заводской инструкции участия).

### 2.4 Антифлуд (services/antispam.py) — как есть в коде
- По умолчанию ВЫКЛЮЧЕН (`ANTISPAM_ENABLED_DEFAULT = "0"`); включается в /mod. Настройки глобальные на все группы.
- Детектор: ≥N сообщений за окно (дефолт 5 за 5 сек), счётчик в памяти процесса (сбрасывается при рестарте — это нормально).
- Срабатывание: временный мут через `restrict_chat_member` (дефолт 300 сек) → **удаление всего спам-всплеска** → запись в `moderation_log` + **тексты удалённых сообщений в `mute_evidence` (улики) ДО удаления** → сообщение в чат «🛡 X получает мут…» с кнопкой «🔓 Размутить».
- Защищены от мута: ADMIN_IDS, сам бот, админы/владелец Telegram-группы; если статус проверить не удалось — НЕ мутим (безопасный дефолт).
- Пока мут активен, «протёкшие» сообщения нарушителя добиваются поштучно.
- Размут (`unmute()`): восстанавливает ДЕФОЛТНЫЕ права группы (не «всё разрешено»), пишет unmute в журнал с именем админа (параметр admin_name — передают кнопка и /unmute). Вызывается кнопкой и командой /unmute.
- Статистика в /mod: мутов/размутов за 7 дней (`MOD_STATS_DAYS`), последние 5 действий (у размутов приписка «(снял: …)» — из admin_name; старые записи без неё), просмотр улик по кнопке. Хранение 7 дней, чистится cleanup_loop.
- **Альбомы фотографий (2026-07-19):** Telegram шлёт альбом как НЕСКОЛЬКО сообщений с общим `media_group_id` — без поправки альбом из 5 фото мгновенно выбирал порог и человек получал мут ни за что (наступили на этом). Теперь `collect_group_message` передаёт `media_group_id` в `check_and_mute`, а размер всплеска считает `_count_messages`: части одного альбома схлопываются в единицу, РАЗНЫЕ альбомы считаются по отдельности (залп альбомами ловится). В памяти при этом остаются ВСЕ части (удаление спама и улики работают как раньше).
- **Персональные настройки участника (2026-07-20, карточка /users).** Общие пороги перекрываются личными по каждому полю отдельно: `get_thresholds()` — общие (панель /mod), `get_thresholds_for(user_id)` — с учётом личных (её зовёт `check_and_mute` и мут за повторные ссылки). Тумблер «иммунитет» (`antispam_immune`) — автоматика такого человека НЕ мутит вообще: проверяется в самом начале check_and_mute (до регистрации сообщения в счётчике) и отдельно перед мутом за повторные ссылки; на УДАЛЕНИЕ ссылок иммунитет не влияет — для этого свой тумблер `links_allowed` в начале `check_and_delete_links`. Ручного статуса «проверенный» БОЛЬШЕ НЕТ (`trusted_manual` удалён 2026-07-20 вместе с тумблером карточки): `trust_info` считает статус только автоматикой, поля `manual` в её ответе нет; графа `trusted_manual` в `user_settings` осталась в БД мёртвой (как `p_antispam` в staff) — из белого списка `_USER_SETTING_FIELDS` убрана.
- Прогрессивных наказаний НЕТ (MVP — всегда один и тот же мут).
- **Личное дело и статус «проверенный» (2026-07-15):** на каждого участника групп копится вечная запись `user_dossier` (стаж first_seen, msg_count — пишет collect_group_message, mute_count/last_mute_ts — оба пути мута, link_count — фильтр ссылок). Правило доверия ОДНО — `antispam.trust_info(user_id)`: стаж ≥ `TRUST_MIN_DAYS`(7) И сообщений ≥ `TRUST_MIN_MESSAGES`(100) И мутов нет (или последний старше `TRUST_FORGIVE_DAYS`(30) — прощение). ⚠️ БОНУСА К ПОРОГУ АНТИФЛУДА БОЛЬШЕ НЕТ: `TRUST_EXTRA_MESSAGES` удалена 2026-07-20 (решение Максима), проверенный мутится наравне со всеми; статус чисто информационный — его показывают /rank, карточка /users и справка об авторе в проактивном режиме, `check_and_mute` его не читает. /rank показывает блок «🪪 Служба в гарнизоне» (стаж, сообщения, статус) через тот же trust_info. Стаж копится с момента внедрения (дата вступления в группу у Telegram не запрашивается).
- **Фильтр ссылок (2026-07-14, `check_and_delete_links`):** отдельный тумблер «🔗» в /mod (ключ `linkfilter_enabled`, дефолт ВЫКЛ, НЕ зависит от тумблера антиспама). Ловит entity url/text_link в тексте И подписи к фото (разбор только через `parse_entities` — смещения UTF-16!). Белый список `LINKFILTER_WHITELIST` (config.py, дефолт wtmobile.com; поддомены проходят, подделки вида wtmobile.com.evil.ru — нет). Защищённые (`_is_protected`) не трогаются. Действие: удалить сообщение → журнал `moderation_log` (action="linkdel") + улика в `mute_evidence` → предупреждение в чат (самоудаляется 15 сек). За `LINKFILTER_MUTE_COUNT`(3) удалений за `LINKFILTER_WINDOW_SEC`(час) — временный мут обычной длительности (ключ antispam_mute_sec) с кнопкой размута; счётчик повторов `_link_strikes` в памяти. Вызов — из collect_group_message ПОСЛЕ антиспама и только если тот не замутил. Ограничение: видит только текст/фото (фильтр group=1), обходы «точка ком» не ловит.
- **Уведомление модераторов об автоматическом муте (2026-07-20):** после мута за флуд И после мута за повторные ссылки `_notify_moderators_mute` шлёт в личку МОДЕРАТОРАМ (только тем, у кого есть право `mod` — остальным кнопка всё равно не нажмётся) сообщение с названием группы, причиной и кнопкой «🔓 Размутить». ВЛАДЕЛЬЦАМ намеренно НЕ шлётся: у них есть панель /mod с полным журналом (захочешь — одна строка в функции). Ручные муты из карточки уведомления не порождают — их сделал человек, и они видны в журнале персонала. Функция «тихая», как вся antispam.py.
- **Иерархия персонала (2026-07-20):** `_is_protected` не даёт автоматике мутить ВЕСЬ персонал (`roles.is_staff` — владельцы и модераторы). У ручных действий своя охрана `_manual_guard(bot, target_id, actor_id)`: владелец может ко всем, модератор — только к обычным участникам (ни к владельцу, ни к другому модератору, ни к себе; правило одно — `roles.can_act_on`). Все ручные действия принимают `actor_id` — не забыть передать его из панели, иначе охрана вернётся к старому правилу «персонал не трогаем» и модератор сможет мутить модератора. Потолок срока мута для модератора — `config.MODERATOR_MAX_MUTE_SEC`, проверяется ДВАЖДЫ: длинные сроки не показываются на кнопках И отбиваются при выполнении (нажатие старой кнопки не должно обходить лимит).
- **Ручные действия админа (2026-07-20, кнопки карточки /users):** `mute_user` (мут на выбранный срок), `unmute` (был), `kick_user` (в Telegram это бан + немедленное снятие — иначе человек не сможет вернуться), `ban_user`, `unban_user`. Возвращают ПУСТУЮ строку при успехе или текст ошибки Telegram (панель показывает его во всплывающем окне) — в отличие от автоматики, которая всё глушит молча. Проверку `_is_protected` они НЕ применяют (админ группы вполне может быть целью), вместо неё `_manual_guard`: нельзя трогать админов бота из ADMIN_IDS и самого бота. Всё остальное разрешает или запрещает Telegram.
- **Типы записей журнала модерации:** `mute` (автоматика), `mute_adm` (ручной мут), `linkdel`, `unmute`, `kick`, `ban`, `unban`. Панель /mod перечисляет их ЯВНО (словарь `icons` в `_build_mod_panel_text_and_keyboard`): раньше «всё остальное» рисовалось как размут, и с приходом ручных действий журнал начал бы врать. Улики (`mute_evidence`) есть только у `mute` и `linkdel` — кнопка «📜» показывается только им. Графа `admin_name` теперь показывается у ВСЕХ ручных действий («снял:» у размута/разбана, «админ:» у остальных).
- Вся модерация «тихая»: любое исключение глушится, бот не падает.

### 2.5 Автоматическое поведение
- **ИИ-ответы:** личка — на всё; группа — только при @упоминании (в тексте или подписи к фото/видео) или Reply на сообщение бота. Фото — анализ (если активная модель без поля vision — DeepSeek/Qwen — фото ей не отправляется: vision-reroute сразу на цепочку Gemini, без уведомления админам), голос/аудио — через `AUDIO_FALLBACK_CHAIN`, ВИДЕО (2026-07-24) — через `VIDEO_FALLBACK_CHAIN` с проверкой поля `"video"`; подпись к видео уходит модели как вопрос, статус «🎬 Смотрю видео…» удаляется после ответа. Видео больше 20 МБ (`VIDEO_MAX_BYTES`) бот не скачивает — Telegram столько и не отдаёт — и честно отвечает, что ролик великоват. Статус «печатает…» висит всё время генерации.
- **RAG при ответах:** каждый текстовый запрос (и подпись к фото) уходит в гибридный поиск (один дешёвый вызов эмбеддинга, кэш повторов на 128 запросов); статьи, прошедшие отбор (балл = смысл + слова, «пик против полки» — см. services/rag.py), попадают в системный промпт ЦЕЛИКОМ под «шапкой»-инструкцией (`get_rag_instruction`: своя из settings `rag_instruction` или заводская `RAG_INSTRUCTION`; правится из /prompt командами /rag_prompt_*), формат каждой статьи «=== РАЗДЕЛ: … ===». Работает НЕЗАВИСИМО от тумблера «PROMPT ВЫКЛ» (решение 2026-07-05), но уважает персональный тумблер «📖 RAG в моей личке» панели /rag (`admin_no_rag_<id>`, только личка админа). Голосовые RAG не используют (аудио уходит модели напрямую).
- **Добор базы знаний (2026-07-19, jobs.py::rag_catchup_loop):** раз в час проверка полноты векторного индекса (`rag.index_lag()` — сравнение файлов и mtime, БЕЗ запросов к Google). Индекс полный → тишина (ни лога, ни запросов). Неполный (сбой квоты Google) → обычная синхронизация недостающего под той же защёлкой `kb_rebuild_running` (идущую ручную пересборку не трогает — пропускает свой час). Уведомления админам в личку ТОЛЬКО на границах инцидента: одно «⚠️ база неполная X из Y» при первой неудаче добора, одно «✅ снова полная» при выздоровлении; между ними — строки в логе раз в час. Первая проверка через час после старта (стартовую синхронизацию делает main.py). Сообщения не регистрируются в гигиене панелей — остаются в чате как история. `collect_group_message` (group=1) пишет ВСЕ текстовые/фото сообщения групп в `group_messages` и зовёт антиспам. Голос/стикеры/видео НЕ попадают (фильтр TEXT|PHOTO). С 2026-07-16 в архив пишутся и СВОИ ответы бота в группах (прямые — через `_archive_bot_group_reply`, проактивные — из `_run_proactive`): собственные сообщения апдейтами не приходят, а стенограмма «Сам в разговор» должна видеть, что бот уже говорил.
- **«Сам в разговор» (2026-07-16, services/proactive.py):** тумблер `proactive_enabled` в панели промптов, дефолт ВЫКЛ. На каждое сообщение группы (в конце collect_group_message, только если не было ни мута, ни удаления ссылки) — дешёвые фильтры: защёлка «в полёте», не бот (защита от петли бот-бот), не команда, не игнор из карточки (`ai_ignored`), не прямое обращение, тумблер, порог новых сообщений (`proactive_min_msgs`). СУДЬЯ УДАЛЁН 2026-07-20: быстрые модели без мышления плохо следуют инструкциям. Прошло всё → отдельной задачей ОДИН запрос `ask_group_proactive` (активная думающая модель И решает, И пишет): стенограмма последних N сообщений («Ты: …» — свои; N — регулятор «📜 контекст» панели, ключ `proactive_context_msgs`, дефолт `PROACTIVE_CONTEXT_MSGS`=25) + характер + RAG по триггеру + инструкция (`get_proactive_instruction`, включает блок рук — модель видит его всегда); модель пишет реплику ИЛИ слово «ПРОПУСК» (маркер ищется в видимой части, без `<thought>`). Запрос идёт через АКТИВНУЮ модель с обычной цепочкой подстраховки. Реплика уходит В ЧАТ С МЫСЛЯМИ (свёрнутый блок). Стенограмму можно обнулить кнопкой «🧹Очистить РАЗГОВОРЫ».
- **Новости:** каждые 10 мин скрейпинг wtmobile.com/ru → форматирование моделью (промпт `news_system_prompt`) → рассылка подписчикам альбомом (главная картинка + 1 карточка ТТХ; текст ≤1024 — подписью, иначе отдельно) → исходный текст в `knowledge/pending/`. С 2026-07-17 при отправке в ГРУППУ (chat_id < 0) сводка новости пишется в архив групп (`save_group_message` в конце `send_news_to_chat`) — попадает в стенограмму «Сам в разговор»; при полном провале отправки записи нет; `note_bot_group_reply` НЕ вызывается (намеренно — см. раздел 3); в личные чаты архив не ведётся.
- **Викторина:** после ответа любого участника через 2.5 сек автоматически следующий вопрос (ровно один раз на опрос; пауза — отложенной задачей, бот в это время не стоит); каждый опрос автоудаляется через 60 сек; повышение звания → поздравление в ЛС.
- **Очередь апдейтов (2026-07-13):** апдейты обрабатываются по одному (concurrent_updates НЕ включён — вариант А аудита отложен), НО четыре долгих хендлера (текст/фото/голос → ИИ и /imagine) зарегистрированы с `block=False` и выполняются отдельными задачами — долгий ответ нейросети одному пользователю больше не морозит остальных, панели, антиспам и архив групп. Побочный эффект: два вопроса от одного человека подряд могут обрабатываться параллельно (ответы придут в произвольном порядке) — принято как допустимое.
- **Гигиена сообщений:** новое «панельное» сообщение бота удаляет предыдущее в том же чате (`bot_sent_messages`, переживает рестарт); команды пользователей удаляются; служебные подтверждения самоудаляются через 15–30 сек.
- **Лог команд (2026-07-13):** каждая входящая команда пишется в лог строкой «⌨️ Команда: … | от … | личка/чат» центральным регистратором `log_incoming_command` (handlers/commands.py, group=-1) — до основных обработчиков, текст длиннее 100 символов обрезается. Старые точечные строки лога команд, «скрытые по просьбе» в 2026-07, НЕ раскомментировывать — их заменяет регистратор. Выключается одной строкой регистрации в handlers/__init__.py.
- **Запуск:** убийство старой копии по bot.pid (кроме Docker) → уведомление ПЕРСОНАЛУ «✅ Бот запущен» → авто-открытие панели каждому (она затирает уведомление; у модератора панель собрана по его правам). Получателей считает `main._staff_recipients()`: владельцы + модераторы, у которых есть хоть одно право (модератора без прав дёргать при каждом перезапуске незачем). ЭТО ТОЛЬКО про запуск/остановку — итоги месяца, расходы, сбои моделей и состояние базы знаний остаются ВЛАДЕЛЬЦАМ (jobs.py, gemini.py читают ADMIN_IDS напрямую).
- **Остановка:** Ctrl+C → «🛑 Бот остановлен вручную»; кнопка рестарта → без уведомления, новая копия в той же консоли.
- **Фолбэк моделей** (⚠️ модели Xiaomi MiMo в цепочках НЕ участвуют — решение Максима 2026-07-25 «сначала посмотреть их в деле»; активная MiMo подстраховывается как Qwen/DeepSeek — бесплатными Gemini-lite)**:** 2 попытки активной (запасные — по одной) → временная цепочка «перекрёстной подстраховки» вокруг FALLBACK_MODEL (активная Gemini → FALLBACK_MODEL → gemini-3.1-flash-lite → qwen3.7-plus последним рубежом; активная Qwen/DeepSeek → сразу Gemini-lite). ПОРЯДОК ТРЁХ ОСТАЛЬНЫХ ЦЕПОЧЕК (аудио, видео, фото-обход при активной Qwen/DeepSeek) ОДИНАКОВ и задан Максимом 2026-07-24: gemini-3.5-flash → gemini-3.6-flash → gemini-3.1-flash-lite (у аудио и видео четвёртым рубежом gemini-3-flash-preview). Живёт в ТРЁХ местах: `AUDIO_FALLBACK_CHAIN` и `VIDEO_FALLBACK_CHAIN` (config.py) и ветка `vision_reroute` в `_gemini_chat_request` (services/gemini.py) — меняешь порядок, правь все три, иначе фото и голос пойдут по разным маршрутам; админам в личку ≤1 раз/час; юзеру только SOFT_FAIL_MESSAGE. ИСКЛЮЧЕНИЕ (2026-07-05): на ТАЙМАУТ (`requests.exceptions.Timeout`, модель зависла на GEMINI_TIMEOUT=60с) вторая попытка НЕ делается — сразу к следующей модели (иначе ожидание ~123с; так вылезало на нестабильной Gemma). На быстрых ошибках (503/500) повтор прежний. Касается только `_gemini_chat_request` (текст/фото); аудио-путь и так по 1 попытке на модель. ОБЩИЙ ПРЕДЕЛ ПОТОКА (2026-07-06): у Qwen/DeepSeek `GEMINI_TIMEOUT`=60с ловит только паузу МЕЖДУ кусочками потока — добавлен `GEMINI_STREAM_DEADLINE`=90с (config.py) на ВЕСЬ ответ; при превышении `_openai_stream_request` бросает `requests.exceptions.Timeout` → уход к следующей модели цепочки. Лечит «висяк», когда сервер потихоньку шлёт данные, но не заканчивает (наступили 2026-07-06 на qwen3.7-plus — висело ~12 мин, фолбэк не срабатывал).
- **Суточный отчёт о расходах (2026-07-24, jobs.py::daily_report_loop):** в 00:00 по Киеву ВЛАДЕЛЬЦУ в личку (только ADMIN_IDS — решение Максима) уходит отчёт по счётчикам панели «📡 Настройки API» за прошедшие сутки: вызовы по каждой модели (блоки Gemini / Картинки / Qwen / DeepSeek в том же порядке, что в панели; модели с нулём показываются — видно, что модель молчала, а не пропала), расход $ по трём копилкам, сожжённые токены и остаток квоты по каждой модели Qwen, остатки на счетах DeepSeek и картинок. Устройство: панельные счётчики накопительные, поэтому в полночь снимается «фотография» (`stats_snapshots`), а расход = разница снимков; вызовы считаются точно, по времени из `api_calls`. Условие отправки ОДНО — `daily_report.period_closed()` («по Киеву наступили новые сутки с момента снимка»): перезапуск среди дня отчёта НЕ порождает, а пропущенная полночь (бот был выключен) досылается сразу при запуске с честной подписью периода «с 24.07 00:00 по 26.07 12:40» и предупреждением «это больше одних суток». Первый запуск после внедрения: снимков нет — просто ставится точка отсчёта, отчёт придёт в ближайшую полночь. Сообщение шлётся БЕЗ `register_and_clean_bot_message` — как «Итоги месяца», история расходов остаётся в чате. Кнопка «📊 Отчёт за вчера» показывает СОХРАНЁННЫЙ текст последнего ночного отчёта (settings `daily_report_last_text`) плюс живой хвост «сегодня с полуночи: вызовов N · ≈$X». Токенов Gemini и DeepSeek в отчёте НЕТ — их нет и в панели (решение Максима: считать ровно панельные счётчики), поэтому services/gemini.py правкой не тронут вовсе.
- **Недельный отчёт о расходах (2026-07-25, тот же `jobs.py::daily_report_loop`):** каждый ПОНЕДЕЛЬНИК в 00:00 по Киеву ВЛАДЕЛЬЦУ в личку следом за суточным уходит ВТОРОЕ сообщение «📅 РАСХОД ЗА НЕДЕЛЮ» за пн–вс — те же блоки и подписи, что у суточного, плюс строка «📈 В среднем в день: вызовов N · ≈$X» в самом низу (решение Максима). Считается СУММОЙ уже посчитанных суток: каждый суточный отчёт кладёт свои цифры в копилку (`weekly_report_acc`), в понедельник копилка становится отчётом и обнуляется — прямая разница снимков за 7 дней врала бы на неделе с месячным сбросом (см. раздел 3). Условия отправки два: наступил понедельник ЛИБО с начала копилки прошло ≥7 суток (страховка на проспанный понедельник); проверка стоит ПОСЛЕ суточного отчёта, чтобы воскресные сутки успели попасть в копилку. Суток в отчёте меньше или больше семи (бот не работал) — честная подпись «дней: N» и предупреждение, среднее делится на реальное число суток. Первый отчёт после внедрения придёт в ближайший понедельник за неполную неделю. Сообщение, как и суточное, идёт БЕЗ гигиены панелей. Кнопка «📅 Отчёт за неделю» в панели «📡 Настройки API» показывает СОХРАНЁННЫЙ текст (settings `weekly_report_last_text`) плюс живой хвост «с начала недели … : вызовов N · ≈$X».
- **🐕 Сторож: уведомление о том, что бот перестал отвечать (2026-07-27, `jobs.py::watchdog_loop` + `watchdog_local.py`):** бот раз в МИНУТУ отмечается у ВНЕШНЕГО сторожа (healthchecks.io, адрес в `.env` → `WATCHDOG_URL`) и пишет местную метку `bot.alive`. Перед КАЖДОЙ отметкой спрашивает `get_me` у Telegram: не ответил — не отмечается нигде. Отметки прекратились дольше Grace Time (у Максима Period 1 мин / Grace 2 мин) → healthchecks.io САМ пишет владельцу в Telegram (@HealthchecksBot). Плюс «крик умирающего»: при штатной остановке бот сам шлёт `/fail` и тревога приходит за секунды (`main.py::_cry_for_help`; кнопка перезапуска крик не шлёт). ⛔ МЕСТНЫЙ СТОРОЖ ВЫКЛЮЧЕН 2026-07-27 вечером (решение Максима: сообщения healthchecks.io в Телеграм и на Gmail достаточно, местный их дублировал) — задание планировщика «C4_Max Bot Watchdog» в состоянии Disabled, `watchdog_local.py` не запускается ничем, ДВУХ сообщений от бота («🔴 БОТ НЕ ОТВЕЧАЕТ» и «✅ Бот снова работает») БОЛЬШЕ НЕТ. Код цел, включается обратно одной командой (см. таблицу файлов). Когда он включён: задание раз в минуту запускает `watchdog_local.py` (три проверки за запуск, шаг 20 сек): процесс из `bot.pid` жив И метка `bot.alive` свежее 3 минут? Нет — и так ДВА раза подряд — сообщение владельцу напрямую через Bot API. За происшествие ровно два сообщения (тревога + восстановление). ЧТО ЛОВИТСЯ СЕЙЧАС (только внешним): выключенный компьютер, пропавший интернет, упавший, закрытый или зависший бот — всё за ~3 мин; ШТАТНАЯ остановка — секунды (крик). С включённым местным было бы быстрее: упавший бот или закрытое окно ~40 сек, зависший ~3.5 мин. ⚠️ Пока интернета нет, отправить не может НИКТО: уведомление уходит, когда связь вернулась (проверено настоящей аварией 2026-07-27). ЧТО НЕ ЛОВИТСЯ: одновременная пропажа интернета у компьютера И у телефона (сообщение придёт, когда телефон вернётся в сеть) и падение самого healthchecks.io (тогда возможна ложная тревога). РЕШЕНИЕ МАКСИМА 2026-07-27: при РУЧНОЙ остановке бота тревогу НЕ глушить — «пусть присылает», паузу через Management API не заводили. Кнопка «🔄 ПЕРЕЗАПУСТИТЬ БОТА» тревоги не поднимает: перезапуск занимает секунды, а обоим сторожам нужны минуты.
- **Месячный сброс статистики API (2026-07-09):** `jobs.py::_monthly_stats_reset` — раз в сутки (из cleanup_loop) сверяет текущий месяц ПО КИЕВУ с меткой settings `stats_reset_month` («ГГГГ-ММ»). Месяц сменился → каждому админу в личку «📊 Итоги месяца» (расходы DeepSeek/Qwen/картинок + вызовы по моделям + обмены «вопрос-ответ»; сообщение НЕ регистрируется в гигиене панелей — остаётся в чате как история), затем обнуление: таблица api_calls очищается (`hist.clear_api_calls`), ключи `qwen_cost_usd`/`image_cost_usd` = 0 (⚠️ `xiaomi_cost_usd` с 2026-07-25 тоже НЕ обнуляется — вечный счётчик, сверяется с кабинетом Xiaomi, в «Итогах месяца» помечен «за всё время»; `deepseek_cost_usd` с 2026-07-21 НЕ обнуляется — вечный счётчик, сверяется с кабинетом DeepSeek; в «Итогах месяца» его строка помечена «за всё время»), обмены «вопрос-ответ» обнуляются (`hist.clear_user_token_usage`, с 2026-07-09 — поэтому в /stats подпись «за этот месяц»). Сброс НЕ ровно в полночь: при первой суточной проверке в новом месяце (бот мог быть выключен 1-го — тогда при первом запуске). Пустая метка (первый запуск) → просто ставится, без сброса. НЕ сбрасываются: лимиты картинок, статистика викторин/званий, промпты, настройки.

## 3. Зависимости: «меняешь X — проверь Y»

- **Добавил модель в `AVAILABLE_MODELS` (config.py)** → кнопка в панели «📡 Настройки API» НЕ появится сама! Раскладка кнопок — списки `_MODEL_BUTTON_ROWS` / `_IMAGE_BUTTON_ROW` в handlers/admin/panel_main.py, собраны ВРУЧНУЮ по ключам моделей. Меняешь состав моделей — правь обе точки. Группировка вызовов в тексте панели идёт по полю `provider` — новые провайдеры (не gemini/qwen/deepseek) в разбивку не попадут. И НЕ забудь поле `vision`: без него (или с False) модель не будет получать фото — их перехватит vision-reroute (services/gemini.py) и отдаст цепочке Gemini.
- **Значки провайдеров в логах и панели API (2026-07-19)** → заданы В ОДНОМ месте: `PROVIDER_ICONS` в config.py (+ запасной `PROVIDER_ICON_FALLBACK` 🤖 и `RAG_ICON` 📚). Читают их services/gemini.py (помощник `_icon_of`, строки «Запрос к модели» / «Ответ от …», текст и аудио), services/rag.py (строки поиска) и handlers/admin/panel_main.py (заголовки блоков панели). Новый провайдер → добавь значок туда, иначе в логах он пойдёт под 🤖. Значок в строке лога брать ПО МОДЕЛИ ЭТОЙ СТРОКИ (`chain[0]`, `model_name`), НЕ по активной — иначе уход на запасную модель другого провайдера станет незаметным. `_icon_of` смотрит в AVAILABLE_MODELS напрямую, а не через `_provider_of` (тот незнакомой модели отвечает «gemini» — значок вышел бы ложным).
- **Новая команда** → обязательно зарегистрировать в `handlers/__init__.py::setup_handlers`, иначе `handle_unknown_command` будет МОЛЧА УДАЛЯТЬ её без ответа (выглядит как «бот игнорирует»). Плюс решить: добавлять ли в меню (main.py::post_init).
- **Объявление «БАН» при выключении режима «Сам в разговор» (2026-07-24, просьба Максима)** → текст `PROACTIVE_OFF_ANNOUNCE` в config.py продиктован им ДОСЛОВНО, формулировку не править и не «улучшать»; обратного объявления («бан снят») он НЕ хочет — включение просто удаляет сообщение. Механика: `_announce_proactive_off` / `_remove_proactive_off_announce` в panel_prompts.py, координаты отправленных сообщений — список пар `[chat_id, message_id]` в JSON, ключ settings `PROACTIVE_OFF_MSGS_KEY`. Три правила, которые нельзя «чинить»: (1) объявление НЕ регистрируется в гигиене панелей (`register_and_clean_bot_message`) — иначе его затрёт первая же панель, а висеть оно должно до включения режима; (2) координаты только в БД, не в памяти — иначе перезапуск бота, и удалять нечем, объявление зависнет навсегда; (3) сбой отправки/удаления в одной группе молча пропускается и НЕ ломает переключение тумблера (сама ветка `toggle` тоже обёрнута в try). Рассылка идёт по `get_known_chats()` — новая группа подхватится сама, как только бот увидит там сообщение. ⚠️ Telegram не даёт ботам удалять сообщения старше 48 часов: режим, выключенный дольше двух суток назад, оставит объявление в группе навсегда — удалять руками.
- **⚠️ ЗАШИТЫЕ ФРАЗЫ-ИНСТРУКЦИИ К МЕДИА УДАЛЕНЫ (2026-07-24, решение Максима)** → модели уходит ТОЛЬКО файл, без единого слова от бота. Удалены все пять: «Кратко опиши, что на этом фото…» (`_proactive_describe_image`), «Расшифруй это голосовое сообщение…» (`_proactive_transcribe_audio`), «Опиши КРАТКО… что происходит на этом видео» (`_proactive_describe_video`), «Ответь на это голосовое сообщение пользователя.» (`ask_gemini_audio`) и заготовка для видео без подписи (`ask_gemini_video`). Повод: Максим не хочет, чтобы бот подсказывал моделям формулировки от себя (претензия была именно к тому, что фразы сочинены и зашиты без его ведома). НЕ возвращать ни одну из них по своей инициативе — только по прямой просьбе. Добавляя новый вид медиа, фразу-инструкцию тоже НЕ сочинять: шли голый файл. ⚠️ ИЗВЕСТНОЕ ПОСЛЕДСТВИЕ, принято Максимом осознанно («оставить как есть»): в ПРЯМЫХ ответах стало лучше (характер бота из `systemInstruction` ведёт модель сам — проверено живым тестом, ответы в образе и по-русски), а в ПРОАКТИВНОМ разборе характера нет, и модель отвечает как хочет — на живом тесте описания фото и видео пришли ПО-АНГЛИЙСКИ, списками и с таймкодами, а вместо расшифровки голосового модель начала сама отвечать на вопрос. Всё это попадает в стенограмму беседы. Увидишь английский/списки в стенограмме — это не баг, а следствие этого решения.
- **Трогаешь ВИДЕО (2026-07-24)** → четыре правила. (1) Потолок `VIDEO_MAX_BYTES` = 20 МБ — это предел Telegram Bot API на скачивание файла ботом, обойти его нельзя; проверять `video.file_size` ДО `get_file()`, иначе тянем впустую, а человек не понимает, почему бот молчит. `file_size` иногда не приходит — тогда пробуем скачать, падение ловит общий `except`. (2) Обработчик зарегистрирован на `filters.VIDEO` и ТОЛЬКО на него: кружочки (`VIDEO_NOTE`) и гифки (`ANIMATION`) Максим обрабатывать не захотел — не добавлять «до кучи». (3) Цепочка `VIDEO_FALLBACK_CHAIN` задаёт ПОРЯДОК, а право на видео проверяется полем `"video"` через `_supports_video` — модель без поля в запрос не попадёт даже из цепочки. Новая модель, принимающая видео → поставь ей `"video": True` в config, иначе видео ей не отправится. (4) В проактивном режиме свой, БОЛЕЕ НИЗКИЙ потолок `PROACTIVE_VIDEO_MAX_BYTES` = 8 МБ: прямое обращение к боту — просьба человека, а проактив срабатывает на КАЖДОЕ видео в группе. Ролик крупнее остаётся в стенограмме пометкой «[видео]» без описания. Размер неизвестен → в проактиве видео НЕ разбирается (лучше пропустить, чем вслепую тянуть 20 МБ на каждой проверке).
- **Новый ДОЛГИЙ хендлер (сетевые вызовы, run_in_executor)** → регистрировать с `block=False` (как handle_message/handle_photo/handle_voice/cmd_imagine), иначе он заморозит всю очередь апдейтов на время работы. И наоборот: хендлеры, чувствительные к порядку (панели, антиспам, настройки), оставлять блокирующими. Внутри block=False-хендлера проверки «проверил → списал» (как лимит картинок) должны идти БЕЗ await между ними, иначе появится гонка.
- **Лимит картинок (/imagine)** → списывается ДО генерации (`register_image_call` сразу после проверки остатка, без await между ними), при неудаче возвращается (`unregister_image_call`). НЕ переносить списание «после генерации» — залп команд /imagine обойдёт лимит (хендлер неблокирующий).
- **Чужой текст в HTML-панелях (имена пользователей, заголовки статей)** → ВСЕГДА через `html.escape(...)`: символ «<» в имени/заголовке ломает отправку панели (наступили на этом в /mod и журнале /rag, исправлено 2026-07-13; также /rank и поздравление о звании).
- **Новая inline-кнопка** → её callback обязан обрабатываться в `handlers/admin/router.py::handle_callback_query` (единый роутер). Кнопки для ВСЕХ пользователей добавлять ДО гейта прав (как quiz_start). Кнопку для персонала — вписать в `_CALLBACK_RULES` (services/roles.py) с нужным правом, иначе она останется доступна ТОЛЬКО владельцу (запрет по умолчанию; это не баг, а защита — но модератор пожалуется, что кнопка не работает). С 2026-07-20 та же строка таблицы решает и ВИДИМОСТЬ: панели персонала прогоняются через `_filter_keyboard` — отдельного «списка что показывать» больше нет и заводить его не надо.
- **Прячешь кнопку от модератора (2026-07-20)** → правь ТОЛЬКО таблицу `_CALLBACK_RULES` / `_ACTION_PERMS` / `_SETTING_PERMS` в services/roles.py. Не возвращать проверки `if roles.can(viewer, …)` в сборку клавиатур: тогда «видит» и «может» снова разъедутся, и однажды кнопка нарисуется, но не нажмётся (или наоборот). Контекстные проверки — другое дело и остаются на месте: ряд картинок не рисуется админам бота (у них безлимит), кнопки роли — цели-владельцу, «↩️ Сбросить всё персональное» — когда сбрасывать нечего.
- **Кнопка-заглушка со значением (`…:noop`)** → у регулятора ➖/значение/➕ средняя кнопка обязана иметь callback, оканчивающийся на `noop`: по этому окончанию `_filter_keyboard` понимает, что ряд без ➖ и ➕ показывать незачем. Назовёшь иначе — у модератора останется висеть одинокая полоса с числом.
- **Названия и подписи прав в roles.PERMS** → их читает владелец на экране «⚙️Настройки Прав Модератора», решая, что выдавать. `title` права `mod` — «🛡DDoS-Guard» (2026-07-20, переименовано из «🛡 Модерация» вслед за разделом; код права, графа `p_mod` и callback'и НЕ менялись — выданные права не слетают). Название права идёт И в текст экрана, И на кнопку-галочку: правится в ОДНОМ месте, в PERMS. Меняешь раскладку кнопок по правам — перечитай подписи: «мут, размут, журнал и улики» у `mod` и «бан, разбан и кик» у `ban` соответствуют `_ACTION_PERMS`, где кик числится за `ban`.
- **Панель /mod собирается ПО СМОТРЯЩЕМУ (2026-07-20)** → `_build_mod_panel_text_and_keyboard(viewer_id)` зовётся из ЧЕТЫРЁХ мест: `send_mod_panel` (команда /mod и кнопка `adm_open_mod` в router.py), перерисовка после ➖/➕ в `_handle_antispam_callback` (там id берётся из `actor_id`) и возврат из улик `mod:evidence:back`. Забудешь передать viewer_id в одном — у владельца кнопка «🧹 Очистить журнал» пропадёт после первого же нажатия регулятора (собралась панель «ни для кого» → is_owner(0) → False).
- **Экран «⚙️ Права модератора» (2026-07-20, `_show_rights` в panel_users.py)** → живёт В ТОМ ЖЕ сообщении, что и карточка. Галочка права перерисовывает ЭКРАН (`_show_rights`), снятие модератора — КАРТОЧКУ (`_redraw_card`): перепутаешь — либо владельца выбрасывает в карточку после каждой галочки, либо снятому модератору покажется пустой экран прав. Ветка `usr:role:<id>:off` теперь только СПРАШИВАЕТ, выполняет `offgo` — старые кнопки с `:off` из необновлённых сообщений безопасны (они просто переспросят).
- **Новое действие персонала (кнопка, меняющая что-либо)** → рядом с `logger.info` поставить `_audit(admin_id, "код", target_id, "подробность")` (handlers/admin/common.py) И добавить код в `_ACTION_TITLES` (panel_users.py), иначе в журнале он покажется значком ❔ голым кодом. Записывать ПОСЛЕ успеха: неудачная попытка действием не была. Действия ВЛАДЕЛЬЦА тоже пишутся — журнал должен отвечать на вопрос «кто это сделал» без исключений (в записи роль видна значком 👑/🛡).
- **Тяжёлые действия модератора (кик/бан/разбан)** → уведомление владельцу шлёт `_notify_owners` (common.py) прямо из `_run_action`, и ТОЛЬКО когда действовал не владелец: сообщать человеку о том, что он сам только что сделал, незачем. Уведомления идут БЕЗ `register_and_clean_bot_message` — намеренно, это история, панели не должны её затирать.
- **Имя человека для журналов и уведомлений** → `_staff_name(user_id)` в common.py: ищет там же, где список пользователей (личное дело → архив групп → викторина), а не только в личном деле — иначе у того, кто не писал в группах после внедрения имён, в журнале будет голый id. На экранах со СПИСКАМИ имена собирать ОДНИМ проходом через `_known_names()` и передавать вторым аргументом, иначе на каждую строку пойдёт отдельный скан таблиц.
- **Доступ к панелям и командам (2026-07-20)** → проверяется ТОЛЬКО через services/roles.py. Команды — помощником `_require(update, context, право)` из handlers/admin/common.py (он сам молча удаляет команду и отказывает); кнопки — `roles.may_press(user_id, data)` в роутере, ОДИН гейт на все. Не писать `if user_id not in ADMIN_IDS` для доступа — иначе модераторы мимо этой проверки не пройдут, а таблица прав перестанет быть полной картиной. Права проверяются В МОМЕНТ НАЖАТИЯ: снял права — следующее нажатие уже не сработает, перезапуск не нужен.
- **ADMIN_IDS теперь означает ТОЛЬКО «владелец»** и используется в трёх оставшихся смыслах, НЕ связанных с доступом к панелям: личные привилегии владельца (безлимит картинок в `user_settings.image_limit_for`, тумблер «ответы ИИ» в messages.py, тумблер «промпт выкл» в gemini.py), рассылка технических уведомлений (main.py, jobs.py, gemini.py `_notify_admins_fallback`) и метка 👑 в карточке. Для вопроса «пускать ли» всегда `roles.*`.
- **Меняешь список команд в меню Telegram (main.py::post_init)** → он же собирается при выдаче/снятии прав модератора (`_sync_staff_menu` в panel_users.py): у назначенного в меню появляется /adm, у снятого исчезает без перезапуска. Правишь публичный список в одном месте — поправь во втором, иначе у модераторов меню разъедется с остальными.
- **Пакет handlers/admin (с 2026-07-13)** → новые функции панелей класть в профильный модуль (panel_main / panel_prompts / panel_mod / panel_rag / common), а имя, нужное снаружи пакета (регистрация команд, main.py, messages.py), ОБЯЗАТЕЛЬНО добавить в re-export в `handlers/admin/__init__.py` — иначе ImportError при старте. Направление импортов внутри пакета: common ← panel_rag ← (panel_main, panel_mod) ← router; новые связи не должны создавать кольцо.
- **⚡ Пишешь НОВУЮ функцию в `database/history.py` (2026-07-27)** → четыре правила, все из-за того, что соединение теперь ОДНО на весь процесс. (1) `conn = _get_connection()` звать ТОЛЬКО внутри `with _lock` — как все 74 существующие функции; вне замка два потока полезут в одно соединение одновременно. (2) `conn.close()` НЕ писать: он ничего не делает (пустой `close()` у `_LoggingConnection` — заслон от убийства общего соединения), и читатель кода будет обманут. (3) Пишешь в базу — обязательно `conn.commit()`, как и раньше: транзакция теперь живёт в общем соединении, и без commit её содержимое уедет в чужой commit следующей функции. (4) Ловишь исключение ВНУТРИ `with _lock` и не выпускаешь его наружу (как `subscribe_chat` с `sqlite3.IntegrityError`) — сделай `conn.rollback()` сам: откат в `_DbLock` срабатывает только на исключение, вылетевшее ИЗ блока замка.
- **⚡ Новый запрос в интернет (к модели, к сайту, к Telegram raw API)** → звать через `services.http.session()` (в gemini.py/rag.py/scraper.py он импортирован как `_http`, то есть `_http().post(...)`), а НЕ через `requests.post/get` напрямую: прямой вызов заново платит TLS-«рукопожатие» 130–200 мс. `import requests` в файлах остаётся — из него берутся типы ошибок (`requests.exceptions.Timeout` и т.п.), обработка ошибок не меняется. ⚠️ В `services/http.py` НЕ включать повторы (`max_retries`): они умножатся на собственные повторы бота, и таймаут 60 с превратится в минуты.
- **Новая таблица БД** → добавить в `database/history.py::init_db` И в `reset_db.py::USER_TABLES`.
- **Новая графа в существующей таблице** → мало добавить её в `CREATE TABLE` (он выполняется только для НОВОЙ базы): у работающего бота таблица уже есть, и графа не появится. Нужен блок `ALTER TABLE … ADD COLUMN` в конце init_db с глушением `sqlite3.OperationalError` («колонка уже есть») — так сделаны model_name, admin_name и username/first_name в user_dossier.
- **Раздел «👥 Пользователи» (2026-07-20)** → имя участника берётся из `user_dossier` (графы username/first_name пишет `dossier_add_message` на каждое сообщение группы) с запасным источником — архивом групп. Убери передачу имени из `collect_group_message` — и через 10 дней (чистка архива) список станет столбиком голых ID. Список групп для карточки ведёт `remember_chat` оттуда же: перестанешь звать — карточка не узнает, где человек состоит и где его мутить.
- **Персональные настройки участника (`user_settings`)** → ПУСТОЕ поле означает «работает общая настройка бота», а не ноль. Читать только через `is None`, НЕ через `or`: персональный ноль (например, лимит картинок 0 = запрет) иначе молча превратится в общую настройку. Менять колонки разрешено только из белого списка `_USER_SETTING_FIELDS` — имя колонки вклеивается в текст SQL, параметрами его не подставить.
- **Меняешь персональные настройки кодом (2026-07-20)** → ТОЛЬКО через `services.user_settings.set_field` / `clear`. Прямая запись в БД (`set_user_settings` из history.py) не обновит кэш в памяти, и бот будет жить со старым значением до перезапуска — а читает он всегда кэш. Читать — тоже через этот модуль (`get`, `thresholds_for`, `is_immune`, …), а не из БД: смысл кэша в том, что на горячем пути похода в базу нет.
- **Убрал `user_settings.load()` из main.py** → кэш подхватится «ленивым» первым обращением (страховка внутри `get`), но одна лишняя загрузка случится посреди обработки сообщения. Вызов при старте не убирать.
- **Суточный лимит картинок** → считается В ОДНОМ месте: `services.user_settings.image_limit_for(user_id)` (None = безлимит у админов бота, иначе персональный или общий `IMAGE_DAILY_LIMIT`). Его зовут И `/imagine` (handlers/media.py), И `/rank` (handlers/quiz.py). Посчитаешь лимит по-своему в одном из них — показанный остаток разъедется с фактическим. При лимите 0 текст отказа в /imagine ДРУГОЙ («отключена администрацией», а не «лимиты обновятся завтра») — иначе бот врёт про завтра.
- **Новая inline-кнопка в карточке /users** → на callback нужно ответить РОВНО ОДИН раз. Многошаговые действия отвечают внутри `_action_step`/`_run_action`, а вызывающая ветка роутера — НЕ отвечает (иначе второй `query.answer()` вернёт ошибку Telegram, карточка не перерисуется и кнопка будет выглядеть зависшей). Наступили бы на этом на пути «одна группа → размут».
- **Новое звание в `QUIZ_RANKS` (config.py)** → правится ТОЛЬКО там: список читают и расчёт звания (`history.get_user_stats`), и кнопки почётного звания в карточке. Почётное звание хранится ИМЕНЕМ — переименуешь звание в конфиге, у тех, кому оно присвоено, значок и характеристика перестанут находиться (само имя останется висеть). Границы min/max — по возрастанию, без дыр.
- **Почётное звание (`honorary_rank`)** → перекрывает заработанное в ДВУХ местах: `/rank` (handlers/quiz.py) и карточка /users. Там же в quiz.py — глушение автоматических поздравлений о повышении (`handle_poll_answer`): без него бот объявит «присвоено звание Сержант» тому, у кого в /rank висит почётный «Полковник». Прогресс-бар и статистика викторины продолжают жить своей жизнью — это намеренно.
- **Тумблер «бот игнорирует этого человека» (`ai_ignored`)** → проверяется в ЧЕТЫРЁХ местах: три обработчика в handlers/messages.py (`_ai_ignored` — текст, фото, голос) И `proactive.consider_message`. Забудешь проактивный режим — игнор будет обходиться: бот сам вступит в разговор в ответ на сообщение того, кого «игнорирует». На /imagine игнор НЕ влияет намеренно — для картинок есть свой лимит (0 = запрет).
- **Новая настраиваемая фича** → дефолт в config.py, живое значение через get_setting/set_setting (settings хранит СТРОКИ: "1"/"0", числа приводить через int с фолбэком).
- **Меняешь FALLBACK_MODEL** → это единственный источник правды; ничего больше трогать не надо, но имя должно существовать в AVAILABLE_MODELS.
- **DeepSeek поменял тарифы** → обновить `DEEPSEEK_PRICES` в config.py, иначе стоимость в логе и счётчик в /stats будут врать (токены при этом всегда точные — их присылает API).
- **Баланс DeepSeek (`deepseek_balance_usd`, 2026-07-21)** → остаток аккаунта показывается в панели API после «/» и уменьшается сам: `add_deepseek_cost` вычитает стоимость каждого запроса. Максим пополнил счёт DeepSeek → поправить значение ключа в settings руками (панель и кнопки его не меняют). Ключа в settings нет → вычитание молча пропускается, панель покажет $0.000000. Счётчик `deepseek_cost_usd` — ВЕЧНЫЙ (решение Максима 2026-07-21): в `_monthly_stats_reset` его обнуление убрано намеренно, не «чинить» обратно.
- **Alibaba поменяла тарифы Qwen / кончилась акция −50% на qwen3.7-max** → обновить `QWEN_PRICES` в config.py (та же логика, что у DEEPSEEK_PRICES). Цены смотреть в карточках моделей консоли Model Studio.
- **Максим сменил ключ Qwen / рабочее пространство Alibaba (2026-07-24)** → менять НАДО ДВА места: ключ `QWEN_API_KEY` в `.env` И адрес `QWEN_API_URL` в config.py. Адрес привязан к рабочему пространству (`ws-…` в имени хоста) — новый ключ со старым адресом НЕ заработает. Полный адрес = `openAiCompatible` из выгрузки ключа + хвост `/chat/completions` (в CSV его нет). После смены — перезапуск бота (`.env` читается только при старте) и обнуление счётчиков руками: `qwen_cost_usd` и ключи `qwen_tokens_*` в settings (новое рабочее пространство = новая квота, старые цифры бессмысленны). ⚠️ ЭТО ВСЁ — ПРО СМЕНУ ПРОСТРАНСТВА. Если Максим просто выпустил НОВЫЙ КЛЮЧ В ТОМ ЖЕ пространстве (сверять `workspaceId` в CSV выгрузки с хостом `ws-…` в `QWEN_API_URL`), менять надо ТОЛЬКО `QWEN_API_KEY` в `.env`: адрес прежний, счётчики и остаток квоты НЕ обнулять — квота живёт у пространства, а не у ключа, и обнуление соврёт про остаток. Так было 2026-07-24 со вторым ключом (id 952403, то же `ws-twqbbc4ukfp8kv5h`).
- **ОСТАТОК бесплатной квоты токенов по моделям Qwen (2026-07-24, решение Максима)** → ключи settings `qwen_tokens_<имя модели>`, УМЕНЬШАЕТ их `hist.spend_qwen_tokens(model_name, _tt)` из services/gemini.py рядом с `add_qwen_cost` (значение — `total_tokens` из usage, то же «всего», что в строке лога). Устроено как остаток баланса DeepSeek: квота заводится в settings РУКАМИ (2026-07-24 выставлено по 1 000 000 на модель), дальше тает сама; ключа нет — UPDATE молча ничего не находит, панель пишет «квота не задана» (это НЕ то же самое, что 0 — ноль значит «квота кончилась»). Остаток ВЕЧНЫЙ: в `_monthly_stats_reset` (jobs.py) его обнуления НЕТ и заводить не надо — квота Alibaba даётся не помесячно, а объёмом токенов на модель (то же решение, что по `deepseek_cost_usd`). Уходит в МИНУС при исчерпанной квоте — это честный сигнал «расход пошёл за деньги», не «чинить» ограничением снизу. Блок Qwen в панели API собирается ОТДЕЛЬНО от остальных провайдеров (общий цикл его пропускает по `provider != "qwen"`): строки моделей берутся из `AVAILABLE_MODELS`, а не из вызовов, — иначе после месячного сброса модель без вызовов пропала бы из панели вместе с остатком квоты. Модели с заведённой квотой, удалённые из конфига, дописываются строкой «(нет в конфиге)». Максим сменил ключ/пространство → выставить квоту заново (INSERT … ON CONFLICT DO UPDATE по ключу `qwen_tokens_<модель>`).
- **Генерация картинок = Nano Banana (`generate_image` в services/gemini.py)** — эндпоинт `:generateContent` (НЕ `predict` как у Imagen!), тело `{"contents":[{"parts":[{"text":prompt}]}],"generationConfig":{"responseModalities":["IMAGE"]}}`, картинка в ответе — `candidates[].content.parts[].inlineData.data` (base64; рядом бывает thoughtSignature). Меняешь состав `AVAILABLE_IMAGE_MODELS` (config.py) → правь и `_IMAGE_BUTTON_ROW` (handlers/admin/panel_main.py). Старый выбор картинок в settings авто-сбрасывается на дефолт, если модели нет в конфиге (`get_setting` для `active_image_model` — как для `active_model`). Разрешение 1K (flash-lite умеет только 1K). Стоимость картинки считается точно по токенам (`_image_cost` × `IMAGE_PRICES`; картинка 1K = 1120 output-токенов, сверено тестом): пишется в лог «🎨 …≈$…», копится в settings (`image_cost_usd`, `hist.add_image_cost` — с 2026-07-21 заодно вычитает сумму из `image_balance_usd`), показывается в панели API отдельным блоком «🎨 Генерация Картинок» + «💰 Расход Картинок» в виде «расход / остаток» (`image_cost_usd` / `image_balance_usd`; остаток заводится вручную, ссылка aistudio.google.com/spend). Каждая генерация теперь и регистрируется как вызов (`register_api_call`) — попадает в отдельную группу "image" панели и общий счётчик (раньше картинки нигде не считались). Надписи кнопок картинок: 🏞️Gemini 3.1 Flash / 🏜️Gemini 3.1 Flash Lite (поле `name` в AVAILABLE_IMAGE_MODELS). **Google поменял тарифы картинок → обновить `IMAGE_PRICES` в config.py.** Imagen отключён Google 17.08.2026.
- **Трогаешь суточный отчёт о расходах (2026-07-24)** → пять правил. (1) Отчёт считает расход РАЗНИЦЕЙ снимков, поэтому месячный сброс (jobs.py::`_monthly_stats_reset`) ОБЯЗАН перед обнулением звать `daily_report.note_monthly_reset()`: он откладывает в settings (`daily_report_carry`) то, что принадлежит текущим, ещё не отчитанным суткам — вызовы с последнего снимка и значения копилок Qwen/картинок ЦЕЛИКОМ. Формула периода: `(текущее + отложенное) − снимок`. Уберёшь вызов — отчёт за 1-е число занизит цифры на всё, что накапало с полуночи до момента сброса. (2) Кнопка «📊 Отчёт за вчера» показывает СОХРАНЁННЫЙ текст (`daily_report_last_text`), а НЕ пересобирает отчёт: после месячного обнуления вызовов исходных данных за те сутки в базе уже нет, и пересборка показала бы нули. (3) Границы периода: начало включительно, конец строго меньше (`called_at >= start AND < end`), а новый снимок ставится ТОЙ ЖЕ меткой времени, что конец периода — вызов, пришедший в ту же секунду, уходит в следующий период, но не теряется и не считается дважды. Снимок ставится ПОСЛЕ сборки текста. (4) Метка снимка хранится в UTC строкой «ГГГГ-ММ-ДД ЧЧ:ММ:СС» — тем же форматом, что `called_at` (строковое сравнение = хронологическое, как в `get_bot_stats`); поменяешь формат — счёт вызовов за период молча перестанет работать. (5) `delete_old_stats_snapshots` НИКОГДА не удаляет последний снимок: от него отсчитывается текущий период. Отдельно: чтобы в отчёте появились токены Gemini/DeepSeek (в панели их нет), пришлось бы вести журнал расхода в services/gemini.py — от этого отказались осознанно (решение Максима: считать ровно панельные счётчики).
- **Правишь строки КАРТОЧКИ участника (2026-07-26)** → значок 🧠 (`_BRAIN` в panel_users.py) обещает: «эту строку бот пересказывает нейросети». Ставить его можно ТОЛЬКО у полей, реально попадающих в `_who_is_talking`, и только когда значение есть (у пустого почётного звания значка нет — модели уходить нечему). Меняешь состав справки — переставь значки в карточке, иначе она соврёт о том, что знает модель. Легенда внизу карточки печатается всегда — без неё значок выглядит украшением.
- **Меняешь состав СПРАВКИ ОБ АВТОРЕ (2026-07-26)** → она уходит модели ИЗ ДВУХ мест: `_build_proactive_parts` (режим «Сам в разговор») и `ask_gemini` (прямое обращение В ГРУППЕ, условие `chat_id != user_id` — в личке справки нет намеренно). Добавится третий путь — проверь, что порядок блоков тот же (характер → база знаний → справка), иначе пути разъедутся. Состав правится ТОЛЬКО `_who_is_talking` в services/gemini.py. Панель промптов показывает её через публичную обёртку `author_brief` и подхватывает изменения сама — второй «витрины» с ручным перечислением строк заводить нельзя, иначе показанное владельцу и отправленное модели разъедутся (ровно та беда, из-за которой блок и появился). Строку «📋 Берётся из карточки: …» в панели поправить не забудь — она перечисляет поля словами. Сводка «📦 ЧТО УХОДИТ МОДЕЛИ» перечисляет части В ПОРЯДКЕ СБОРКИ `_build_proactive_parts`: меняешь порядок или состав частей там — поправь сводку, иначе она начнёт врать.
- **Трогаешь НЕДЕЛЬНЫЙ отчёт (2026-07-25)** → пять правил. (1) Неделя считается СУММОЙ суток, а не разницей снимков «неделю назад → сейчас», и переделывать на разницу НЕЛЬЗЯ: месячный сброс обнуляет вызовы и копилки Qwen/картинок, и неделя, на которую попало 1-е число, показала бы бессмыслицу (перенос `daily_report_carry` живёт ровно одни сутки — недельную дыру он не закрывает). Проверено прогоном: сброс в середине недели цифры не занижает. (2) `week_add_day` зовётся ТОЛЬКО из `midnight_report` и обёрнут в try: сбой копилки не должен мешать суточному отчёту. Появится второй путь закрытия суток — не забыть позвать и его, иначе неделя молча недосчитает день. (3) `week_closed()` проверяется ПОСЛЕ отправки суточного (jobs.py): поменяешь порядок — в недельный отчёт не попадут воскресные сутки. (4) Сожжённые токены Qwen копятся только положительной разницей; сутки, где остаток ВЫРОС (квоту завели заново после смены ключа), помечают модель в `qwen_reset`, и недельная строка честно пишет «сожжено ≥ N · квоту переставляли». Не «чинить» простым суммированием — цифра станет ложной. (5) Кнопка «📅 Отчёт за неделю» показывает СОХРАНЁННЫЙ текст (`weekly_report_last_text`), как и суточная, — не пересобирать. Рисовалка `render` и счёт `period_totals` ОБЩИЕ для обоих отчётов: правишь вид — меняются оба сразу, в этом и смысл (иначе разъедутся).
- **Трогаешь XIAOMI MiMo (2026-07-25)** → пять правил, все выяснены ЖИВОЙ проверкой на их API, а не из документации. (1) Управление размышлениями у MiMo — В ТОЧНОСТИ как у DeepSeek (`{"thinking": {"type": "enabled"/"disabled", "reasoning_effort": "xhigh"}}`), поэтому `_xiaomi_chat_request` — копия `_deepseek_chat_request` с другим адресом; кэш входа приходит по-другому, ПО-QWEN'ОВСКИ (`prompt_tokens_details.cached_tokens`, а не `prompt_cache_hit_tokens`) — в `_xiaomi_cost` это учтено, не «поправить» на дипсиковский лад. (2) КАРТИНКИ принимает только `mimo-v2.5` (`vision: True`, проверено настоящим изображением — цвет назван верно); `mimo-v2.5-pro` отвечает 404 «No endpoints found that support image input». Активна Pro → фото уходят цепочке Gemini обычным vision-reroute, как с DeepSeek. (3) ⚠️ ПОЛЕ `video` НЕ СТАВИТЬ НИ ОДНОЙ модели MiMo, хотя `mimo-v2.5` видео реально понимает (в тесте назвала цвета кадров по порядку). Причина: поле `video` включает модель в `VIDEO_FALLBACK_CHAIN`, а `ask_gemini_video` шлёт запрос в НАТИВНОМ формате Google (`contents`/`parts`/`inlineData` на `GEMINI_API_URL`) — MiMo такого не понимает. То же и с голосовыми (`ask_gemini_audio`). Хочешь отдать видео или речь MiMo — нужен отдельный OpenAI-формат запроса (`video_url` / `input_audio`), это отдельная работа, а не поле в конфиге. (4) ⚠️ РЕШЕНИЕ МАКСИМА 2026-07-25: видео и голосовые ОСТАВЛЕНЫ НА GEMINI, к MiMo они не идут даже когда она активная. Решение принято ПОСЛЕ прямого сравнения на одних и тех же файлах, повторять сравнение незачем: РЕЧЬ (русская фраза «куда пробивать Абрамс… на карте Синай», ogg/opus — формат Telegram) — Gemini 3.5 Flash расшифровала дословно, MiMo выдала «куда пробиваться на бронеслох… на карте синяя», то есть потеряла и название танка, и название карты (для игрового бота это ровно то, ради чего голосовое присылают); ВИДЕО (ролик с надписями ABRAMS → SINAI MAP → FIRE! и едущим танком) — обе справились, MiMo подробнее и с таймкодами, но подмешала в русский текст китайское слово «继续». Довод в пользу Gemini не только качество: голос и видео у неё идут на БЕСПЛАТНОМ ключе, а каждый разбор у MiMo — деньги со счёта. Форматы, которые API Xiaomi принимает (на случай, если решение когда-нибудь пересмотрят): аудио mp3/flac/m4a/wav/ogg, видео mp4/wmv/mov/avi; максимальный размер файла НЕ выяснен. (5) Расход `xiaomi_cost_usd` — ВЕЧНЫЙ (как у DeepSeek): в `_monthly_stats_reset` его обнуления нет и заводить не надо, иначе сломается сверка с кабинетом Xiaomi. Пополнил счёт — поправить `xiaomi_balance_usd` в settings руками (панель и кнопки его не меняют).
- **Добавляешь НОВОГО ПРОВАЙДЕРА (проверено на Xiaomi 2026-07-25)** → мест СЕМЬ, и три из них молчаливые. Обязательно: (1) адрес, ключ, цены и модели в `config.py`; (2) значок в `PROVIDER_ICONS` — иначе логи пойдут под запасным 🤖; (3) ветка `provider == "…"` в `_try_model` и подсчёт денег в блоке логирования `_gemini_chat_request` (services/gemini.py); (4) копилка расхода в `database/history.py`; (5) раскладка кнопок `_MODEL_BUTTON_ROWS` — кнопка сама не появится; (6) ⚠️ словарь `groups` в `send_api_panel` (handlers/admin/panel_main.py) — забудешь, вызовы просто НЕ ПОКАЖУТСЯ в панели, без единой ошибки; (7) ⚠️ `_calls_by_group` И блок в `render` (services/daily_report.py) — забудешь, вызовы молча уедут в графу «прочие» суточного и недельного отчётов, а расход не попадёт в «Всего денег». Плюс строка в «Итогах месяца» (jobs.py::`_monthly_stats_reset`), если счётчик вечный.
- **🐕 Трогаешь сторожа (2026-07-27)** → пять правил. ⛔ Сначала имей в виду: МЕСТНЫЙ сторож с вечера 2026-07-27 ВЫКЛЮЧЕН заданием планировщика (Disabled), работает только внешний. Правила ниже писались при обоих живых и остаются верны — просто половина из них сейчас спит. Включаешь местный обратно (`schtasks /change /tn "C4_Max Bot Watchdog" /enable`) — перечитай их все. (1) `watchdog_loop` обязан работать ДАЖЕ БЕЗ `WATCHDOG_URL` — выход в начале функции возвращать нельзя: без него не пишется метка `bot.alive`, и местный сторож поднимет вечную ложную тревогу (наступил на этом при написании). (2) Отметка идёт ТОЛЬКО после удачного `get_me` — это и делает её осмысленной («бот достаёт до Telegram», а не «процесс запущен»); безусловная отметка молчала бы при недоступном Telegram. (3) `WATCHDOG_PING_SEC` (1 мин) должен быть заметно МЕНЬШЕ, чем Period + Grace Time в настройках проверки на healthchecks.io (у Максима 1 + 2), иначе тревоги станут ложными; меняешь одно — поправь второе, вторая половина настройки живёт НЕ в коде, а в личном кабинете сервиса. (4) `WATCHDOG_STALE_SEC` (3 мин) должен быть больше `WATCHDOG_PING_SEC` с запасом — иначе одна пропущенная отметка (моргнул интернет) поднимет местную тревогу. ⚠️ ИМЕННО ЭТО ЧИСЛО, А НЕ ЧАСТОТА ПРОВЕРОК, решает, как быстро придёт тревога о зависшем боте — выяснено на настоящей аварии 2026-07-27. И оно же — главный регулятор ложных тревог: раздражают — поднимать его, а не выключать сторожа. (6) Меняешь темп — меняй ОБА конца: числа в config.py И Period/Grace в личном кабинете healthchecks.io. Порядок важен: сначала код и перезапуск бота, потом затягивание сроков на сервисе, иначе получишь ложную тревогу в промежутке (наступил на этом 2026-07-27 — пришлось ставить проверку на паузу). (5) `watchdog_local.py` НЕ импортировать из кода бота и не тащить в него зависимости от `services`/`database`: он обязан работать, когда бот сломан. И ⚠️ никогда не проверять процесс через `os.kill(pid, 0)` — в Windows это ЗАВЕРШАЕТ процесс, сторож убьёт бота.
- **Новая переменная в `.env`, которую читает не только бот** → скрипты, запускаемые ПЛАНИРОВЩИКОМ (сейчас это `watchdog_local.py`), обязаны читать `.env` ПО ПОЛНОМУ ПУТИ и ДО `import config`: `load_dotenv()` внутри config.py ищет файл ОТ РАБОЧЕЙ ПАПКИ, а у задания планировщика она своя (обычно `C:\Windows\System32`). Забудешь — переменная окажется пустой, и скрипт не упадёт, а МОЛЧА не сделает свою работу. Проверять правку запуском из чужой папки (`cd C:\ && python <скрипт>`), а не из папки проекта.
- **Меняешь `collect_group_message` или `check_and_mute`** → они обязаны НИКОГДА не бросать исключения наружу (архив и модерация не должны ронять бота). То же — `proactive.consider_message` (зовётся оттуда же).
- **Меняешь подсчёт всплеска в антиспаме (2026-07-19)** → размер всплеска считает ТОЛЬКО `_count_messages` (альбом = одно отправление); единственный его читатель — проверка порога в `_register_and_check` (второй читатель, бонус «проверенным», удалён 2026-07-20 вместе с самим бонусом; ⚠️ докстринг `_count_messages` в коде всё ещё упоминает бонус — устарел). Записи в `_timestamps` читаются по НОМЕРУ элемента (`_burst_message_ids`, `_burst_records`) — новые поля добавлять только В КОНЕЦ кортежа.
- **Меняешь запись ошибок моделей (services/gemini.py)** → причина отказа приходит в ТЕЛЕ ответа, а не в HTTP-коде: логировать через `_err_body(e)` рядом с `_err_code(e)` (наступили 2026-07-19 — три отказа 400 у qwen3.7-max, причину по логу установить не удалось). Помощник обязан оставаться «тихим» (свои ошибки глушит) — разбор сбоя не должен порождать новый сбой.
- **Новый тип мута в журнале модерации** → добавить его В ТРИ места: словарь значков панели /mod (`panel_mod.py`), `_ACTION_TITLES` (panel_users.py, если это действие персонала) И ⚠️ `get_moderation_counts` (database/history.py) — в последнем перечислены типы, которые считаются в сводке «За 7 дней: мутов N». Забудешь третье — мут будет виден в списке последних действий, но пропадёт из счётчика, и панель начнёт занижать цифру (наступили на этом с `mute_ai` 2026-07-20, нашёл Максим).
- **Трогаешь руки (2026-07-20)** → пометку `[МУТ:…]` ищем ТОЛЬКО в ВИДИМОЙ части ответа (модель может упомянуть её в `<thought>` как пример — это не команда), а ВЫРЕЗАЕМ из всего текста (служебный маркер не должен попасть в чат ни при каких условиях). Цель мута — `trigger_user_id` из апдейта, НИКОГДА не из текста модели: иначе выдуманный ответ сможет ткнуть в произвольного человека. `actor_id=None` в `mute_user` обязателен — это и есть защита персонала (`_manual_guard`); подставишь реальный id — модератор станет доступной целью. Тумблер `proactive_hands` проверяется ДВАЖДЫ (перед запросом и перед выдачей): между ними проходят секунды. Новый тип записи журнала (`mute_ai`) обязан быть в словаре значков панели /mod И в `_ACTION_TITLES` (panel_users.py). Судья удалён 2026-07-20 — `ask_group_judge`, `PROACTIVE_JUDGE_MODEL`, `PROACTIVE_JUDGE_MODELS`, `PROACTIVE_JUDGE_YES` больше не нужны. Блок рук — внутри заводского `PROACTIVE_INSTRUCTION`, модель видит его всегда; `{max_sec}` подставляется вызовом format при первом использовании.
- **Режим «Сам в разговор» (services/proactive.py)** → `consider_message` обязана оставаться СИНХРОННОЙ (без await между проверкой фильтров и защёлкой — иначе гонка), хук в collect_group_message — только при `not muted and not deleted` (иначе Reply на удалённое сообщение). Проверка `user.is_bot` в фильтрах ОБЯЗАТЕЛЬНА — без неё два авто-отвечающих бота в чате уйдут в «пинг-понг». `reply_to` при отправке обязателен — он же кладёт реплику в нужную тему форум-группы. Реплика в ЧАТ уходит с `<thought>`, а в АРХИВ групп — только видимая часть (strip_thoughts); перепутаешь — либо стенограмма замусорится мыслями, либо пропадут «Мысли» из чата. Новые места, где бот пишет в группу сам, — не забыть `save_group_message` своей реплики (иначе стенограмма «слепнет») и `note_bot_group_reply` (иначе бот влезет сам сразу после ответа). ИСКЛЮЧЕНИЕ — новостная рассылка (jobs.py::send_news_to_chat): она пишет сводку в архив, но `note_bot_group_reply` НЕ зовёт НАМЕРЕННО (решение 2026-07-17) — новость не «реплика в беседе», и пауза после неё мешала бы боту участвовать в обсуждении этой же новости; не «чинить».
- **КОМАНДЫ НЕ ПОПАДАЮТ В СТЕНОГРАММУ (2026-07-24, просьба Максима)** → в `_build_proactive_parts` (services/gemini.py) строки архива, чей текст начинается со слэша, пропускаются целиком: команды адресованы механизму, а не собеседникам. Повод — `/clear`: он стирает личный контекст человека, само сообщение Telegram удаляет сразу, но в архив групп оно попасть успевает (архиватор group=1 работает параллельно с обработчиком команды) — и бот видел стёртое. Фильтр стоит ИМЕННО в сборке стенограммы, а НЕ в `collect_group_message`: архив обязан остаться целым — по нему считается «архив группы за 10 дней» в /stats и берутся запасные имена для /users. Работает и на уже накопленный архив. Триггером команды и так не бывают (`text.startswith("/")` в `consider_message`) — это ВТОРОЙ, независимый заслон, снимать ни один из двух нельзя. Подмена `[фото]`/`[голосовое]`/`[видео]` реальным разбором смотрит на `rows[-1]` и требует пустой текст при вложении — команду она не заденет.
- **Трогаешь «черту» стенограммы (2026-07-20, кнопка «🧹Очистить РАЗГОВОРЫ»)** → метка `proactive_reset_mark` ставится ТОЛЬКО временем самой SQLite (`SELECT CURRENT_TIMESTAMP` в `set_proactive_reset_mark`), а не питоновским временем: `created_at` заполняется SQLite, и при разных форматах сравнение строк перестанет совпадать с хронологическим — черта начнёт скрывать не то. `get_setting`/`set_setting` берут `_lock` САМИ, поэтому звать их только ВНЕ блока `with _lock` (обычный threading.Lock не реентерабельный — вложение повесит бота намертво). Условие «новее черты» живёт в ОДНОМ месте — `get_recent_group_messages`; появится второй читатель стенограммы, добавь условие и ему, иначе бот «вспомнит» забытое. Физическое удаление архива вместо черты НЕ делать (решение Максима 2026-07-20): оно обнулит счётчик «архив группы за 10 дней» в /stats и выбьет из списка /users тех, кто известен боту только по архиву.
- **Меняешь панели/служебные сообщения** → отправка через `register_and_clean_bot_message`, иначе сообщения начнут копиться в чате. В `_send_panel_message` (common.py) куски ОДНОЙ длинной панели регистрируются с `keep_count=idx+1` (починка 2026-07-16: с keep_count=1 второй кусок удалял первый — панель длиннее 4000 симв. приходила без начала).
- **Меняешь ответы модели** → отправка ТОЛЬКО через `send_formatted`/`convert_md` с entities; `parse_mode` вместе с entities НЕ РАБОТАЕТ (взаимоисключающие).
- **Меняешь MOD_STATS_DAYS (antispam.py)** → он же используется в jobs.py::cleanup_loop как срок хранения журнала.
- **Белый список фильтра ссылок** → `LINKFILTER_WHITELIST` в config.py (кортеж доменов; поддомены разрешаются автоматически). Пополняется правкой конфига + рестарт. Он же показывается текстом в панели /mod.
- **Меняешь критерии «проверенного»** → только константы `TRUST_*` в config.py; само правило живёт в ОДНОМ месте — `services/antispam.py::trust_info` (его же показывает /rank). Новые события, влияющие на доверие, — писать в user_dossier через dossier_add_* (database/history.py).
- **Меняешь срок хранения архива групп (10 дней в jobs.py::cleanup_loop)** → число «10» зашито текстом в подписи «Архив группы за последние 10 дней» панели /stats (handlers/admin/panel_main.py) и в комментарии `history.py::get_bot_stats` — поправь оба места.
- **Меняешь cleanup_loop или месячный сброс (jobs.py)** → `cleanup_loop(application)` принимает application (с 2026-07-09, нужен `_monthly_stats_reset` для отправки итогов админам) — main.py::post_init передаёт его при создании задачи, сигнатуру без правки main.py не менять. «Итоги месяца» шлются БЕЗ register_and_clean_bot_message — намеренно (история расходов должна оставаться в чате, панели её не затирают). Обнуляются: api_calls (`clear_api_calls`), три ключа `*_cost_usd` и user_token_usage (`clear_user_token_usage` — ⚠️ именно UPDATE до нулей, НЕ DELETE: при полностью пустой таблице init_db заново зальёт старые числа одноразовым переносом из user_context). Подпись «за этот месяц» в панели /stats привязана к этому сбросу — уберёшь сброс, верни «за всё время».
- **Меняешь файлы в knowledge/approved/ кодом** → после approve/add/replace/delete вызывать `sync_knowledge_base()` (панель это уже делает через `_kb_sync_in_executor`), иначе индекс отстанет.
- **Меняешь индексацию RAG / кнопку пересборки (2026-07-19)** → `sync_knowledge_base`/`rebuild_knowledge_base` возвращают честный итог (проиндексировано, всего) или None — его показывает панель. `get_embedding` при ИНДЕКСАЦИИ (is_query=False) может бросить `RagQuotaError` (429 не отпустил после пауз 10/30/60 сек) — новые массовые вызовы эмбеддингов оборачивать в try, как чанковый цикл sync; запросы пользователей (is_query=True) не ждут и не бросают НИКОГДА. Пересборка в панели — фоновая задача под защёлкой `kb_rebuild_running` (bot_data): новые операции панели, которые пишут в knowledge/ или зовут sync, ОБЯЗАНЫ проверять `_kb_rebuild_running(context)` (как одобрение/удаление/приём файла) — иначе две синхронизации наперегонки пишут один файл индекса. Ежечасный добор (jobs.py::rag_catchup_loop) читает и ставит ту же защёлку через application.bot_data напрямую — имя ключа менять только везде сразу. Правило «файл актуален» (есть в индексе И mtime совпадает) живёт в ДВУХ местах: цикл sync_knowledge_base и `index_lag()` — меняешь одно, поправь второе.
- **Меняешь services/gemini.py** → НЕ переводить на google-genai SDK (намеренно REST+requests из-за event-loop конфликтов на Windows). Вызовы идут из worker-потоков — Telegram-уведомления оттуда только через raw Bot API (как в `_notify_admins_fallback`).
- **Меняешь RAG_EMBEDDING_MODEL** → обязательно пересобрать индекс И перекалибровать все константы поиска (порог, RAG_PEAK_MARGIN, RAG_STRONG_SIM): текущие значения откалиброваны под gemini-embedding-2 стендом 2026-07-05 (32/32 запроса). Qwen text-embedding-v4 проверялась и ОТВЕРГНУТА: проваливает русский сленг/прозвища («куда пробивать умку» 34% и не та статья).
- **Трогаешь отбор статей в RAG (2026-07-19)** → запас «пик над полкой» больше НЕ константа: живое значение `_live_peak_margin()` (settings `rag_peak_margin`, регулятор в панели /rag), дефолт `RAG_PEAK_MARGIN` в config поднят 0.06 → 0.14. Читать его РАЗ на запрос и передавать в `_chunk_passes` параметром (иначе чтение настройки на каждую статью базы) — так сделано в `retrieve_relevant_context` и `test_search`. Перекалибровка 2026-07-19 на 30 запросах (20 реальных + 10 болтовни): отрыв от фона у настоящих вопросов ≥0.164, у болтовни ≤0.110 — 0.14 стоит посередине коридора, 20/20 и 10/10. Меняешь модель эмбеддингов или состав базы — замер повторить (скрипт разовый, лежал в scratchpad; воспроизводится через `rag._rank_chunks` + `rag._chunk_passes`, эмбеддинг по одному на запрос).
- **Добавляешь кнопки в панель /rag** → потолок Telegram ~100 кнопок на сообщение делится между статьями и служебными: сейчас 81 статья (`_KB_LIST_LIMIT`) + 15 служебных = 96. Новый ряд регуляторов (3 кнопки) — уменьшай `_KB_LIST_LIMIT` на 3, иначе панель при полной базе просто не отправится. Новые кнопки-регуляторы добавлять и в белый список, который НЕ гасит режим «Проверить поиск» (`_handle_kb_callback`), — иначе кручение настройки будет выкидывать из теста.
- **Меняешь RAG_CHUNK_MODE** → индекс пересоберётся сам при следующем sync (метка chunk_mode в файле индекса), но вектора будут считаться заново для всех статей.
- **Значок типа техники на кнопках /rag** → и значок, и порядок групп заданы В ОДНОМ месте: `ARTICLE_KINDS` в services/knowledge_store.py (панель только читает его). Новый тип техники (например, вертолёты) → добавить запись в `ARTICLE_KINDS` И правило в `_KIND_PATTERNS`; правила проверяются СВЕРХУ ВНИЗ, наземные обороты со словом «истребитель» («истребитель танков», «поддержки танков») обязаны стоять ВЫШЕ авиационного правила, иначе AGS и Centauro станут самолётами. Тип берётся ТОЛЬКО из первой строки описания статьи (где «War Thunder Mobile»), не из всего текста — иначе корабль со словом «самолёт» в тактике «улетит» в авиацию. Проверка после правки правил — скриптом-однодневкой: `list_articles()` и распечатать группы (все 79 статей на 2026-07-18 распознаются, 📄 быть не должно).
- **Пишешь/правишь статьи БЗ** → первая строка описания ОБЯЗАНА содержать тип техники словами («основной боевой танк», «реактивный истребитель», «ракетный эсминец», «подводная лодка») — по ней панель /rag ставит значок 🚜/✈️/🛥️/⚓ и сортирует список (см. навык rag-etalon). Забудешь тип — статья получит 📄 и уедет в конец списка.
- **Пишешь/правишь статьи БЗ** → русские написания прозвищ и названий ОБЯЗАТЕЛЬНО в тексте («Спайк», «умка», «хамер») — лексический канал гибридного поиска работает по буквальному совпадению слов; латиница «Spike» по запросу «спайки» не находится.
- **Меняешь handle_message (messages.py)** → в начале стоит перехват `kb_test_mode` (режим «🔍 Проверить поиск» панели /rag, зовёт `admin.handle_kb_test_query`) — он должен оставаться ДО проверки тумблера «ответы ИИ», иначе проверка поиска перестанет работать при выключенных ответах.
- **Трогаешь режим «🔍 Проверить поиск» (2026-07-06)** → выход из режима ВСЕГДА идёт через один помощник `admin._end_kb_test(bot, chat_id, context)`: он гасит `kb_test_mode` И удаляет из чата все накопленные тестовые сообщения (id вопросов админа и ответов бота копятся в `user_data["kb_test_msgs"]`, пишутся в `handle_kb_test_query`; удаление работает, т.к. режим только в личке админа — бот вправе удалять входящие). Кнопка `kb_test` — ПЕРЕКЛЮЧАТЕЛЬ: включена → метка «⏹️ Завершить проверку», повторное нажатие зовёт `_end_kb_test` (в `_build_rag_panel` метка зависит от `user_data["kb_test_mode"]`). `_end_kb_test` вызывается на КАЖДОМ выходе: кнопка-переключатель, любая другая кнопка панели RAG (`_handle_kb_callback`, кроме белого списка kb_test/kb_noop/kb_thr_*/kb_topk_*/kb_margin_*/kb_myrag), навигация `adm_open_*`/`adm_logs`, и набранные команды `cmd_rag`/`cmd_adm`/`cmd_stats`/`cmd_mod`. Добавляешь новый выход из панели RAG — не забудь позвать `_end_kb_test`, иначе тестовые сообщения останутся висеть.
- **Меняешь main.py** → `setup_logging()` должен остаться ПЕРВЫМ импортом-вызовом, до импорта модулей бота.
- **`.env` И КНОПКА ПЕРЕЗАПУСКА (2026-07-24)** → `load_dotenv(override=True)` в config.py — файл ГЛАВНЕЕ окружения, и снимать `override` нельзя. Кнопка «🔄 ПЕРЕЗАПУСТИТЬ БОТА» порождает копию через `subprocess.Popen`, а та НАСЛЕДУЕТ окружение старой; без `override` dotenv не перезаписывает уже имеющиеся переменные, и ключ, прочитанный самой первой копией, тащился по всей цепочке перезапусков — правки `.env` игнорировались МОЛЧА (ни лога, ни ошибки). Наступили 2026-07-24: после смены рабочего пространства Qwen бот несколько часов слал СТАРЫЙ ключ на НОВЫЙ адрес и получал 403 с пустым телом; лечилось только полным перезапуском из консоли. Диагностический признак: 403 «Forbidden» = ключ живой, но от ДРУГОГО пространства; 401 «Invalid API-key» = ключ удалён. Новая переменная в `.env` — проверять правку прогоном дочернего процесса с подставленной старой переменной в окружении, а не глазами. ⚠️ В Docker переменные задают окружением: появится там ещё и `.env` — он теперь перебьёт настройки контейнера.
- **Окно консоли при перезапуске (2026-07-24, просьба Максима — вернуть как было)** → в Windows новая копия запускается с `creationflags=subprocess.CREATE_NEW_CONSOLE`: своё чистое окно, старое закрывается вместе с процессом. Без флага Windows отдаёт копии окно РОДИТЕЛЯ, и логи всех перезапусков идут в одно окно сплошной лентой (так вело себя с ~20.07 — заметил Максим 24.07). Флаг существует ТОЛЬКО в Windows, на других системах `Popen` бросит ValueError — проверка `sys.platform == "win32"` обязательна, словарь `extra` пустой во всех прочих случаях. На ФАЙЛЫ логов это не влияет никогда: имя файла даёт `logging_setup`, каждый запуск пишет свой. ⚠️ Побочный эффект: если новая копия падает на старте, её окно закрывается мгновенно и человек видит только исчезнувшего бота — причину искать в `logs/` (setup_logging зовётся первым, до импорта модулей бота, поэтому в файл попадает почти всё).
- **Меняешь механику рестарта** → связка: кнопка ставит `bot_data["shutdown_reason"]="restart"` → post_stop молчит → main() после run_polling зовёт `_restart_self()`; при `IS_DOCKER=true` — просто выход.
- **⚡ IS_DOCKER теперь зависит от системы (2026-07-27)** → значение по умолчанию считается в `config.py`: на Windows `false` (бот сам сторожит единственность через `bot.pid` и сам порождает копию при перезапуске), на ЛЮБОЙ другой системе `true` (процессом управляет панель хостинга/контейнер). Переменная окружения перебивает автоопределение. Повод: на хостинге UkrLine кнопка «🔄 ПЕРЕЗАПУСТИТЬ БОТА» гасила бота и не поднимала — `_restart_self()` порождал копию через `subprocess.Popen`, та становилась дочерним процессом умирающего родителя, панель считала бота остановленным и добивала сироту. ⚠️ Не «упрощать» обратно к жёсткому `false`: на любом управляемом хостинге это ломает кнопку перезапуска. ⚠️ Оборотная сторона: если бот на Linux запускается руками из консоли (без супервизора), кнопка перезапуска станет кнопкой остановки — там нужен явный `IS_DOCKER=false`.

## 4. Хрупкие места (легко сломать по незнанию)

- **entities vs parse_mode** — указать оба = ошибка отправки. Модельные ответы — entities, свои тексты панелей — HTML parse_mode. Не смешивать.
- **callback_data ≤ 64 байта** — имена файлов БЗ туда не влезают, поэтому токены в `bot_data["kb_file_map"]`. После рестарта карта пуста → старые кнопки «протухают» (обработано: панель открывается заново). Любые новые кнопки с длинными данными — тот же паттерн.
- **settings — только строки.** `get_setting` возвращает str; забыть int() = тихий баг сравнения.
- **⚡ Соединение с базой одно на процесс (2026-07-27)** — и это меняет цену ошибки. Раньше забытый `commit()` или упавшая на середине функция просто теряли свою запись вместе с соединением; теперь недописанная транзакция остаётся в общем соединении. Страховка есть (`_DbLock` откатывает при вылетевшем исключении), но она НЕ спасает от двух вещей: забытого `commit()` (запись уедет в чужую транзакцию) и исключения, проглоченного ВНУТРИ блока замка (там нужен свой `rollback()`). Ещё: `close_db()` в `main.py::post_shutdown` стоит ПОСЛЕ остановки фоновых задач — перенесёшь выше, и задачи, ещё живые, полезут в закрытую базу. Само `close_db()` безопасно: после него первое же обращение откроет базу заново.
- **⚡ Сетевые сессии живут по потокам (2026-07-27)** — `services.http.session()` отдаёт разные объекты в разных потоках, и это НЕ баг. Не «оптимизировать» до одной общей сессии: запросы к моделям идут из рабочих потоков (`run_in_executor`), а `requests.Session` потокобезопасности не обещает. И не хранить сессию в переменной модуля (`_S = session()` на уровне файла) — она достанется одному потоку, а пользоваться ею начнут все.
- **`inline_keyboard` у InlineKeyboardMarkup — кортеж**: складывать клавиатуры только через `list(...) + list(...)`, иначе TypeError.
- **Групповой фильтр group=1 — TEXT|PHOTO|VOICE|AUDIO|VIDEO (с 2026-07-24)**: антиспам НЕ видит стикеры, кружочки и гифки. Расширяя фильтр — проверь, что collect_group_message переживёт новые типы.
- **Панельные админ-команды в группе молча выходят** — это не баг, а защита; не «чинить».
- **`_is_protected` при ошибке проверки статуса возвращает True (не мутить)** — не «оптимизировать» в обратную сторону.
- **bot.pid** — вторая копия убивает первую через taskkill. Запуск двух ботов для «теста» невозможен без IS_DOCKER=true. На Linux/хостинге файл вообще не создаётся: там IS_DOCKER=true по умолчанию (см. раздел 3).
- **Лимит Telegram ~15 сек на ответ callback** — долгие операции (пересборка RAG) идут через run_in_executor, а `_kb_popup` имеет запасной путь. Новые долгие кнопки делать так же.
- **Всплывающее окно кнопки (query.answer show_alert) — максимум 200 символов.** Длиннее → BadRequest Message_too_long, окно НЕ показывается, кнопка выглядит «мёртвой» (наступили 2026-07-05 на подсказке «Проверить поиск»). Вдобавок в PTB BadRequest наследуется от NetworkError, поэтому `main._error_handler` пишет такие ошибки в лог как «Временная сетевая ошибка» — не верь этой формулировке, проверяй текст ошибки.
- **Перерисовка клавиатуры «в то же самое» = ошибка Telegram** «Message is not modified»: кнопка-тумблер, нажатая в уже установленное положение, роняет предупреждение в лог и выглядит мёртвой. Перед `edit_message_reply_markup`/`edit_message_text` сравнивай новое состояние с текущим и молча выходи, если ничего не изменилось (сделано на кнопках выбора моделей 2026-07-19).
- **Тексты панелей ≤4096 символов** — превью промптов обрезаются до 600 (`PROMPT_PREVIEW_MAX`); сворачиваемая цитата ВСЁ РАВНО считается в лимит.
- **Надписи кнопок-тумблеров = ТОЛЬКО через `_onoff(состояние)` из handlers/admin/common.py** (стандарт 2026-07-20, решение Максима). Кнопка показывает ТЕКУЩЕЕ СОСТОЯНИЕ — «🟢ВКЛ» / «🔴ВЫКЛ», а НЕ действие: надписи вида «🔴 Выключить антиспам» (означало «сейчас включён», красный кружок при работающей защите) читались наоборот и все переделаны. В ТЕКСТЕ панелей остаются полные слова — «🟢 ВКЛЮЧЁН» / «🔴 ВЫКЛЮЧЕН», их `_onoff` не касается. Новый тумблер — брать метку отсюда, свою надпись не сочинять. ⚠️ ДВЕ настройки хранятся ПЕРЕВЁРНУТЫМИ: `admin_no_prompt_<id>` и `admin_no_rag_<id>` держат «1» при ВЫКЛЮЧЕННОЙ возможности — перед передачей в `_onoff` значение инвертируется (`!= "1"` / `not off`). (Тумблер «✅ Проверенный» с тремя состояниями удалён 2026-07-20 — все живые тумблеры теперь двухпозиционные и обязаны идти через `_onoff`.) Проверять правку тумблера прогоном обоих положений (сборка панели + печать надписи), а не глазами — на инверсии ошибиться проще всего.
- **Разделитель панелей = 27 символов `─` (U+2500)** — единый стандарт для ВСЕХ панелей (2026-07-06). Новые панели/строки-разделители делать ровно 27. НЕ путать с комментариями-разделителями в коде (45/47 символов `─` в шапках функций — они пользователю не видны, их не трогать).
- **Логи** — новый файл `logs/«дд.мм чч-мм».log` на каждый запуск; логи прошлых запусков не удаляются, а склеиваются в `logs/archive.log` (последние 7 сессий, `ARCHIVE_SESSIONS_TO_KEEP`). В папке всегда ровно два файла. Границы сессий в архиве размечены строкой-заголовком `_SESSION_MARK_PREFIX` — по ней же считаются сессии при обрезке: **поменяешь формат заголовка — старые куски архива перестанут распознаваться и обрезка их не тронет** (архив будет расти). Вызов `archive_old_logs()` в main.py стоит ПОСЛЕ остановки старой копии — перенесёшь его в `setup_logging`, и хвост лога умирающей копии будет теряться при каждом перезапуске. SQL-шум приглушён до INFO в logging_setup. Файла CLAUDE.md в проекте НЕТ (удалён) — единственная документация проекта — эта карта.
- **reset_db.py** — только при остановленном боте; KEEP_SETTINGS сейчас False (сотрёт и настройки/промпты!).
- **Русский язык** — все комментарии, логи и тексты пользователю только по-русски.

## 5. Самотест карты (в конце каждой сессии с правками)

Пройди список; при расхождении — обнови карту прямо сейчас:

1. `handlers/__init__.py`: список CommandHandler'ов совпадает с разделами 2.1–2.2? Новые MessageHandler'ы отражены?
2. `handlers/admin/router.py::handle_callback_query` + панели: все callback-префиксы (set_model, set_image_model, mod: (в т.ч. mod:clearlog / mod:clearlog_yes), proactive: (в т.ч. proactive:wipe / wipe_yes / wipe_no), usr: (в т.ч. usr:rights:, usr:role:…:offgo), kb_, adm_ (в т.ч. adm_open_users, adm_logs, adm_logs_file), toggle_, system_restart, adm_daily_report, adm_weekly_report (обе — в панели API, не в /adm), back_to_stats, prompts_files, prompt_*, news_prompt_*, rag_prompt_reset_confirm/cancel, proactive_prompt_reset_confirm/cancel, quiz_start, clear_history_btn) совпадают с разделом 2.3?
3. `config.py::AVAILABLE_MODELS` и раскладка `_MODEL_BUTTON_ROWS`/`_IMAGE_BUTTON_ROW` в handlers/admin/panel_main.py — синхронны? (это место расходится чаще всего)
4. `database/history.py::init_db` (число таблиц) == `reset_db.py::USER_TABLES` + settings? Сейчас: 20 таблиц, 19 в USER_TABLES.
4b. ⚡ Все обращения к базе — внутри `with _lock`, ни одного `conn.close()` в функциях, у каждой записи есть `commit()`? (проверяется скриптом-однодневкой: пройти файл и для каждого `_get_connection()` найти `with _lock` выше в той же функции — на 2026-07-27 было 74 из 74). Все сетевые запросы идут через `_http()`, а не `requests.post/get`? (`grep -rn "requests\.post(\|requests\.get(" services/ handlers/ *.py` должен находить только упоминания в комментариях `services/http.py`).
4a. Доступ: все админ-команды идут через `_require(update, context, право)`, а не через `if user_id not in ADMIN_IDS`? Каждый callback-префикс из раздела 2.3 есть в `_CALLBACK_RULES` (services/roles.py) с нужным правом — или намеренно оставлен владельцу по умолчанию? Панели персонала (/adm, /mod, список и карточка /users) прогоняются через `_filter_keyboard` — и ни одна из них не решает видимость кнопок сама, через `roles.can(...)`?
5. `services/antispam.py`: пороги, защита, улики, кнопка размута, ручные действия (mute_user/kick_user/ban_user/unban_user) — как в 2.4? Все типы действий журнала перечислены в словаре значков панели /mod?
6. `jobs.py`: интервалы (10 мин новости; отметки сторожу каждые 5 мин; чистка 10/7/30/30/400 дней — архив групп, модерация, база знаний, журнал персонала, снимки счётчиков; добор базы знаний раз в час; суточный отчёт о расходах в 00:00 по Киеву и недельный по понедельникам вторым сообщением; месячный сброс api_calls, расходов и user_token_usage по метке `stats_reset_month`, с переносом для суточного отчёта) — как в 2.5?
7. Появились новые файлы/папки — есть ли они в таблице раздела 1?
7a. `services/user_settings.py`: `load()` зовётся из main.py при старте? Все правки настроек идут через `set_field`/`clear` (не мимо кэша)? Читатели (`antispam`, `messages`, `proactive`, `media`, `quiz`, `panel_users`) берут значения из этого модуля, а не из БД?
8. Изменились ли скрытые связи (раздел 3) — например, новый «единый источник правды» или новый обязательный вызов после операции?
