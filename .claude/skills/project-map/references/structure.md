# Структура: файлы и за что отвечают


| Файл | За что отвечает |
|---|---|
| `main.py` | Запуск: логирование → PID-защита от двух копий → init_db → RAG-синк → Application с хуками post_init/post_stop/post_shutdown → polling; кнопка рестарта перезапускает бота через `_restart_self()` (в Windows — в НОВОМ окне консоли, см. часть `references/dependencies-*.md`; НА СЕРВЕРЕ копия не порождается вовсе — процесс завершается с кодом 1, и его поднимает systemd, см. часть `references/server.md`) |
| `config.py` | Все дефолты: токены из .env, реестр моделей `AVAILABLE_MODELS` (provider: gemini/qwen/deepseek; поле vision — кто принимает картинки: Gemini/Gemma да, Qwen/DeepSeek нет — их API только текст, сверено 2026-07-10), `FALLBACK_MODEL`, `AUDIO_FALLBACK_CHAIN`, модели картинок Nano Banana `AVAILABLE_IMAGE_MODELS` + цены `IMAGE_PRICES`, `ADMIN_IDS`, лимит картинок (3/день), `GEMINI_TIMEOUT` 60с + `GEMINI_STREAM_DEADLINE` 90с (общий предел на весь потоковый ответ Qwen/DeepSeek), дефолты антиспама, пути и дефолты RAG (`RAG_MIN_SIMILARITY` 0.58, `RAG_TOP_K` 3 — живые значения в settings; `RAG_CHUNK_MODE` "file", константы гибридного поиска `RAG_LEX_BOOST`/`RAG_STRONG_SIM` и `RAG_PEAK_MARGIN` (0.14 с 2026-07-19 — ДЕФОЛТ живого регулятора «Запас над фоном», значение в settings `rag_peak_margin`; в комментарии рядом записан замер, которым выбрано число), `RAG_INSTRUCTION` — заводская «шапка»-инструкция RAG, живой текст в settings `rag_instruction`, правится из /prompt), `DEEPSEEK_PRICES` (сверены 2026-07-04) и `QWEN_PRICES` (сверены 2026-07-05; ⚠️ qwen3.7-max — АКЦИОННАЯ цена −50%) — цены $/1 млн токенов, зашиты руками; адрес и ключ Xiaomi MiMo `XIAOMI_API_URL` / `XIAOMI_API_KEY` (2026-07-25) с ценами `XIAOMI_PRICES`; адрес и ключ OpenRouter `OPENROUTER_API_URL` / `OPENROUTER_API_KEY` (2026-08-01) с ценами `OPENROUTER_PRICES` — ⚠️ у бесплатного варианта модели они НУЛЕВЫЕ и это не заглушка, а основной счёт денег идёт вообще не по ним, а по `usage.cost` из ответа; РЕЕСТР ПРОВАЙДЕРОВ `PROVIDERS` (2026-08-03 — ЕДИНЫЙ ИСТОЧНИК: значок, имя, заголовки блоков, ключи копилки и остатка, ссылка на кабинет, `monthly_reset`/`report_approx`/`quota_tokens`; порядок записей задаёт порядок блоков в панели API и в отчётах) и собираемые из него значки `PROVIDER_ICONS` (♊️ gemini / 🎨 image / 🐪 qwen / 🐋 deepseek / 🍚 xiaomi / 🧭 openrouter) + `PROVIDER_ICON_FALLBACK` (🤖, модель не из конфига) + `RAG_ICON` (📚) — ЕДИНЫЙ источник значков для логов и панели API; `BACKUP_DIR` / `BACKUP_KEEP` (2026-07-31, папка и число ночных копий базы); `BOT_BUILD` — метка сборки из файла `.build` (2026-07-31; пустая, если файла нет: дома, на первом запуске, вне git-клона) и зависящий от неё `BOT_VERSION_HTML` — со меткой ссылка ведёт на САМ КОММИТ, без метки на тег `vX.YZ` (у версий не с ПК Максима тега нет, и ссылка ведёт в никуда); `PROACTIVE_LOG_DAYS` (2026-07-31, срок хранения журнала проактивных проверок); `QUIZ_RANKS` — ЕДИНЫЙ список званий викторины (имя, значок, границы min/max верных ответов, характеристика; 2026-07-20 вынесен сюда из `get_user_stats`): читают его расчёт звания в history.py и кнопки выбора ПОЧЁТНОГО звания в карточке /users; границы обязаны идти по возрастанию без «дыр» (max предыдущего = min следующего −1), иначе попавший в дыру получит первое звание списка; блок «Сам в разговор» `PROACTIVE_*` (дефолты тумблера/порога, `PROACTIVE_CONTEXT_MSGS` 25, `PROACTIVE_MUTE_MAX_SEC` 86400 (24 ч, с 2026-07-21 было 300), маркер `PROACTIVE_SKIP_MARKER` «ПРОПУСК», заводская инструкция `PROACTIVE_INSTRUCTION` с блоком рук внутри — живой текст в settings `proactive_instruction`; судья удалён, бывший ключ `hands_instruction` мёртв) |
| `logging_setup.py` | Единая настройка логов: DEBUG в консоль + файл сессии `logs/«дд.мм чч-мм».log` (новый на каждый запуск). С 2026-07-20 в папке `logs` всегда ДВА файла: текущая сессия и общий архив `archive.log` — `archive_old_logs()` дописывает логи прошлых запусков в конец архива (каждый под заголовком `_session_mark` «════ СЕССИЯ дд.мм чч-мм ════»), удаляет их и обрезает архив до `ARCHIVE_SESSIONS_TO_KEEP` (=7) последних сессий (`_trim_archive` — построчно, через временный файл и `os.replace`; счёт сессий идёт ПО ЗАГОЛОВКАМ). Зовётся НЕ из `setup_logging`, а из `main.py` после `_kill_existing_instance()` — пока старая копия жива, её лог дописывается и последние строки в архив бы не попали; все ошибки глушатся (перенос логов не должен мешать запуску). Путь к файлу текущей сессии хранится в `CURRENT_LOG_FILE` (читает кнопка «💾 Текущий лог»), путь к архиву — `_archive_path()` (читает кнопка «🗄 Архив логов»); легенда эмодзи-значков (с 2026-07-19 у нейросетей значок ПО ПРОВАЙДЕРУ: ♊️/🐪/🐋, поиск по базе — 📚, общий 🤖 остался только запасным); импортируется в main.py ДО всего остального |
| `jobs/` | ПАКЕТ фоновых задач (2026-08-04 файл jobs.py на 837 строк разрезан ПО ЦИКЛАМ, дословно: шесть циклов не звали друг друга вовсе). `__init__.py` — оглавление и re-export, снаружи пакет выглядит как прежний модуль (`from jobs import cleanup_loop` работает как работал). Части: `news.py` (`news_polling_loop` каждые 10 мин; `send_news_to_chat` после успешной отправки в ГРУППУ (chat_id < 0) пишет сводку в архив групп через `save_group_message` — БЕЗ `note_bot_group_reply`, намеренно: новость не должна откладывать проактивное участие в её обсуждении), `cleanup.py` (`cleanup_loop` — суточная чистка: архив групп 10 дн, модерация 7 дн, журнал БЗ 30 дн, журнал персонала и проактивных проверок 30 дн, снимки счётчиков 400 дн; + месячный сброс статистики API `_monthly_stats_reset`), `reports.py` (`daily_report_loop` — суточный отчёт в 00:00 по Киеву владельцу в личку, по понедельникам ВТОРЫМ сообщением недельный: проверка `week_closed()` стоит ПОСЛЕ суточного, чтобы последние сутки успели попасть в копилку; спит до полуночи кусками не больше часа, чтобы пережить спящий компьютер и перевод часов; ТРЕТЬИМ шлёт ночную копию базы — `nightly_backup` в том же файле, своим try и со своей меткой дня: отчёт мог не уйти вовсе, а копия нужна в любом случае), `rag.py` (`rag_catchup_loop` — ежечасный добор базы знаний; при выключенном RAG завершается сразу), `update.py` (`auto_update_loop` — САМООБНОВЛЕНИЕ раз в 10 минут тем же `deploy.sh`, что кнопка и `.bat`), `watchdog.py` (`watchdog_loop` — «отметки живости» внешнему сторожу; работает ВСЕГДА, даже без `WATCHDOG_URL`: без него пропускается только отметка наружу, а местная метка `bot.alive` пишется по-прежнему). ⚠️ Новая фоновая задача — СВОЙ файл + имя в re-export `jobs/__init__.py`, иначе `from jobs import …` в main.py упадёт при старте. ⚠️ Новый пакет проекта надо дописывать и в `preflight._PACKAGES`, иначе его модули выпадут из проверок |
| `services/backup.py` | 💾 2026-07-31. НОЧНАЯ КОПИЯ БАЗЫ: сжатие, имена, ротация, метка «за какой день копия сделана». ⚠️ САМ SQLITE НЕ ТРОГАЕТ — снимок делает `history.backup_to` (правило «единственный модуль с SQLite» не нарушено); полезешь сюда с sqlite3 напрямую — снимешь копию мимо общего замка, то есть в разгар чужой записи. `make_backup()` → (путь, размер): снимок → gzip → удаление промежуточного .tmp → `_rotate()` (оставляет `BACKUP_KEEP` штук, сортировка ПО ИМЕНИ — дата в начале имени, время файла врёт при переносе). `due_today()` / `note_done()` — метка в settings `backup_last_date` (дата по Киеву): она и защищает от повтора при перезапусках, и ДОГОНЯЕТ пропущенную ночь, как у суточного отчёта. `backup_dir()` считает путь ОТ ЭТОГО ФАЙЛА, а не от рабочей папки (у службы systemd она чужая — те же грабли, что с VERSION и .env сторожа). ⚠️ Модуль НЕ «тихий», в отличие от antispam/proactive/deploy: ошибки летят наружу. Копия, о которой доложили «готово», а её нет, хуже честного «не смог» — разбираться будут в тот единственный день, когда она понадобится. Киевское время берёт у `daily_report.kyiv_now` (второго расчёта не заводить — разъедутся на переводе часов) |
| `preflight.py` | ✅ 2026-07-31. ПРЕДПОЛЁТНЫЕ ПРОВЕРКИ перед выкаткой; зовёт их `deploy.sh` после `compileall`, руками — `python preflight.py` (0 = можно, 1 = нельзя). Восемь проверок: импорт ВСЕХ модулей проекта; модели ↔ кнопки ↔ цепочки подстраховки (+ поле `video` у видео-цепочки); провайдеры (с 2026-08-03 — полнота записи в реестре `config.PROVIDERS` и попадание денежных провайдеров в кортежи порядка `_BALANCE_ORDER`/`_COST_ORDER`; прежняя проверка искала имя провайдера в ТЕКСТЕ функций панели и отчётов и после реестра стала бессмысленной); таблицы init_db ↔ `reset_db.USER_TABLES`; границы `QUIZ_RANKS`; КНОПКИ ↔ РОУТЕР; сборка 10 панелей с замером лимитов Telegram; регистрация обработчиков. ⚠️ ГЛАВНОЕ: НИКОГДА не трогает боевую базу — `main()` первым делом переводит `history.DB_PATH` на временную папку и убирает её за собой; новая проверка, пишущая в БД, обязана идти ПОСЛЕ этого перевода. ⚠️ ВТОРОЕ: не ходит в сеть и не требует ключей (Application собирается с ненастоящим токеном правильной формы) — проверка, падающая из-за молчащего Google, однажды откатит исправное обновление. ⚠️ Ветки роутера читаются ИЗ `router.py` разбором ast (`_router_patterns`), свой список веток не заводить: это второй источник правды, он разъедется в первую же правку. `callback_data`, собранные f-строкой, проверяются по неизменному началу. ⚠️ Поломки печатаются ЕЩЁ РАЗ в самом конце — `deploy.sh` кладёт в сообщение об откате `tail -3`, без повтора Максим получил бы «поломок 3» без единого слова о том, каких |
| `services/daily_report.py` | СУТОЧНЫЙ И НЕДЕЛЬНЫЙ ОТЧЁТЫ О РАСХОДАХ (с 2026-08-03 блоки провайдеров, снимки счётчиков и расход за период собираются ИЗ РЕЕСТРА `config.PROVIDERS` — руками там больше ничего не перечислено) (суточный 2026-07-24, недельный 2026-07-25 — в том же файле, отдельного модуля намеренно нет): собирает текст отчёта по счётчикам панели «📡 Настройки API» за период. Панельные счётчики накопительные, поэтому раз в сутки снимается их «фотография» (`collect_counters` → `stats_snapshots`), а расход = РАЗНИЦА снимков; вызовы берутся точно, по времени из `api_calls` (`hist.count_api_calls_between`). Главное: `midnight_report()` (закрывает период, шлёт текст, ставит новый снимок, сохраняет текст в settings `daily_report_last_text` для кнопки), `period_closed()` (единственное условие отправки — «по Киеву наступили новые сутки с момента снимка»: ловит и пропущенную полночь, и не даёт слать отчёт на каждый перезапуск), `start_snapshot_if_needed()` (первый запуск — только точка отсчёта, без отчёта), `today_so_far()` (живой хвост кнопки «сколько набежало с полуночи»), `note_monthly_reset()` (перенос — см. часть `references/dependencies-*.md`), `last_report_text()`. Подписи периода: ровные сутки → «24 июля 2026 (00:00–24:00 по Киеву)», иначе честный диапазон + предупреждение «бот был выключен в полночь». НЕДЕЛЬНАЯ ЧАСТЬ (2026-07-25, нижний раздел файла): цифры считает `period_totals` (одна на оба отчёта), рисует `render` (тоже одна — иначе отчёты разъедутся по виду); `week_add_day` (зовётся из `midnight_report` — закрытые сутки идут в копилку `weekly_report_acc`), `week_closed()` (понедельник по Киеву ЛИБО ≥7 суток с начала копилки — страховка на проспанный понедельник), `weekly_report()` (собирает, сохраняет текст в `weekly_report_last_text`, копилку обнуляет), `week_so_far()` (живой хвост кнопки), `last_weekly_text()`. Подпись недели — «20–26 июля 2026 (по Киеву) · дней: 7» + предупреждение, если суток не семь; внизу строка «📈 В среднем в день» (делится на РЕАЛЬНОЕ число суток) |
| `utils.py` | `should_respond_in_group` (mention/reply, в т.ч. в подписи к фото), `clean_mention`, `keep_chat_action` (статус «печатает…»), `register_and_clean_bot_message` (новая панель удаляет старую), `delete_user_message_safe`, `schedule_delete`, `mention` |
| `utils_format.py` | Ответы модели → (text, entities) через telegramify-markdown; `<thought>` → сворачиваемая цитата; `send_formatted` (нарезка длинных по границам entity), `convert_md`, `reply_md`, `fits_caption`, `strip_thoughts`. С 2026-08-03 — ЖИВОЙ ТУМБЛЕР ПОКАЗА МЫСЛЕЙ: `thoughts_enabled()` (ключ settings `THOUGHTS_SETTING_KEY` = `thoughts_enabled`, дефолт "1", семантика ПРЯМАЯ) проверяется В ОДНОМ месте — `build_text_and_entities`, единственном, где цитата вообще собирается; выключен — мыслей нет ни в личке, ни в группах, ни в «Сам в разговор». ⚠️ Прячет ТОЛЬКО показ: параметры размышлений моделям не трогает, токены тратятся как прежде |
| `watchdog_local.py` | 🐕 2026-07-27. ⛔ **СЕЙЧАС ВЫКЛЮЧЕН** (решение Максима 2026-07-27 вечером: уведомления healthchecks.io в Телеграм и на Gmail достаточно, местный дублировал их). Задание планировщика в состоянии Disabled — файл цел и не запускается ничем; включить обратно: `schtasks /change /tn "C4_Max Bot Watchdog" /enable`. Всё описанное ниже — про то, как он работает, КОГДА включён. МЕСТНЫЙ сторож: отдельный скрипт, который запускает ЗАДАНИЕ ПЛАНИРОВЩИКА Windows «C4_Max Bot Watchdog» раз в минуту (`pythonw.exe` — без мигающего окна), а внутри одного запуска делается ТРИ проверки с шагом `CHECK_STEP_SEC`=20 сек: минута — предел планировщика, и без этого она была бы пределом скорости реакции. Смотрит, жив ли бот (процесс из `bot.pid` + свежесть метки `bot.alive`), и при падении пишет владельцу в Telegram НАПРЯМУЮ через Bot API. ⚠️ НАМЕРЕННО НЕ ЗАВИСИТ ОТ КОДА БОТА (ни handlers, ни services, ни базы — сторож, ломающийся вместе с подопечным, бесполезен); единственное исключение — `config.py`, откуда берутся ТОКЕН и ADMIN_IDS, чтобы не заводить их вторую копию. ⚠️ `os.kill(pid, 0)` для проверки процесса использовать НЕЛЬЗЯ: в Windows это ЗАВЕРШАЕТ процесс — сторож убил бы бота; процесс проверяется через ctypes (`OpenProcess` + `QueryFullProcessImageNameW`). ⚠️ И НИКАКИХ ВНЕШНИХ ПРОГРАММ через subprocess: задание работает под `pythonw.exe` без консоли, и каждый запуск консольной программы МИГАЕТ ОКНОМ (наступили 2026-07-27 на `tasklist` — 180 миганий в час). Память между запусками — `watchdog.state`, свой лог — `watchdog.log` (в папку `logs` не лезет: там всегда ровно два файла). Ручная проверка: `python watchdog_local.py --test` — пришлёт пробное сообщение и ничего больше не сделает |
| `reset_db.py` | Сброс таблиц БД (только при остановленном боте); флаг `KEEP_SETTINGS`; список `USER_TABLES` — 19 таблиц (+settings при KEEP_SETTINGS=False) |
| `handlers/__init__.py` | `setup_handlers`: регистрация ВСЕХ команд и хендлеров. ЧЕТЫРЕ ГРУППЫ: group=0 — основные; group=1 — `collect_group_message` (TEXT\|PHOTO\|VOICE\|AUDIO\|VIDEO — голос/аудио с 2026-07-21, видео с 2026-07-24); group=−1 — `log_incoming_command` (регистратор команд); group=−2 — `_note_chat_activity` (дешёвая отметка «в чатах пишут» для самообновления, `filters.ALL`; ОТДЕЛЬНАЯ группа, а не −1, иначе отобрала бы команды у регистратора). Незарегистрированная команда попадает в `handle_unknown_command`. С 2026-07-13 ПЯТЬ ДОЛГИХ хендлеров (handle_message, handle_photo, handle_voice, handle_video с 2026-07-24, cmd_imagine) зарегистрированы с `block=False` — не держат очередь апдейтов, пока ждут нейросеть; всё остальное обрабатывается строго по одному |
| `handlers/commands.py` | Публичные: /start, /help, /clear, /subscribe, /unsubscribe; `handle_unknown_command` (молча удаляет); `log_incoming_command` (2026-07-13) — центральный регистратор: пишет в лог КАЖДУЮ входящую команду (значок ⌨️), включая неизвестные и админские от не-админов; зарегистрирован в group=-1 (раньше основных), никогда не бросает исключения. С 2026-07-27 он же ГАСИТ ожидание числа для экрана «💰 Счета и квоты» (`user_data["balance_edit"]`) — единственное место, где это ловится для ВСЕХ команд сразу |
| `handlers/admin/` | ПАКЕТ (2026-07-13 монолит admin.py ~2140 строк разрезан дословно; бэкап admin.py.bak-before-split из корня УДАЛЁН — сверка 2026-07-21 его не нашла, откатываться не на что; шапка `handlers/admin/__init__.py` на него ещё ссылается). `__init__.py` — «оглавление»: re-export всех имён, снаружи пакет выглядит как прежний handlers.admin. Части: `common.py` (гейт админа, `_send_panel_message`, чтение логов, `_fmt_mod_time`), `router.py` (`handle_callback_query` — ЕДИНСТВЕННЫЙ роутер всех callback-кнопок), `panel_main.py` (панель /adm, /stats, модели/API + раскладки `_MODEL_BUTTON_ROWS`/`_IMAGE_BUTTON_ROW`; ), `panel_balance.py` (2026-08-04 — экран «💰 Счета и квоты», выделен из panel_main: свой префикс кнопок `bal:`, реестры `_BALANCE_FIELDS`/`_COST_FIELDS` из реестра провайдеров, кортежи порядка `_BALANCE_ORDER`/`_COST_ORDER`, `_handle_balance_callback` и перехват ввода числа `handle_balance_input`), `panel_prompts.py` (панель промптов + команды /prompt_*, /news_prompt_*, /rag_prompt_*, /proactive_prompt_* — с 2026-08-04 ВОСЕМЬ команд обслуживают два общих исполнителя `_prompt_set_command` / `_prompt_reset_command` по таблице `_PROMPTS`, сами команды остались тонкими обёртками ради регистрации в PTB; с 2026-08-03 там же ИСПОЛНИТЕЛЬ СБРОСА ПРОМПТОВ — таблица `_PROMPTS` и `handle_prompt_reset` на все восемь кнопок подтверждения/отмены, роутер только диспетчер; тумблер и регуляторы «Сам в разговор» — `_handle_proactive_callback`, `_PROACTIVE_LIMITS`), `panel_mod.py` (модерация, /unmute), `panel_rag.py` (база знаний, `handle_kb_document`, `_end_kb_test`, «Проверить поиск»), `panel_users.py` (2026-07-20 — раздел «👥 Пользователи»: список `_build_users_panel` + карточка участника `_build_user_card`, роутер `_handle_users_callback` с префиксом `usr:`, команда /users) |
| `handlers/media.py` | /imagine: генерация картинки (Nano Banana — `generate_image` в services/gemini.py), лимит 3/день (админ — безлимит), статус «отправляет фото…». Лимит списывается АВАНСОМ до генерации (`register_image_call`), при неудаче попытка возвращается (`unregister_image_call`) — защита от залпа /imagine при неблокирующем хендлере |
| `handlers/messages.py` | Текст/фото/голос/видео (`handle_video`, 2026-07-24 — только `message.video`, кружочки и гифки намеренно не берём) → ИИ-ответ; тумблер «ответы ИИ» глушит ТОЛЬКО личку админа; `collect_group_message` — архив всех сообщений групп (TEXT/PHOTO/VOICE/AUDIO/VIDEO; различает `has_photo`/`has_voice` с 2026-07-21 и `has_video` с 2026-07-24) + личное дело (dossier_add_message) + антиспам + фильтр ссылок (только если антиспам не замутил) + хук проактивного режима `proactive.consider_message` (только если не было ни мута, ни удаления ссылки). Прямые ответы бота в группах пишутся в архив помощником `_archive_bot_group_reply` (свои сообщения апдейтами не приходят — без этого стенограмма не видела бы реплик бота) |
| `handlers/quiz.py` | /rank (личное дело, звания, прогресс-бар); викторины Quiz Poll: автоудаление через 60 сек, после ответа следующий вопрос через 2.5 сек (отложенной задачей `application.create_task`, НЕ sleep в обработчике — иначе стояла бы вся очередь), повышение звания → поздравление в ЛС |
| `database/history.py` | ЕДИНСТВЕННЫЙ модуль с SQLite (`history.db`, WAL + `synchronous=NORMAL`, каждый запрос логируется на DEBUG). С 2026-08-03 `init_db` РАЗЛОЖЕН НА ТРИ ШАГА: `_create_schema` (таблицы и индексы), `_run_column_migrations` (кортеж `_COLUMN_MIGRATIONS` — графы, появившиеся позже создания базы), `_seed_once` (разовые засевы known_chats и user_token_usage); сам `init_db` только берёт замок, зовёт три шага по порядку и коммитит. ⚡ С 2026-07-27 СОЕДИНЕНИЕ ОДНО НА ВЕСЬ ПРОЦЕСС: `_conn` + ленивое открытие в `_get_connection` (там же оба PRAGMA), настоящее закрытие — `close_db()` из `main.py::post_shutdown`, а `close()` у `_LoggingConnection` ПУСТОЙ (заслон: случайный `conn.close()` в новой функции убил бы базу для всего бота). Замок `_lock` — не простой threading.Lock, а обёртка `_DbLock`: при ошибке внутри `with _lock` она ОТКАТЫВАЕТ недописанную транзакцию (при общем соединении та висела бы и уехала в чужой commit). Реентерабельности НЕТ — `get_setting`/`set_setting` звать только ВНЕ `with _lock`. 21 таблица (2026-07-31 добавлена `proactive_log` — журнал проактивных проверок: `log_proactive_check` / `proactive_stats(start,end)` / `proactive_by_chat` / `proactive_by_day` / `delete_old_proactive_log`; пишутся ТОЛЬКО дошедшие до модели проверки, отсев фильтрами живёт в памяти proactive.py). `backup_to(path)` (2026-07-31) — снимок базы для ночной копии штатным `Connection.backup` под общим замком: ⚠️ обычным копированием файла базу в режиме WAL брать НЕЛЬЗЯ (свежие записи лежат в history.db-wal, копия выйдет отставшей, а при попадании на запись — битой). Ошибки не глушит. `add_provider_cost(провайдер, сумма)` (2026-08-03) — ОДНА копилка расхода на всех вместо пяти почти одинаковых `add_*_cost`: ключи `cost_key`/`balance_key` берёт из реестра `config.PROVIDERS`, прибавление и вычитание идут прямо в SQL (атомарно, без гонки между worker-потоками); провайдер без копилки молча пропускается. `delete_setting(key)` (2026-07-27) — УДАЛЯЕТ настройку целиком, для случаев, где «не задано» ≠ «ноль» (остатки счетов и квоты Qwen: пока ключа нет, вычитающие UPDATE его не находят, а с нулём остаток ушёл бы в минус). ПОЛНЫЙ СПИСОК ТАБЛИЦ — 21 штука, ровно столько же считает `preflight.py` (в `reset_db.USER_TABLES` их 20: `settings` там отдельно, под флагом KEEP_SETTINGS). Из них 2026-07-24 добавлена `stats_snapshots` (снимки счётчиков для суточного отчёта: `save_stats_snapshot` / `get_last_stats_snapshot` / `get_prev_stats_snapshot` / `count_api_calls_between` / `delete_old_stats_snapshots`; последний снимок чистка НЕ удаляет — от него отсчитывается текущий период): messages, user_context, sent_news, news_subscriptions, quiz_stats, bot_sent_messages, settings, api_calls, user_image_calls, group_messages (с 2026-07-21 графа `has_voice` — голосовые в стенограмме проактивного режима, с 2026-07-24 графа `has_video`; обе доставлены миграцией ALTER TABLE), user_token_usage, moderation_log (с 2026-07-17 графа admin_name — кто снял мут; у мутов/ссылок автоматики пусто), mute_evidence, knowledge_log, user_dossier (ВЕЧНОЕ личное дело участника групп: first_seen-стаж, счётчики сообщений/мутов/ссылок — не чистится никем; с 2026-07-20 графы username/first_name — имя для списка пользователей, архив групп чистится через 10 дней и именами больше не источник), user_settings и known_chats (2026-07-20, раздел «👥 Пользователи»). `user_settings` — персональные настройки участника, ПУСТОЕ поле = «работает общая настройка бота» (пороги антифлуда, лимит картинок, звание); белый список изменяемых колонок `_USER_SETTING_FIELDS`, функции get/set/clear_user_settings + get_all_user_settings (загрузка кэша). `known_chats` — группы бота (chat_id, title, last_seen; `remember_chat`/`get_known_chats`), нужны карточке, чтобы знать, ГДЕ мутить/кикать; одноразово засеяны из архива групп в init_db, название подтягивается при первом сообщении или при открытии карточки. `list_known_users` — сводный список всех, кого знает бот (личное дело + архив групп + викторина + учёт токенов; люди из лички тоже попадают). `staff` (2026-07-20) — МОДЕРАТОРЫ и их права: графы p_mod / p_ban / p_cards / p_cards_edit / p_antispam + granted_by/granted_at; владельцев здесь НЕТ (они в config.ADMIN_IDS); белый список колонок `_STAFF_PERM_FIELDS`, функции get_all_staff / get_staff / add_staff / remove_staff / set_staff_perm — но звать их напрямую нельзя, только через services/roles.py (иначе кэш прав отстанет). `staff_log` (2026-07-20) — ЖУРНАЛ ДЕЙСТВИЙ ПЕРСОНАЛА (ts, actor_id, actor_name, action, target_id, details): сюда пишутся ТОЛЬКО осознанные действия человека — и владельца, и модераторов, включая смену настроек и выдачу прав (в moderation_log, для сравнения, пишет и автоматика). Функции log_staff_action / get_recent_staff_actions / count_staff_actions / delete_old_staff_log / clear_staff_log; хранится `STAFF_LOG_DAYS`(30) дней, чистит cleanup_loop. Журнал МОДЕРАЦИИ чистят двое: `delete_old_moderation_log` (по сроку, cleanup_loop) и `clear_moderation_log` (2026-07-20, кнопка «🧹 Очистить журнал» в /mod) — оба стирают `mute_evidence` ВМЕСТЕ с записями журнала: улики привязаны к `log_id`, без журнала до них уже не добраться. Для проактивного режима: `get_recent_group_messages(chat_id, limit)` (стенограмма чата, индекс idx_group_messages_chat; уважает «черту» — см. ниже) и `get_proactive_instruction()` (своя из settings или заводская — как get_rag_instruction). `set_proactive_reset_mark()` (2026-07-20, кнопка «🧹Очистить РАЗГОВОРЫ») — «ЧЕРТА» под стенограммой: пишет в settings ключ `proactive_reset_mark` (время самой SQLite, `SELECT CURRENT_TIMESTAMP` — формат обязан совпадать с `created_at`, иначе сравнение строк перестанет быть хронологическим), после чего `get_recent_group_messages` добавляет условие `created_at > метка`. НИЧЕГО НЕ УДАЛЯЕТ — архив цел, счётчик /stats и имена для /users не страдают; обратимо стиранием ключа |
| `services/http.py` | ⚡ 2026-07-27. Переиспользуемые сетевые соединения: `session()` отдаёт `requests.Session` ТЕКУЩЕГО ПОТОКА (thread-local — запросы к моделям идут из рабочих потоков, а Session потокобезопасности не обещает; потоки в пуле переиспользуются, поэтому TLS-«рукопожатие» в каждом случается один раз за жизнь бота). Заменяет прямые `requests.post/get` в gemini.py, rag.py, scraper.py — раньше каждый запрос заново делал рукопожатие (замерено 130–200 мс до Google на КАЖДЫЙ вызов). ⚠️ `max_retries=0` НАМЕРЕННО: у бота своя логика повторов и подстраховки в gemini.py, повторы здесь умножились бы на те и превратили таймаут 60 с в минуты молчания |
| `services/gemini.py` | Прямые REST-вызовы через переиспользуемую сессию `services.http.session()` (импортируется как `_http`; НЕ SDK — намеренно!). 2026-08-01: пятый провайдер OpenRouter — `_openrouter_chat_request` (размышления полем `reasoning`, а не `enable_thinking`/`thinking`; в запросе `"usage": {"include": True}`) и `_openrouter_cost` (берёт ФАКТИЧЕСКИ списанное `usage.cost` из ответа, `OPENROUTER_PRICES` — запасной путь). Заодно `_openai_stream_request` читает мысли из ОБОИХ полей — `reasoning_content or reasoning`. `ask_gemini` (текст/фото), `ask_gemini_audio` (цепочка `AUDIO_FALLBACK_CHAIN`), `format_news_as_colonel`, `generate_image`. Qwen и DeepSeek ходят через общий потоковый OpenAI-совместимый запрос `_openai_stream_request` (мысли из reasoning_content → `<thought>`; общий предел `GEMINI_STREAM_DEADLINE`=90с на весь поток — иначе «висяк» при подтекающих данных, см. часть `references/features.md` (пункт 2.5)); параметр включения рассуждений у каждого свой (enable_thinking / thinking.type). Для DeepSeek и Qwen считается точная стоимость запроса (`_deepseek_cost` / `_qwen_cost`: кэш-поля usage × `DEEPSEEK_PRICES`/`QWEN_PRICES`; у Qwen кэш из `prompt_tokens_details.cached_tokens`, на практике часто 0 — неявный кэш Alibaba «без гарантий») — пишется в лог строкой «≈$…» и копится в settings (`deepseek_cost_usd` / `qwen_cost_usd`, атомарно через `hist.add_deepseek_cost`/`add_qwen_cost`; `add_deepseek_cost` с 2026-07-21 заодно вычитает сумму из остатка баланса `deepseek_balance_usd` — ключа нет, вычитать нечего, UPDATE молча пропускается). Учёт токенов сверен с кабинетами всех 3 провайдеров 2026-07-05 — совпадение токен в токен. 2 попытки активной модели (запасные в цепочке — по одной; НО на таймаут — сразу к следующей, без второй попытки, см. часть `references/features.md` (пункт 2.5)) → фолбэк-цепочка «перекрёстной подстраховки»: активна Gemini → `FALLBACK_MODEL`, затем `gemini-3.1-flash-lite`, в конце `qwen3.7-plus`; активна Qwen/DeepSeek → сразу бесплатные Gemini-lite; active_model НЕ меняется, админам уведомление ≤1 раз/час, юзеру — `SOFT_FAIL_MESSAGE`. VISION-REROUTE (2026-07-10): фото при активной модели БЕЗ поля vision (DeepSeek/Qwen) в неё не отправляется вовсе — анализ сразу идёт цепочке Gemini (2026-07-24 порядок задан Максимом и совпадает с аудио/видео: gemini-3.5-flash → `FALLBACK_MODEL` → gemini-3.1-flash-lite), БЕЗ уведомления админам (штатный режим, как у аудио; `has_image` в `_gemini_chat_request`, помощник `_supports_vision`). `ask_group_proactive` (2026-07-16) — проактивный запрос «Сам в разговор»: стенограмма чата + характер + RAG по триггеру + инструкция участия, ответ либо реплика (С блоком `<thought>` — в чате станет свёрнутыми «Мыслями»), либо маркер «ПРОПУСК» (`_is_proactive_skip` — проверяется по видимой части ответа, без мыслей); идёт через АКТИВНУЮ модель с обычной цепочкой подстраховки (решение Максима 2026-07-16, принудительная бесплатная цепочка force_chain удалена в тот же день), add_messages НЕ вызывается. С 2026-07-21: `_proactive_describe_image` (лёгкий vision-запрос к gemini-3.1-flash-lite, без истории/RAG) и `_proactive_transcribe_audio` (native generateContent к gemini-3.1-flash-lite) — анализ медиа для проактивного триггера; время ответа логируется на INFO. Стенограмма в `_build_proactive_parts` подменяет `[фото]`/`[голосовое]` реальным текстом анализа для последнего сообщения |
| `services/roles.py` | ЕДИНСТВЕННЫЙ источник правды О ДОСТУПЕ (2026-07-20). Три уровня: ВЛАДЕЛЕЦ (`config.ADMIN_IDS`, из панели не меняется — иначе управление ботом можно потерять), МОДЕРАТОР (таблица `staff` + галочки прав), обычный участник. Права `PERMS` — ЧЕТЫРЕ (2026-07-20): `mod` (мут/размут, журнал, улики), `ban` (бан/разбан И КИК), `cards` (смотреть карточки), `cards_edit` (лимит картинок, обнуление нарушений, почётное звание). Право `antispam` УДАЛЕНО — общие пороги и тумблеры только владельцу; графа p_antispam осталась в БД, но не читается. Связка `_IMPLIES`: `cards_edit` включает `cards`, снятие `cards` снимает `cards_edit`. Кэш в памяти, `load()` из main.py; менять права только через `make_moderator`/`unmake_moderator`/`grant_perm` (пишут в БД И в кэш). Вопросы: `is_owner`, `is_moderator`, `is_staff`, `role_of`, `can(uid, право)`, `has_any_perm`, `perms_of`, `list_moderators`, `can_act_on(actor, target)` (иерархия: владелец — ко всем, модератор — только к обычным участникам, не к себе). **ТАБЛИЦА ДОСТУПА К КНОПКАМ** `_CALLBACK_RULES` + `perm_for_callback` + `may_press` — ЕДИНОЕ место, где записано, какое право нужно для какой кнопки; ветки `usr:pick/step/do` разбираются отдельно (право зависит от действия внутри данных, `_ACTION_PERMS`). ЗАПРЕТ ПО УМОЛЧАНИЮ: кнопка, которой нет в таблице, доступна ТОЛЬКО владельцу |
| `services/user_settings.py` | ЕДИНСТВЕННЫЙ источник правды о персональных настройках участников (2026-07-20, карточка /users). Держит таблицу `user_settings` ЦЕЛИКОМ В ПАМЯТИ: настройки нужны на каждое сообщение группы, а на горячем пути бот в БД не ходит (замерено: обращений к БД на сообщение поровну — с персональными настройками и без). `load()` — один раз при старте из main.py сразу после init_db; `set_field(user_id, поле, значение)` — ЕДИНСТВЕННЫЙ разрешённый способ записи (пишет в БД И обновляет кэш), `clear(user_id)`, `refresh(user_id)`, `get(user_id)` → dict ({} = всё общее; при незагруженном кэше сам зовёт load один раз). Смысловые помощники, которыми пользуется остальной код: `thresholds_for`, `is_immune`, `links_allowed`, `ai_ignored`, `image_limit_for` (None = безлимит у админов), `honorary_rank` (`trusted_override` удалён 2026-07-20 вместе с ручным статусом «проверенный»). Модуль «тихий»: ошибка = «персональных настроек нет» |
| `services/antispam.py` | Антифлуд + фильтр ссылок: см. часть `references/features.md` (пункт 2.4). Входы — `check_and_mute()` и `check_and_delete_links()` из collect_group_message; счётчики в памяти, журнал/улики в БД. Размер всплеска считает ОДИН помощник `_count_messages` (2026-07-19): альбом фото = одно отправление |
| `services/proactive.py` | (2026-07-31 добавлен СЧЁТ УЧАСТИЯ: `_skip(причина)` на каждом фильтре и `skip_counts()` для панели — счётчик в ПАМЯТИ, в базу с этого пути не ходим; в `_run_proactive` — помощник `_note(исход)`, пишущий строку в `proactive_log` в тех же ветках, где стоят строки лога. Вид триггера и активная модель берутся до запроса; ⚠️ в журнал идёт АКТИВНАЯ модель, а не та, что реально ответила: при отказе отвечает запасная из цепочки, и знает об этом только gemini.py.) Режим «Сам в разговор» (2026-07-16): бот сам решает, вступить ли в беседу группы — см. 2.5. `consider_message` (СИНХРОННАЯ, зовётся из collect_group_message: дешёвые фильтры → защёлка → отдельная задача; с 2026-07-21 передаёт `has_photo`/`has_voice` и `file_id` в `_run_proactive`), `_run_proactive` (с 2026-07-21: ДО вызова `ask_group_proactive` анализирует медиа-триггер — фото через `_proactive_describe_image`, голосовое через `_proactive_transcribe_audio`; результат подставляется как `trigger_text`; при пустом результате проверка пропускается; запрос модели через executor БЕЗ «печатает…»; при реплике — пауза 1.5–6с с «печатает…» по длине ВИДИМОЙ части → send_formatted с Reply, мысли уходят в чат свёрнутым блоком → своя реплика в архив групп БЕЗ мыслей, strip_thoughts), `note_bot_group_reply` (обновление паузы после ПРЯМЫХ ответов), `is_enabled`, `forget_conversations` (2026-07-20 — сбрасывает счётчик `_msgs_since_check` после «Забыть разговоры»; паузы и защёлку `_in_flight` НЕ трогает намеренно: сброс пауз только приблизил бы проверку, а снятие защёлки открыло бы дорогу второй параллельной). Счётчики/паузы в памяти (обнуляются рестартом, как у антиспама); модуль «тихий» — наружу не бросает никогда. Значок логов 🤖 (с 2026-07-21, было 🗣) |
| `services/scraper.py` | wtmobile.com (всегда версия /ru/): `fetch_latest_news` (список), `fetch_article` (полный текст + главная картинка + карточки ТТХ) |
| `services/knowledge_store.py` | ВСЕ файловые операции с knowledge/: save_pending_news, list_articles, approve/add/replace/delete_article, read_title. С 2026-07-18 — тип статьи для панели /rag: `ARTICLE_KINDS` (единственное место, где заданы значок и порядок групп: 🚜 ground 0, ✈️ air 1, 🛥️ ship 2, ⚓ sub 3, 📘 guide 4, 📄 other 5), `detect_kind(descriptor)` (по ПЕРВОЙ СТРОКЕ ОПИСАНИЯ статьи — где есть «War Thunder Mobile»; порядок правил `_KIND_PATTERNS` важен: «истребитель танков» ловится наземным правилом ДО авиационного), `_read_meta(path)` → (title, kind) одним чтением файла (read_title — обёртка над ним, старые вызовы работают). `list_articles` кладёт в каждый элемент поле `kind` и сортирует ОДОБРЕННЫЕ по (порядок группы, заголовок); pending остаётся новыми сверху |
| `services/rag.py` | База знаний (⚡ 2026-07-27: длина каждого вектора считается ОДИН раз при загрузке в память — поле `norm` в `_load_into_memory`, счёт сходства через `_cosine_fast(вектор, длина, вектор, длина)` вместо `cosine_similarity`; поиск 30.3 → 12.3 мс, числа совпадают до бита, калибровка остаётся верной. `cosine_similarity` оставлена как есть — ею больше никто не пользуется). Режим нарезки `RAG_CHUNK_MODE` (дефолт "file" — 1 статья = 1 чанк, решение владельца 2026-07-05; "sections" — по `## `), эмбеддинги gemini-embedding-2, индекс `knowledge/knowledge_base_vectors.json` (формат 2 + метка chunk_mode: смена режима → авто-пересборка; инкрементально по mtime); `sync_knowledge_base` (дёшево), `rebuild_knowledge_base` (всё заново). ГИБРИДНЫЙ поиск (2026-07-05, откалиброван 32/32): балл = косинус + лексический бонус (`RAG_LEX_BOOST` 0.10 × доля основ слов запроса, найденных в статье; грубый стемminг `_stem`, слова ≥4 букв, минус `_QUERY_STOPWORDS` и минус сверхчастые основы `_COMMON_STEMS` — в >половине чанков). Отбор `_chunk_passes(score, baseline, floor, margin)`: балл ≥ порога (вспомогательный, панель /rag) И (балл ≥ `RAG_STRONG_SIM` 0.70 ИЛИ отрыв от медианы всех чанков ≥ запаса — «пик против полки»; запас живой, `_live_peak_margin()`, регулятор панели /rag, дефолт 0.14). `test_search` — диагностика для «🔍 Проверить поиск». Устойчивость к лимиту Google (2026-07-19): при индексации 429 → паузы/повторы `_INDEX_RETRY_DELAYS` (10/30/60 сек) + `_INDEX_PAUSE_SEC` (1 сек) между статьями, при исчерпанной квоте `RagQuotaError` и досрочная остановка sync; sync/rebuild возвращают итог (проиндексировано, всего); `index_lag()` — дешёвая проверка полноты индекса для ежечасного добора (без запросов к Google); эмбеддинги запросов пользователей не ждут никогда |
| `data/quiz_questions.py` | Вопросы викторины + `get_random_question()` |
| `knowledge/pending/` | Новости, ждущие одобрения админа в БЗ |
| `knowledge/approved/` | Одобренные статьи — источник RAG (по одному .md на статью) |
| `.env` | TELEGRAM_TOKEN, GEMINI_API_KEY, GEMINI_IMAGEN_API_KEY, QWEN_API_KEY, DEEPSEEK_API_KEY, XIAOMI_API_KEY, WATCHDOG_URL (2026-07-27, адрес отметки healthchecks.io; пустой = наружу не отмечаемся), RAG_ENABLED (мёртвая переменная TRANSLATE_THOUGHTS удалена 2026-07-09 — код её никогда не читал). ⚠️ Ключей ночной копии базы здесь НЕТ: `BACKUP_DIR` и `BACKUP_KEEP` живут в config.py, настраивать их через окружение незачем |
| `requirements.txt` | Внешние библиотеки бота: `python-telegram-bot` (движок), `requests` (запросы к нейросетям и сайту), `beautifulsoup4` (парсинг новостей), `python-dotenv` (чтение `.env`), `telegramify-markdown` (форматирование ответов), `tzdata` (часовой пояс Киева для суточного лимита картинок) |
| `deploy.sh` | ⬇️ 2026-07-27. ЕДИНСТВЕННОЕ место, где записано, КАК бот обновляется на сервере: запомнить текущий коммит → `git fetch` → сравнить с `origin/main` → `merge --ff-only` → доставить библиотеки, если изменился `requirements.txt` → ПРОВЕРИТЬ, что код запускается (три шага: `compileall -f` → загрузка четырёх ключевых модулей → `preflight.py` целиком, с 2026-07-31; файла проверок нет — шаг пропускается, чтобы откат на старую версию не валился) → при беде `git reset --hard` назад. Печатает строки `КЛЮЧ=значение` (`STATUS` = UPDATED/NOCHANGE/ROLLBACK/NETFAIL/FAIL/BUSY, `VERSION`, `WAS`, `MSG`, `ERR`). С 2026-07-31 на ЛЮБОМ пути выхода пишет метку сборки `.build` (короткий номер коммита, который реально лежит на сервере) — через `trap … EXIT`, чтобы её нельзя было забыть на новом пути выхода; файл в `.gitignore`. ⚠️ Бота НЕ перезапускает — это дело позвавшего. ⚠️ Работает только от имени `c4bot` (папка бота принадлежит ему; git от root наплодил бы там root-овых файлов). Зовут его двое: `services/deploy.py` (кнопка) и `/usr/local/bin/c4max-update` (компьютер Максима) |
| `deploy-restart.sh` | ⬇️ 2026-07-27. Тонкая обёртка «обновить и перезапустить» для вызова с ПК одной строкой `ssh c4max c4max-update`. Берёт `deploy.sh` ИЗ `origin/main` (а не из папки бота) и запускает от `c4bot`; перезапускает службу ТОЛЬКО при `STATUS=UPDATED` — иначе бот замолчал бы на 10 секунд впустую. ⚠️ РАБОЧАЯ КОПИЯ ЛЕЖИТ НЕ В РЕПОЗИТОРИИ, а в `/usr/local/bin/c4max-update`; здесь исходник ради истории правок. Поправил — переустанови: `scp deploy-restart.sh root@сервер:/usr/local/bin/c4max-update && ssh root@сервер chmod 755 /usr/local/bin/c4max-update` |
| `services/deploy.py` | ⬇️ 2026-07-27. Обёртка над `deploy.sh` для кнопки «🔄 ПЕРЕЗАПУСТИТЬ БОТА»: `can_update()` (на Windows ВСЕГДА False — дома скачивание дралось бы с ещё не отправленными правками Максима), `update()` (запуск в отдельном потоке, разбор ответа), `describe()` (готовая строка со значком). Модуль «тихий»: наружу не бросает никогда |
| `VERSION` | 🔢 2026-07-27. Номер версии бота одной строкой (сейчас `3.24`) — ЕДИНСТВЕННОЕ место, где он записан. Читает его `config.py::_read_version` → `BOT_VERSION` (путь считается ОТ config.py, а не от рабочей папки: у службы systemd на сервере она чужая; файл пропал или испорчен → `?`, бот всё равно стартует). Показывается в заголовке `/adm` и в сообщении «✅ Бот запущен». Последнюю цифру поднимает САМ `Отправить изменения на GitHub.bat` при каждой отправке (решение Максима: повод — ЛЮБАЯ правка, значит человек однажды забудет), первую — вручную по случаю крупного изменения; после 3.99 идёт 4.00. Той же строкой `.bat` ставит метку на GitHub (`v3.02`) — ради читаемого отката. ⚠️ ЗНАЙ ЕГО СЛЕПОЕ ПЯТНО (2026-07-31): правки, приехавшие ЧЕРЕЗ PULL REQUEST из сессии Claude, номер НЕ поднимают — `.bat` при этом не запускается, и метки на GitHub тоже не появляется. Раньше это молча ломало весь смысл номера («не сменился → сервер не забрал»); теперь рядом показывается МЕТКА СБОРКИ `.build`, которая меняется всегда. Номер остаётся человеческой пометкой: правишь из сессии — подними его в том же коммите, как это делалось в сессии по воздушной тревоге |
