# Сервер: где бот живёт и как им управлять (с 2026-07-27)


Боевое место бота — **не компьютер Максима, а арендованный сервер**. Дома бот
обычно выключен: две копии с одним токеном Telegram работать не могут.

| Что | Где |
|---|---|
| Хостинг | s-host.com.ua, тариф VPSn-1 (2 ядра, 2 ГБ ОЗУ, 30 ГБ диска), KVM |
| Адрес | `185.235.219.94`, имя хоста `vpswuff3.s-host.host` |
| Система | Ubuntu 24.04, часовой пояс Europe/Kyiv |
| Пользователь бота | `c4bot` (НЕ root — если бота однажды поломают, машина не достанется) |
| Папка бота | `/home/c4bot/bot` |
| Питон бота | `/home/c4bot/bot/.venv/bin/python` (Python 3.12) |
| Служба | `c4max-bot` (systemd), `Restart=always` |
| Вход | только по ключу с ПК Максима; короткое имя `c4max` (задано в `~/.ssh/config`) |

**Команды, которые нужны чаще всего** (выполняются с ПК Максима):

```
ssh c4max "systemctl status c4max-bot"     # жив ли
ssh c4max "journalctl -u c4max-bot -n 50"  # свежие строки лога
ssh c4max "systemctl restart c4max-bot"    # перезапустить
ssh c4max c4max-update                     # забрать код с GitHub и перезапустить
```

**Чем сервер отличается от домашнего компьютера — важное для правок кода:**

- `IS_DOCKER` там **True** (считается автоматически: не Windows → управляемая
  среда). Отсюда: `bot.pid` не пишется, старые копии не убиваются, а кнопка
  «🔄 ПЕРЕЗАПУСТИТЬ БОТА» просто завершает процесс с кодом 1 — поднимает его
  systemd. Уберёшь `Restart=always` из службы — кнопка станет кнопкой
  выключения.
- Логи идут **в journalctl**, а не только в файлы `logs/`. Файлы тоже пишутся,
  их отдаёт кнопка «📜 Логи бота».
- **Windows-приспособления там не работают и не нужны:** `watchdog_local.py`
  (задание планировщика, ctypes), новое окно консоли при перезапуске,
  `taskkill` по `bot.pid`. Код это уже учитывает — не «чинить».
- Снаружи открыт **только SSH** (ufw), вход по паролю выключен, подбор паролей
  банит `fail2ban`. Откроешь боту веб-порт — не забудь `ufw allow`.
- `.env` и `history.db` лежат на сервере и **через GitHub не ездят никогда**.
  Меняешь ключ — меняй на сервере отдельно.
- Папка `backups/` (ночные копии базы, 2026-07-31) — там же и тоже мимо
  GitHub: это те же живые данные. Вторая копия с 2026-08-10 лежит **в Google
  Drive** (папка `C4Max-Backups`) — при переезде на другой сервер
  восстанавливаться проще оттуда. До 2026-08-10 вторую копию Максим получал
  файлом в личку каждую ночь; теперь файл выдаёт только кнопка «💾 Копия базы».
- **☁️ rclone — разовая ручная настройка, как `.env`** (2026-08-10). Программа
  ставится из apt (`apt install rclone`), место назначения `gdrive` заводится
  командой `rclone config` **от пользователя `c4bot`** (`sudo -u c4bot -H
  rclone config`) — не от root: бот работает под `c4bot` и читает ключ из его
  домашней папки, `/home/c4bot/.config/rclone/rclone.conf` (права 600, мимо
  GitHub). Google не пускает по паролю: при настройке нужен браузер и живой
  человек — на вопрос «Use auto config?» отвечаем **n**, дальше `rclone
  authorize "drive"` запускается НА КОМПЬЮТЕРЕ с браузером, а полученная
  строка вставляется на сервере. ⚠️ Пароли и коды доступа Google вводит
  Максим сам — я их не ввожу и не храню. Проверка, что всё живо:
  `sudo -u c4bot -H rclone lsf gdrive:C4Max-Backups`.
- Файлы, которые бот перезаписывает сам, **не должны быть в репозитории**
  (см. `knowledge/knowledge_base_vectors.json` в части `references/dependencies-*.md`) — иначе обновление
  упрётся в «файл изменён и там, и тут».
- Управление самим сервером (стоп/старт/перезагрузка, VNC, переустановка) —
  в личном кабинете s-host. **«Стоп» + «Старт» безопасны:** бот поднимется сам,
  ничего не теряется; сторож healthchecks.io пришлёт тревогу «не отвечает» и
  потом «снова работает» — это ожидаемо, он не отличает выключение от аварии.
