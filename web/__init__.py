# ───────────────────────────────────────────────
#  web/__init__.py — ОГЛАВЛЕНИЕ пакета веб-админки (сайта).
#
#  Сайт живёт внутри процесса бота: фоновая задача jobs/web.py поднимает
#  aiohttp на 127.0.0.1, наружу его выводит Caddy с HTTPS. Смысл такого
#  устройства и предупреждение про WEB_HOST — в config.py, блок «ВЕБ-АДМИНКА».
#
#  Что где:
#    auth.py   — проверка, что вход правда от Telegram, и сессионная кука
#    pages.py  — сборка HTML: сводка, страница входа, отказ
#    routes.py — таблица адресов сайта и обработчики
#    static/   — оформление (CSS), отдаётся как есть
#
#  ⚠️ Новый адрес сайта — дописывать в web/routes.py::ROUTES, иначе
#  preflight.py::check_web о нём не узнает и проверять его будет некому.
# ───────────────────────────────────────────────

from .routes import ROUTES, build_app

__all__ = ["ROUTES", "build_app"]
